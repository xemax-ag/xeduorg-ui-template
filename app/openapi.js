// Auto-generated by core/openapi/build_openapi_models_js.py - do not edit.
// Source: http://localhost:8078/openapi.json, converted with OpenAPI Generator
// (https://openapi-generator.tech), bundled into one ES module with named exports.

export class ApiClient {
    static parseDate(str) {
        if (isNaN(str)) {
            return new Date(str.replace(/(\d)(T)(\d)/i, '$1 $3'));
        }
        return new Date(+str);
    }

    static convertToType(data, type) {
        if (data === null || data === undefined)
            return data

        switch (type) {
            case 'Boolean':
                return Boolean(data);
            case 'Integer':
                return parseInt(data, 10);
            case 'Number':
                return parseFloat(data);
            case 'String':
                return String(data);
            case 'Date':
                return ApiClient.parseDate(String(data));
            case 'Blob':
                return data;
            default:
                if (type === Object) {
                    // generic object, return directly
                    return data;
                } else if (typeof type.constructFromObject === 'function') {
                    // for model type like User and enum class
                    return type.constructFromObject(data);
                } else if (Array.isArray(type)) {
                    // for array type like: ['String']
                    var itemType = type[0];

                    return data.map((item) => {
                        return ApiClient.convertToType(item, itemType);
                    });
                } else if (typeof type === 'object') {
                    // for plain object type like: {'String': 'Integer'}
                    var keyType, valueType;
                    for (var k in type) {
                        if (type.hasOwnProperty(k)) {
                            keyType = k;
                            valueType = type[k];
                            break;
                        }
                    }

                    var result = {};
                    for (var k in data) {
                        if (data.hasOwnProperty(k)) {
                            var key = ApiClient.convertToType(k, keyType);
                            var value = ApiClient.convertToType(data[k], valueType);
                            result[key] = value;
                        }
                    }

                    return result;
                } else {
                    // for unknown type, return the data directly
                    return data;
                }
        }
    }

    static constructFromObject(data, obj, itemType) {
        if (Array.isArray(data)) {
            for (var i = 0; i < data.length; i++) {
                if (data.hasOwnProperty(i))
                    obj[i] = ApiClient.convertToType(data[i], itemType);
            }
        } else {
            for (var k in data) {
                if (data.hasOwnProperty(k))
                    obj[k] = ApiClient.convertToType(data[k], itemType);
            }
        }
    }
}

/**
 * The Aoe2Cur model module.
 * @module model/Aoe2Cur
 * @version 0.9.0
 */
class Aoe2Cur {
    /**
     * Constructs a new <code>Aoe2Cur</code>.
     * Link table connecting areas of operational expertise to curricula.
     * @alias module:model/Aoe2Cur
     * @param fkProject {Number} Reference to the owning project this link belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        Aoe2Cur.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>Aoe2Cur</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Aoe2Cur} obj Optional instance to populate.
     * @return {module:model/Aoe2Cur} The populated <code>Aoe2Cur</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Aoe2Cur();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('fk_aoe')) {
                obj['fk_aoe'] = ApiClient.convertToType(data['fk_aoe'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur')) {
                obj['fk_cur'] = ApiClient.convertToType(data['fk_cur'], 'Number');
            }
            if (data.hasOwnProperty('tsl_label')) {
                obj['tsl_label'] = ApiClient.convertToType(data['tsl_label'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_label_txt')) {
                obj['tsl_label_txt'] = ApiClient.convertToType(data['tsl_label_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Aoe2Cur</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Aoe2Cur</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Aoe2Cur.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_label_txt'] && !(typeof data['tsl_label_txt'] === 'string' || data['tsl_label_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_label_txt` to be a primitive type in the JSON string but got " + data['tsl_label_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }

        return true;
    }


}

Aoe2Cur.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying this area-of-expertise-to-curriculum link row.
 * @member {Number} pk
 */
Aoe2Cur.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
Aoe2Cur.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this link belongs to; cascades on delete.
 * @member {Number} fk_project
 */
Aoe2Cur.prototype['fk_project'] = undefined;

/**
 * Reference to the linked area of operational expertise; cascades on delete.
 * @member {Number} fk_aoe
 */
Aoe2Cur.prototype['fk_aoe'] = undefined;

/**
 * Reference to the linked curriculum; cascades on delete.
 * @member {Number} fk_cur
 */
Aoe2Cur.prototype['fk_cur'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this area-of-operational-expertise-to-curriculum link; logical reference, not a database foreign key.
 * @member {Number} tsl_label
 */
Aoe2Cur.prototype['tsl_label'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this area-of-operational-expertise-to-curriculum link; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
Aoe2Cur.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
Aoe2Cur.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
Aoe2Cur.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
Aoe2Cur.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
Aoe2Cur.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
Aoe2Cur.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
Aoe2Cur.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
Aoe2Cur.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
Aoe2Cur.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
Aoe2Cur.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this area-of-operational-expertise-to-curriculum link; logical reference, not a database foreign key.
 * @member {String} tsl_label_txt
 */
Aoe2Cur.prototype['tsl_label_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this area-of-operational-expertise-to-curriculum link; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
Aoe2Cur.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
Aoe2Cur.prototype['tsl_language'] = undefined;






export { Aoe2Cur };

/**
 * The Aoe2CurQuery model module.
 * @module model/Aoe2CurQuery
 * @version 0.9.0
 */
class Aoe2CurQuery {
    /**
     * Constructs a new <code>Aoe2CurQuery</code>.
     * Query payload for area-of-operational-expertise-to-curriculum junction rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/Aoe2CurQuery
     */
    constructor() { 
        
        Aoe2CurQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Aoe2CurQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Aoe2CurQuery} obj Optional instance to populate.
     * @return {module:model/Aoe2CurQuery} The populated <code>Aoe2CurQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Aoe2CurQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Aoe2CurQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Aoe2CurQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Aoe2CurQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Aoe2CurQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Aoe2CurQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
Aoe2CurQuery.prototype['language'] = undefined;






export { Aoe2CurQuery };

/**
 * The Aoe2CurView model module.
 * @module model/Aoe2CurView
 * @version 0.9.0
 */
class Aoe2CurView {
    /**
     * Constructs a new <code>Aoe2CurView</code>.
     * Aoe2Cur junction row extended with its nested area of operational expertise rows.
     * @alias module:model/Aoe2CurView
     * @param fkProject {Number} Reference to the owning project this link belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        Aoe2CurView.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>Aoe2CurView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Aoe2CurView} obj Optional instance to populate.
     * @return {module:model/Aoe2CurView} The populated <code>Aoe2CurView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Aoe2CurView();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('fk_aoe')) {
                obj['fk_aoe'] = ApiClient.convertToType(data['fk_aoe'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur')) {
                obj['fk_cur'] = ApiClient.convertToType(data['fk_cur'], 'Number');
            }
            if (data.hasOwnProperty('tsl_label')) {
                obj['tsl_label'] = ApiClient.convertToType(data['tsl_label'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_label_txt')) {
                obj['tsl_label_txt'] = ApiClient.convertToType(data['tsl_label_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
            if (data.hasOwnProperty('area_of_operational_expertises')) {
                obj['area_of_operational_expertises'] = ApiClient.convertToType(data['area_of_operational_expertises'], [AreaOfOperationalExpertiseView]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Aoe2CurView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Aoe2CurView</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Aoe2CurView.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_label_txt'] && !(typeof data['tsl_label_txt'] === 'string' || data['tsl_label_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_label_txt` to be a primitive type in the JSON string but got " + data['tsl_label_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }
        if (data['area_of_operational_expertises']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['area_of_operational_expertises'])) {
                throw new Error("Expected the field `area_of_operational_expertises` to be an array in the JSON data but got " + data['area_of_operational_expertises']);
            }
            // validate the optional field `area_of_operational_expertises` (array)
            for (const item of data['area_of_operational_expertises']) {
                AreaOfOperationalExpertiseView.validateJSON(item);
            };
        }

        return true;
    }


}

Aoe2CurView.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying this area-of-expertise-to-curriculum link row.
 * @member {Number} pk
 */
Aoe2CurView.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
Aoe2CurView.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this link belongs to; cascades on delete.
 * @member {Number} fk_project
 */
Aoe2CurView.prototype['fk_project'] = undefined;

/**
 * Reference to the linked area of operational expertise; cascades on delete.
 * @member {Number} fk_aoe
 */
Aoe2CurView.prototype['fk_aoe'] = undefined;

/**
 * Reference to the linked curriculum; cascades on delete.
 * @member {Number} fk_cur
 */
Aoe2CurView.prototype['fk_cur'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this area-of-operational-expertise-to-curriculum link; logical reference, not a database foreign key.
 * @member {Number} tsl_label
 */
Aoe2CurView.prototype['tsl_label'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this area-of-operational-expertise-to-curriculum link; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
Aoe2CurView.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
Aoe2CurView.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
Aoe2CurView.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
Aoe2CurView.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
Aoe2CurView.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
Aoe2CurView.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
Aoe2CurView.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
Aoe2CurView.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
Aoe2CurView.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
Aoe2CurView.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this area-of-operational-expertise-to-curriculum link; logical reference, not a database foreign key.
 * @member {String} tsl_label_txt
 */
Aoe2CurView.prototype['tsl_label_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this area-of-operational-expertise-to-curriculum link; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
Aoe2CurView.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
Aoe2CurView.prototype['tsl_language'] = undefined;

/**
 * Area of operational expertise rows
 * @member {Array.<module:model/AreaOfOperationalExpertiseView>} area_of_operational_expertises
 */
Aoe2CurView.prototype['area_of_operational_expertises'] = undefined;






export { Aoe2CurView };

/**
 * The Aoe2Curs model module.
 * @module model/Aoe2Curs
 * @version 0.9.0
 */
class Aoe2Curs {
    /**
     * Constructs a new <code>Aoe2Curs</code>.
     * Aoe2Cur result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/Aoe2Curs
     */
    constructor() { 
        
        Aoe2Curs.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Aoe2Curs</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Aoe2Curs} obj Optional instance to populate.
     * @return {module:model/Aoe2Curs} The populated <code>Aoe2Curs</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Aoe2Curs();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [Aoe2Cur]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Aoe2Curs</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Aoe2Curs</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                Aoe2Cur.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Aoe2Curs.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Aoe2Curs.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Aoe2Curs.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
Aoe2Curs.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
Aoe2Curs.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
Aoe2Curs.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
Aoe2Curs.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/Aoe2Cur>} rows
 */
Aoe2Curs.prototype['rows'] = undefined;






export { Aoe2Curs };

/**
 * The AppApiSkillsCommandsBestMatchSearchModelsDocument model module.
 * @module model/AppApiSkillsCommandsBestMatchSearchModelsDocument
 * @version 0.9.0
 */
class AppApiSkillsCommandsBestMatchSearchModelsDocument {
    /**
     * Constructs a new <code>AppApiSkillsCommandsBestMatchSearchModelsDocument</code>.
     * One BM25 search hit: the search text row that matched, the tsl_translations row it belongs to, the source table and attribute whose text was translated, and the BM25 score the row was ranked by.
     * @alias module:model/AppApiSkillsCommandsBestMatchSearchModelsDocument
     */
    constructor() { 
        
        AppApiSkillsCommandsBestMatchSearchModelsDocument.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AppApiSkillsCommandsBestMatchSearchModelsDocument</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AppApiSkillsCommandsBestMatchSearchModelsDocument} obj Optional instance to populate.
     * @return {module:model/AppApiSkillsCommandsBestMatchSearchModelsDocument} The populated <code>AppApiSkillsCommandsBestMatchSearchModelsDocument</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AppApiSkillsCommandsBestMatchSearchModelsDocument();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('tsl_pk')) {
                obj['tsl_pk'] = ApiClient.convertToType(data['tsl_pk'], 'Number');
            }
            if (data.hasOwnProperty('tsl_table_pk')) {
                obj['tsl_table_pk'] = ApiClient.convertToType(data['tsl_table_pk'], 'Number');
            }
            if (data.hasOwnProperty('tsl_table_name')) {
                obj['tsl_table_name'] = ApiClient.convertToType(data['tsl_table_name'], 'String');
            }
            if (data.hasOwnProperty('tsl_table_attribut_name')) {
                obj['tsl_table_attribut_name'] = ApiClient.convertToType(data['tsl_table_attribut_name'], 'String');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('bm25_score')) {
                obj['bm25_score'] = ApiClient.convertToType(data['bm25_score'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AppApiSkillsCommandsBestMatchSearchModelsDocument</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AppApiSkillsCommandsBestMatchSearchModelsDocument</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['tsl_table_name'] && !(typeof data['tsl_table_name'] === 'string' || data['tsl_table_name'] instanceof String)) {
            throw new Error("Expected the field `tsl_table_name` to be a primitive type in the JSON string but got " + data['tsl_table_name']);
        }
        // ensure the json data is a string
        if (data['tsl_table_attribut_name'] && !(typeof data['tsl_table_attribut_name'] === 'string' || data['tsl_table_attribut_name'] instanceof String)) {
            throw new Error("Expected the field `tsl_table_attribut_name` to be a primitive type in the JSON string but got " + data['tsl_table_attribut_name']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }

        return true;
    }


}



/**
 * Primary key (pk) of the row in the language-specific search text table (tsl_search_texts_<language>).
 * @member {Number} pk
 */
AppApiSkillsCommandsBestMatchSearchModelsDocument.prototype['pk'] = undefined;

/**
 * UUID of the tsl_translations row the search text belongs to; the derived search text tables carry no uuid of their own.
 * @member {String} uuid
 */
AppApiSkillsCommandsBestMatchSearchModelsDocument.prototype['uuid'] = undefined;

/**
 * Primary key (pk) of the tsl_translations row whose text was indexed.
 * @member {Number} tsl_pk
 */
AppApiSkillsCommandsBestMatchSearchModelsDocument.prototype['tsl_pk'] = undefined;

/**
 * tsl_translations.fk_table_pk -- primary key (pk) of the row in the source table whose attribute was translated.
 * @member {Number} tsl_table_pk
 */
AppApiSkillsCommandsBestMatchSearchModelsDocument.prototype['tsl_table_pk'] = undefined;

/**
 * tsl_translations.fk_table -- name of the source table whose attribute was translated; logical reference, not a database foreign key.
 * @member {String} tsl_table_name
 */
AppApiSkillsCommandsBestMatchSearchModelsDocument.prototype['tsl_table_name'] = undefined;

/**
 * tsl_translations.fk_attribute -- name of the attribute (column) in the source table whose text was translated.
 * @member {String} tsl_table_attribut_name
 */
AppApiSkillsCommandsBestMatchSearchModelsDocument.prototype['tsl_table_attribut_name'] = undefined;

/**
 * Text the BM25 index was built over: the copy of the <language> column of the tsl_translations row held in the search text table. This is what the query terms were matched against.
 * @member {String} text
 */
AppApiSkillsCommandsBestMatchSearchModelsDocument.prototype['text'] = undefined;

/**
 * BM25 relevance score of this row for the query, as computed by pg_search. It has no upper bound and no fixed scale -- it only ranks rows within one result set, so it is not comparable across queries or across languages. Rows are returned in descending score, so the first row is the best match.
 * @member {Number} bm25_score
 */
AppApiSkillsCommandsBestMatchSearchModelsDocument.prototype['bm25_score'] = undefined;






export { AppApiSkillsCommandsBestMatchSearchModelsDocument };

/**
 * The AppApiSkillsCommandsFullTextSearchModelsDocument model module.
 * @module model/AppApiSkillsCommandsFullTextSearchModelsDocument
 * @version 0.9.0
 */
class AppApiSkillsCommandsFullTextSearchModelsDocument {
    /**
     * Constructs a new <code>AppApiSkillsCommandsFullTextSearchModelsDocument</code>.
     * One full text search hit: the search text row that matched, the tsl_translations row it belongs to, the source table and attribute whose text was translated, and the rank the row was ordered by.
     * @alias module:model/AppApiSkillsCommandsFullTextSearchModelsDocument
     */
    constructor() { 
        
        AppApiSkillsCommandsFullTextSearchModelsDocument.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AppApiSkillsCommandsFullTextSearchModelsDocument</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AppApiSkillsCommandsFullTextSearchModelsDocument} obj Optional instance to populate.
     * @return {module:model/AppApiSkillsCommandsFullTextSearchModelsDocument} The populated <code>AppApiSkillsCommandsFullTextSearchModelsDocument</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AppApiSkillsCommandsFullTextSearchModelsDocument();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('tsl_pk')) {
                obj['tsl_pk'] = ApiClient.convertToType(data['tsl_pk'], 'Number');
            }
            if (data.hasOwnProperty('tsl_table_pk')) {
                obj['tsl_table_pk'] = ApiClient.convertToType(data['tsl_table_pk'], 'Number');
            }
            if (data.hasOwnProperty('tsl_table_name')) {
                obj['tsl_table_name'] = ApiClient.convertToType(data['tsl_table_name'], 'String');
            }
            if (data.hasOwnProperty('tsl_table_attribut_name')) {
                obj['tsl_table_attribut_name'] = ApiClient.convertToType(data['tsl_table_attribut_name'], 'String');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('rank')) {
                obj['rank'] = ApiClient.convertToType(data['rank'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AppApiSkillsCommandsFullTextSearchModelsDocument</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AppApiSkillsCommandsFullTextSearchModelsDocument</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['tsl_table_name'] && !(typeof data['tsl_table_name'] === 'string' || data['tsl_table_name'] instanceof String)) {
            throw new Error("Expected the field `tsl_table_name` to be a primitive type in the JSON string but got " + data['tsl_table_name']);
        }
        // ensure the json data is a string
        if (data['tsl_table_attribut_name'] && !(typeof data['tsl_table_attribut_name'] === 'string' || data['tsl_table_attribut_name'] instanceof String)) {
            throw new Error("Expected the field `tsl_table_attribut_name` to be a primitive type in the JSON string but got " + data['tsl_table_attribut_name']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }

        return true;
    }


}



/**
 * Primary key (pk) of the row in the language-specific search text table (tsl_search_texts_<language>).
 * @member {Number} pk
 */
AppApiSkillsCommandsFullTextSearchModelsDocument.prototype['pk'] = undefined;

/**
 * UUID of the tsl_translations row the search text belongs to; the derived search text tables carry no uuid of their own.
 * @member {String} uuid
 */
AppApiSkillsCommandsFullTextSearchModelsDocument.prototype['uuid'] = undefined;

/**
 * Primary key (pk) of the tsl_translations row whose text was indexed.
 * @member {Number} tsl_pk
 */
AppApiSkillsCommandsFullTextSearchModelsDocument.prototype['tsl_pk'] = undefined;

/**
 * tsl_translations.fk_table_pk -- primary key (pk) of the row in the source table whose attribute was translated.
 * @member {Number} tsl_table_pk
 */
AppApiSkillsCommandsFullTextSearchModelsDocument.prototype['tsl_table_pk'] = undefined;

/**
 * tsl_translations.fk_table -- name of the source table whose attribute was translated; logical reference, not a database foreign key.
 * @member {String} tsl_table_name
 */
AppApiSkillsCommandsFullTextSearchModelsDocument.prototype['tsl_table_name'] = undefined;

/**
 * tsl_translations.fk_attribute -- name of the attribute (column) in the source table whose text was translated.
 * @member {String} tsl_table_attribut_name
 */
AppApiSkillsCommandsFullTextSearchModelsDocument.prototype['tsl_table_attribut_name'] = undefined;

/**
 * Text the tsvector was built from: the copy of the <language> column of the tsl_translations row held in the search text table. This is what the query was matched against.
 * @member {String} text
 */
AppApiSkillsCommandsFullTextSearchModelsDocument.prototype['text'] = undefined;

/**
 * Relevance of this row for the query, from ts_rank() over the row's tsvector: it rises with how often and how prominently the query terms occur in the text, and falls off as the text gets longer. It is not normalised to any range and only orders rows within one result set, so it is not comparable across queries or across languages. Rows are returned in descending rank, so the first row is the best match.
 * @member {Number} rank
 */
AppApiSkillsCommandsFullTextSearchModelsDocument.prototype['rank'] = undefined;






export { AppApiSkillsCommandsFullTextSearchModelsDocument };

/**
 * The AppApiSkillsCommandsSemanticSearchModelsDocument model module.
 * @module model/AppApiSkillsCommandsSemanticSearchModelsDocument
 * @version 0.9.0
 */
class AppApiSkillsCommandsSemanticSearchModelsDocument {
    /**
     * Constructs a new <code>AppApiSkillsCommandsSemanticSearchModelsDocument</code>.
     * One semantic search hit: the vector row that matched, the tsl_translations row it belongs to, the source table and attribute whose text was translated, and the score the row was ranked by.
     * @alias module:model/AppApiSkillsCommandsSemanticSearchModelsDocument
     */
    constructor() { 
        
        AppApiSkillsCommandsSemanticSearchModelsDocument.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AppApiSkillsCommandsSemanticSearchModelsDocument</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AppApiSkillsCommandsSemanticSearchModelsDocument} obj Optional instance to populate.
     * @return {module:model/AppApiSkillsCommandsSemanticSearchModelsDocument} The populated <code>AppApiSkillsCommandsSemanticSearchModelsDocument</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AppApiSkillsCommandsSemanticSearchModelsDocument();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('tsl_pk')) {
                obj['tsl_pk'] = ApiClient.convertToType(data['tsl_pk'], 'Number');
            }
            if (data.hasOwnProperty('tsl_table_pk')) {
                obj['tsl_table_pk'] = ApiClient.convertToType(data['tsl_table_pk'], 'Number');
            }
            if (data.hasOwnProperty('tsl_table_name')) {
                obj['tsl_table_name'] = ApiClient.convertToType(data['tsl_table_name'], 'String');
            }
            if (data.hasOwnProperty('tsl_table_attribut_name')) {
                obj['tsl_table_attribut_name'] = ApiClient.convertToType(data['tsl_table_attribut_name'], 'String');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('similarity')) {
                obj['similarity'] = ApiClient.convertToType(data['similarity'], 'Number');
            }
            if (data.hasOwnProperty('rerank_similarity')) {
                obj['rerank_similarity'] = ApiClient.convertToType(data['rerank_similarity'], 'Number');
            }
            if (data.hasOwnProperty('vector')) {
                obj['vector'] = ApiClient.convertToType(data['vector'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AppApiSkillsCommandsSemanticSearchModelsDocument</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AppApiSkillsCommandsSemanticSearchModelsDocument</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['tsl_table_name'] && !(typeof data['tsl_table_name'] === 'string' || data['tsl_table_name'] instanceof String)) {
            throw new Error("Expected the field `tsl_table_name` to be a primitive type in the JSON string but got " + data['tsl_table_name']);
        }
        // ensure the json data is a string
        if (data['tsl_table_attribut_name'] && !(typeof data['tsl_table_attribut_name'] === 'string' || data['tsl_table_attribut_name'] instanceof String)) {
            throw new Error("Expected the field `tsl_table_attribut_name` to be a primitive type in the JSON string but got " + data['tsl_table_attribut_name']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['vector'])) {
            throw new Error("Expected the field `vector` to be an array in the JSON data but got " + data['vector']);
        }

        return true;
    }


}



/**
 * Primary key (pk) of the row in the language-specific vector table (tsl_embeddings_<language>).
 * @member {Number} pk
 */
AppApiSkillsCommandsSemanticSearchModelsDocument.prototype['pk'] = undefined;

/**
 * UUID of the tsl_translations row the vector belongs to; the derived vector tables carry no uuid of their own.
 * @member {String} uuid
 */
AppApiSkillsCommandsSemanticSearchModelsDocument.prototype['uuid'] = undefined;

/**
 * Primary key (pk) of the tsl_translations row whose text was embedded.
 * @member {Number} tsl_pk
 */
AppApiSkillsCommandsSemanticSearchModelsDocument.prototype['tsl_pk'] = undefined;

/**
 * tsl_translations.fk_table_pk -- primary key (pk) of the row in the source table whose attribute was translated.
 * @member {Number} tsl_table_pk
 */
AppApiSkillsCommandsSemanticSearchModelsDocument.prototype['tsl_table_pk'] = undefined;

/**
 * tsl_translations.fk_table -- name of the source table whose attribute was translated; logical reference, not a database foreign key.
 * @member {String} tsl_table_name
 */
AppApiSkillsCommandsSemanticSearchModelsDocument.prototype['tsl_table_name'] = undefined;

/**
 * tsl_translations.fk_attribute -- name of the attribute (column) in the source table whose text was translated.
 * @member {String} tsl_table_attribut_name
 */
AppApiSkillsCommandsSemanticSearchModelsDocument.prototype['tsl_table_attribut_name'] = undefined;

/**
 * Text the vector was computed from, read from the <language> column of the tsl_translations row. Also what the rerank model scores the query against.
 * @member {String} text
 */
AppApiSkillsCommandsSemanticSearchModelsDocument.prototype['text'] = undefined;

/**
 * Cosine similarity between this row vector and the query vector, in the range -1 to 1. Rows are returned in descending similarity, so the first row is the closest match -- unless `rerank_result` was set, in which case `rerank_similarity` decides the order.
 * @member {Number} similarity
 */
AppApiSkillsCommandsSemanticSearchModelsDocument.prototype['similarity'] = undefined;

/**
 * Relevance score from the rerank model, set only when `rerank_result` was requested. It scores the query against the row text directly instead of comparing vectors, so it ranks more accurately than `similarity`. None on a row the rerank model could not score because its text is empty.
 * @member {Number} rerank_similarity
 */
AppApiSkillsCommandsSemanticSearchModelsDocument.prototype['rerank_similarity'] = undefined;

/**
 * Stored embedding vector of this row, 2048 dimensions. Only present when `return_vectors` was requested -- it is not even read from the database otherwise, because it is large.
 * @member {Array.<Number>} vector
 */
AppApiSkillsCommandsSemanticSearchModelsDocument.prototype['vector'] = undefined;






export { AppApiSkillsCommandsSemanticSearchModelsDocument };

/**
 * The AreaOfOperationalExpertise model module.
 * @module model/AreaOfOperationalExpertise
 * @version 0.9.0
 */
class AreaOfOperationalExpertise {
    /**
     * Constructs a new <code>AreaOfOperationalExpertise</code>.
     * Area of operational expertise within a skills project.
     * @alias module:model/AreaOfOperationalExpertise
     * @param fkProject {Number} Reference to the owning project this area of operational expertise belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        AreaOfOperationalExpertise.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>AreaOfOperationalExpertise</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AreaOfOperationalExpertise} obj Optional instance to populate.
     * @return {module:model/AreaOfOperationalExpertise} The populated <code>AreaOfOperationalExpertise</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AreaOfOperationalExpertise();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('tsl_title')) {
                obj['tsl_title'] = ApiClient.convertToType(data['tsl_title'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_title_txt')) {
                obj['tsl_title_txt'] = ApiClient.convertToType(data['tsl_title_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AreaOfOperationalExpertise</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AreaOfOperationalExpertise</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AreaOfOperationalExpertise.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_title_txt'] && !(typeof data['tsl_title_txt'] === 'string' || data['tsl_title_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_title_txt` to be a primitive type in the JSON string but got " + data['tsl_title_txt']);
        }

        return true;
    }


}

AreaOfOperationalExpertise.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the area of operational expertise row.
 * @member {Number} pk
 */
AreaOfOperationalExpertise.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
AreaOfOperationalExpertise.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this area of operational expertise belongs to; cascades on delete.
 * @member {Number} fk_project
 */
AreaOfOperationalExpertise.prototype['fk_project'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual title of the area of operational expertise; logical reference, not a database foreign key.
 * @member {Number} tsl_title
 */
AreaOfOperationalExpertise.prototype['tsl_title'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
AreaOfOperationalExpertise.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
AreaOfOperationalExpertise.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
AreaOfOperationalExpertise.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
AreaOfOperationalExpertise.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
AreaOfOperationalExpertise.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
AreaOfOperationalExpertise.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
AreaOfOperationalExpertise.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
AreaOfOperationalExpertise.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
AreaOfOperationalExpertise.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual title of the area of operational expertise; logical reference, not a database foreign key.
 * @member {String} tsl_title_txt
 */
AreaOfOperationalExpertise.prototype['tsl_title_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
AreaOfOperationalExpertise.prototype['tsl_language'] = undefined;






export { AreaOfOperationalExpertise };

/**
 * The AreaOfOperationalExpertiseView model module.
 * @module model/AreaOfOperationalExpertiseView
 * @version 0.9.0
 */
class AreaOfOperationalExpertiseView {
    /**
     * Constructs a new <code>AreaOfOperationalExpertiseView</code>.
     * AreaOfOperationalExpertise row extended with its nested ops_2_aoe_2_cur junction rows.
     * @alias module:model/AreaOfOperationalExpertiseView
     * @param fkProject {Number} Reference to the owning project this area of operational expertise belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        AreaOfOperationalExpertiseView.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>AreaOfOperationalExpertiseView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AreaOfOperationalExpertiseView} obj Optional instance to populate.
     * @return {module:model/AreaOfOperationalExpertiseView} The populated <code>AreaOfOperationalExpertiseView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AreaOfOperationalExpertiseView();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('tsl_title')) {
                obj['tsl_title'] = ApiClient.convertToType(data['tsl_title'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_title_txt')) {
                obj['tsl_title_txt'] = ApiClient.convertToType(data['tsl_title_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
            if (data.hasOwnProperty('ops_2_aoe_2_cur')) {
                obj['ops_2_aoe_2_cur'] = ApiClient.convertToType(data['ops_2_aoe_2_cur'], [Ops2Aoe2CurView]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AreaOfOperationalExpertiseView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AreaOfOperationalExpertiseView</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AreaOfOperationalExpertiseView.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_title_txt'] && !(typeof data['tsl_title_txt'] === 'string' || data['tsl_title_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_title_txt` to be a primitive type in the JSON string but got " + data['tsl_title_txt']);
        }
        if (data['ops_2_aoe_2_cur']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ops_2_aoe_2_cur'])) {
                throw new Error("Expected the field `ops_2_aoe_2_cur` to be an array in the JSON data but got " + data['ops_2_aoe_2_cur']);
            }
            // validate the optional field `ops_2_aoe_2_cur` (array)
            for (const item of data['ops_2_aoe_2_cur']) {
                Ops2Aoe2CurView.validateJSON(item);
            };
        }

        return true;
    }


}

AreaOfOperationalExpertiseView.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the area of operational expertise row.
 * @member {Number} pk
 */
AreaOfOperationalExpertiseView.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
AreaOfOperationalExpertiseView.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this area of operational expertise belongs to; cascades on delete.
 * @member {Number} fk_project
 */
AreaOfOperationalExpertiseView.prototype['fk_project'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual title of the area of operational expertise; logical reference, not a database foreign key.
 * @member {Number} tsl_title
 */
AreaOfOperationalExpertiseView.prototype['tsl_title'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
AreaOfOperationalExpertiseView.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
AreaOfOperationalExpertiseView.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
AreaOfOperationalExpertiseView.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
AreaOfOperationalExpertiseView.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
AreaOfOperationalExpertiseView.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
AreaOfOperationalExpertiseView.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
AreaOfOperationalExpertiseView.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
AreaOfOperationalExpertiseView.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
AreaOfOperationalExpertiseView.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual title of the area of operational expertise; logical reference, not a database foreign key.
 * @member {String} tsl_title_txt
 */
AreaOfOperationalExpertiseView.prototype['tsl_title_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
AreaOfOperationalExpertiseView.prototype['tsl_language'] = undefined;

/**
 * Operational skill to area of operational expertise rows
 * @member {Array.<module:model/Ops2Aoe2CurView>} ops_2_aoe_2_cur
 */
AreaOfOperationalExpertiseView.prototype['ops_2_aoe_2_cur'] = undefined;






export { AreaOfOperationalExpertiseView };

/**
 * The AreaOfOperationalExpertises model module.
 * @module model/AreaOfOperationalExpertises
 * @version 0.9.0
 */
class AreaOfOperationalExpertises {
    /**
     * Constructs a new <code>AreaOfOperationalExpertises</code>.
     * AreaOfOperationalExpertise result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/AreaOfOperationalExpertises
     */
    constructor() { 
        
        AreaOfOperationalExpertises.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>AreaOfOperationalExpertises</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AreaOfOperationalExpertises} obj Optional instance to populate.
     * @return {module:model/AreaOfOperationalExpertises} The populated <code>AreaOfOperationalExpertises</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AreaOfOperationalExpertises();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [AreaOfOperationalExpertise]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AreaOfOperationalExpertises</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AreaOfOperationalExpertises</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                AreaOfOperationalExpertise.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
AreaOfOperationalExpertises.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
AreaOfOperationalExpertises.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
AreaOfOperationalExpertises.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
AreaOfOperationalExpertises.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
AreaOfOperationalExpertises.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
AreaOfOperationalExpertises.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
AreaOfOperationalExpertises.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/AreaOfOperationalExpertise>} rows
 */
AreaOfOperationalExpertises.prototype['rows'] = undefined;






export { AreaOfOperationalExpertises };

/**
 * The AreaOfOperationalExpertisesQuery model module.
 * @module model/AreaOfOperationalExpertisesQuery
 * @version 0.9.0
 */
class AreaOfOperationalExpertisesQuery {
    /**
     * Constructs a new <code>AreaOfOperationalExpertisesQuery</code>.
     * Query payload for area-of-operational-expertise rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/AreaOfOperationalExpertisesQuery
     */
    constructor() { 
        
        AreaOfOperationalExpertisesQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>AreaOfOperationalExpertisesQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AreaOfOperationalExpertisesQuery} obj Optional instance to populate.
     * @return {module:model/AreaOfOperationalExpertisesQuery} The populated <code>AreaOfOperationalExpertisesQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AreaOfOperationalExpertisesQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AreaOfOperationalExpertisesQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AreaOfOperationalExpertisesQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
AreaOfOperationalExpertisesQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
AreaOfOperationalExpertisesQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
AreaOfOperationalExpertisesQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
AreaOfOperationalExpertisesQuery.prototype['language'] = undefined;






export { AreaOfOperationalExpertisesQuery };

/**
 * The BestMatchSearch model module.
 * @module model/BestMatchSearch
 * @version 0.9.0
 */
class BestMatchSearch {
    /**
     * Constructs a new <code>BestMatchSearch</code>.
     * BM25 search result set: echoes the query it was produced from and carries the matching rows, how many came back, how many matched in total, and how large the searchable pool is.
     * @alias module:model/BestMatchSearch
     * @param query {String} Text to search for. It is tokenised the same way as the indexed search texts, and the hits are the rows whose text scores highest against those terms under BM25. Matching is on terms, not on meaning, so a word that does not occur in a row cannot match it. Must not be blank.
     */
    constructor(query) { 
        
        BestMatchSearch.initialize(this, query);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, query) { 
        obj['query'] = query;
        obj['where'] = 'tsl_translations.pk > 0';
        obj['limit'] = 25;
    }

    /**
     * Constructs a <code>BestMatchSearch</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BestMatchSearch} obj Optional instance to populate.
     * @return {module:model/BestMatchSearch} The populated <code>BestMatchSearch</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BestMatchSearch();

            if (data.hasOwnProperty('query')) {
                obj['query'] = ApiClient.convertToType(data['query'], 'String');
            }
            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [AppApiSkillsCommandsBestMatchSearchModelsDocument]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BestMatchSearch</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BestMatchSearch</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BestMatchSearch.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['query'] && !(typeof data['query'] === 'string' || data['query'] instanceof String)) {
            throw new Error("Expected the field `query` to be a primitive type in the JSON string but got " + data['query']);
        }
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                AppApiSkillsCommandsBestMatchSearchModelsDocument.validateJSON(item);
            };
        }

        return true;
    }


}

BestMatchSearch.RequiredProperties = ["query"];

/**
 * Text to search for. It is tokenised the same way as the indexed search texts, and the hits are the rows whose text scores highest against those terms under BM25. Matching is on terms, not on meaning, so a word that does not occur in a row cannot match it. Must not be blank.
 * @member {String} query
 */
BestMatchSearch.prototype['query'] = undefined;

/**
 * SQL WHERE clause, applied before the full text search runs, so it decides which rows are candidates at all. It can name columns of the language-specific search text table (tsl_search_texts_<language>) and of the joined tsl_translations row, with or without the table name in front. A bare `pk`, `created_at` or `updated_at` is the search text table's; the tsl_translations ones are `tsl_pk`, `tsl_created_at` and `tsl_updated_at`, or spell them out as `tsl_translations.pk` and so on. Every other tsl_translations column keeps its own name either way. || Three of them are what scopes a search, because together they say where each translated text came from. `tsl_translations.fk_table` is the name of the source table whose attribute is translated -- a logical reference, not a database foreign key -- so `tsl_translations.fk_table = 'cur_curricula'` searches the curricula texts only, and `tsl_translations.fk_table IN ('prj_projects', 'cur_curricula')` or `tsl_translations.fk_table LIKE 'ops%'` widen that to several. || `tsl_translations.fk_table_pk` is the primary key (pk) of the row in that source table, so `tsl_translations.fk_table = 'cur_curricula' AND tsl_translations.fk_table_pk = 5` narrows the search to the texts of one single curriculum. It is only meaningful together with `fk_table`: every source table numbers its rows from 1, so `fk_table_pk = 5` on its own matches one unrelated row per table. || `tsl_translations.fk_attribute` is the name of the attribute (column) in the source table whose text is translated, so `tsl_translations.fk_attribute = 'tsl_title'` searches titles and ignores descriptions. || The three combine freely, e.g. `tsl_translations.fk_table = 'ops_operational_skills' AND tsl_translations.fk_attribute = 'tsl_description'`.
 * @member {String} where
 * @default 'tsl_translations.pk > 0'
 */
BestMatchSearch.prototype['where'] = 'tsl_translations.pk > 0';

/**
 * Maximum number of rows to return. Must be 1 or greater.
 * @member {Number} limit
 * @default 25
 */
BestMatchSearch.prototype['limit'] = 25;

/**
 * Number of rows to skip off the top of the ranking before `limit` is taken. BM25 scoring is deterministic for a given query and index state, so paging is stable as long as the indexed rows do not change underneath it.
 * @member {Number} offset
 */
BestMatchSearch.prototype['offset'] = undefined;

/**
 * Language used for translated fields. It also selects which search text table is searched.
 * @member {module:model/TslLanguageType} language
 */
BestMatchSearch.prototype['language'] = undefined;

/**
 * Number of rows matching both the query terms and `where`, before `limit` and `offset` are applied: how many hits the ranking has to offer, not how many were returned. Unlike a vector search this is not the candidate set -- a row sharing no term with the query is not a weak hit but no hit at all, so `total` is usually far below `all`.
 * @member {Number} total
 */
BestMatchSearch.prototype['total'] = undefined;

/**
 * Number of rows actually returned in `rows`, never more than `limit`.
 * @member {Number} count
 */
BestMatchSearch.prototype['count'] = undefined;

/**
 * Number of rows the language search text table offers to search at all, ignoring `where`: the pool `total` was narrowed down from. Rows without a stored search text are left out here too, since the search could never return them, so `all` equals `total` when `where` matches everything.
 * @member {Number} all
 */
BestMatchSearch.prototype['all'] = undefined;

/**
 * The matching rows, ordered by descending `bm25_score`, so the best match comes first. None when nothing matched.
 * @member {Array.<module:model/AppApiSkillsCommandsBestMatchSearchModelsDocument>} rows
 */
BestMatchSearch.prototype['rows'] = undefined;






export { BestMatchSearch };

/**
 * The BestMatchSearchQuery model module.
 * @module model/BestMatchSearchQuery
 * @version 0.9.0
 */
class BestMatchSearchQuery {
    /**
     * Constructs a new <code>BestMatchSearchQuery</code>.
     * Query payload for a BM25 full-text search over the translation search texts: the text to search for, a SQL WHERE clause narrowing which rows are searched, a row limit, an offset, and the language whose search text table is used.
     * @alias module:model/BestMatchSearchQuery
     * @param query {String} Text to search for. It is tokenised the same way as the indexed search texts, and the hits are the rows whose text scores highest against those terms under BM25. Matching is on terms, not on meaning, so a word that does not occur in a row cannot match it. Must not be blank.
     */
    constructor(query) { 
        
        BestMatchSearchQuery.initialize(this, query);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, query) { 
        obj['query'] = query;
        obj['where'] = 'tsl_translations.pk > 0';
        obj['limit'] = 25;
    }

    /**
     * Constructs a <code>BestMatchSearchQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BestMatchSearchQuery} obj Optional instance to populate.
     * @return {module:model/BestMatchSearchQuery} The populated <code>BestMatchSearchQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BestMatchSearchQuery();

            if (data.hasOwnProperty('query')) {
                obj['query'] = ApiClient.convertToType(data['query'], 'String');
            }
            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BestMatchSearchQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BestMatchSearchQuery</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BestMatchSearchQuery.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['query'] && !(typeof data['query'] === 'string' || data['query'] instanceof String)) {
            throw new Error("Expected the field `query` to be a primitive type in the JSON string but got " + data['query']);
        }
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}

BestMatchSearchQuery.RequiredProperties = ["query"];

/**
 * Text to search for. It is tokenised the same way as the indexed search texts, and the hits are the rows whose text scores highest against those terms under BM25. Matching is on terms, not on meaning, so a word that does not occur in a row cannot match it. Must not be blank.
 * @member {String} query
 */
BestMatchSearchQuery.prototype['query'] = undefined;

/**
 * SQL WHERE clause, applied before the full text search runs, so it decides which rows are candidates at all. It can name columns of the language-specific search text table (tsl_search_texts_<language>) and of the joined tsl_translations row, with or without the table name in front. A bare `pk`, `created_at` or `updated_at` is the search text table's; the tsl_translations ones are `tsl_pk`, `tsl_created_at` and `tsl_updated_at`, or spell them out as `tsl_translations.pk` and so on. Every other tsl_translations column keeps its own name either way. || Three of them are what scopes a search, because together they say where each translated text came from. `tsl_translations.fk_table` is the name of the source table whose attribute is translated -- a logical reference, not a database foreign key -- so `tsl_translations.fk_table = 'cur_curricula'` searches the curricula texts only, and `tsl_translations.fk_table IN ('prj_projects', 'cur_curricula')` or `tsl_translations.fk_table LIKE 'ops%'` widen that to several. || `tsl_translations.fk_table_pk` is the primary key (pk) of the row in that source table, so `tsl_translations.fk_table = 'cur_curricula' AND tsl_translations.fk_table_pk = 5` narrows the search to the texts of one single curriculum. It is only meaningful together with `fk_table`: every source table numbers its rows from 1, so `fk_table_pk = 5` on its own matches one unrelated row per table. || `tsl_translations.fk_attribute` is the name of the attribute (column) in the source table whose text is translated, so `tsl_translations.fk_attribute = 'tsl_title'` searches titles and ignores descriptions. || The three combine freely, e.g. `tsl_translations.fk_table = 'ops_operational_skills' AND tsl_translations.fk_attribute = 'tsl_description'`.
 * @member {String} where
 * @default 'tsl_translations.pk > 0'
 */
BestMatchSearchQuery.prototype['where'] = 'tsl_translations.pk > 0';

/**
 * Maximum number of rows to return. Must be 1 or greater.
 * @member {Number} limit
 * @default 25
 */
BestMatchSearchQuery.prototype['limit'] = 25;

/**
 * Number of rows to skip off the top of the ranking before `limit` is taken. BM25 scoring is deterministic for a given query and index state, so paging is stable as long as the indexed rows do not change underneath it.
 * @member {Number} offset
 */
BestMatchSearchQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields. It also selects which search text table is searched.
 * @member {module:model/TslLanguageType} language
 */
BestMatchSearchQuery.prototype['language'] = undefined;






export { BestMatchSearchQuery };

/**
 * The CurTitle model module.
 * @module model/CurTitle
 * @version 0.9.0
 */
class CurTitle {
    /**
     * Constructs a new <code>CurTitle</code>.
     * Localized curriculum title and shortcut record.
     * @alias module:model/CurTitle
     * @param fkProject {Number} Reference to the owning project this curriculum title belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        CurTitle.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>CurTitle</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurTitle} obj Optional instance to populate.
     * @return {module:model/CurTitle} The populated <code>CurTitle</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurTitle();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur')) {
                obj['fk_cur'] = ApiClient.convertToType(data['fk_cur'], 'Number');
            }
            if (data.hasOwnProperty('tsl_shortcut')) {
                obj['tsl_shortcut'] = ApiClient.convertToType(data['tsl_shortcut'], 'Number');
            }
            if (data.hasOwnProperty('tsl_title')) {
                obj['tsl_title'] = ApiClient.convertToType(data['tsl_title'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_shortcut_txt')) {
                obj['tsl_shortcut_txt'] = ApiClient.convertToType(data['tsl_shortcut_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_title_txt')) {
                obj['tsl_title_txt'] = ApiClient.convertToType(data['tsl_title_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurTitle</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurTitle</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CurTitle.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_shortcut_txt'] && !(typeof data['tsl_shortcut_txt'] === 'string' || data['tsl_shortcut_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_shortcut_txt` to be a primitive type in the JSON string but got " + data['tsl_shortcut_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_title_txt'] && !(typeof data['tsl_title_txt'] === 'string' || data['tsl_title_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_title_txt` to be a primitive type in the JSON string but got " + data['tsl_title_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }

        return true;
    }


}

CurTitle.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the curriculum title row.
 * @member {Number} pk
 */
CurTitle.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
CurTitle.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this curriculum title belongs to; cascades on delete.
 * @member {Number} fk_project
 */
CurTitle.prototype['fk_project'] = undefined;

/**
 * Reference to the linked curriculum; cascades on delete.
 * @member {Number} fk_cur
 */
CurTitle.prototype['fk_cur'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual shortcut of the curriculum title; logical reference, not a database foreign key.
 * @member {Number} tsl_shortcut
 */
CurTitle.prototype['tsl_shortcut'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual text of the curriculum title; logical reference, not a database foreign key.
 * @member {Number} tsl_title
 */
CurTitle.prototype['tsl_title'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of the curriculum title; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
CurTitle.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
CurTitle.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
CurTitle.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
CurTitle.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
CurTitle.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
CurTitle.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
CurTitle.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
CurTitle.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
CurTitle.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
CurTitle.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual shortcut of the curriculum title; logical reference, not a database foreign key.
 * @member {String} tsl_shortcut_txt
 */
CurTitle.prototype['tsl_shortcut_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual text of the curriculum title; logical reference, not a database foreign key.
 * @member {String} tsl_title_txt
 */
CurTitle.prototype['tsl_title_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of the curriculum title; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
CurTitle.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
CurTitle.prototype['tsl_language'] = undefined;






export { CurTitle };

/**
 * The CurTitleQuery model module.
 * @module model/CurTitleQuery
 * @version 0.9.0
 */
class CurTitleQuery {
    /**
     * Constructs a new <code>CurTitleQuery</code>.
     * Query payload for curriculum title rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/CurTitleQuery
     */
    constructor() { 
        
        CurTitleQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>CurTitleQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurTitleQuery} obj Optional instance to populate.
     * @return {module:model/CurTitleQuery} The populated <code>CurTitleQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurTitleQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurTitleQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurTitleQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
CurTitleQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
CurTitleQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
CurTitleQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
CurTitleQuery.prototype['language'] = undefined;






export { CurTitleQuery };

/**
 * The CurTitles model module.
 * @module model/CurTitles
 * @version 0.9.0
 */
class CurTitles {
    /**
     * Constructs a new <code>CurTitles</code>.
     * CurTitle result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/CurTitles
     */
    constructor() { 
        
        CurTitles.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>CurTitles</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurTitles} obj Optional instance to populate.
     * @return {module:model/CurTitles} The populated <code>CurTitles</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurTitles();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [CurTitle]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurTitles</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurTitles</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                CurTitle.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
CurTitles.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
CurTitles.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
CurTitles.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
CurTitles.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
CurTitles.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
CurTitles.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
CurTitles.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/CurTitle>} rows
 */
CurTitles.prototype['rows'] = undefined;






export { CurTitles };

/**
 * The CurTypeDegree model module.
 * @module model/CurTypeDegree
 * @version 0.9.0
 */
class CurTypeDegree {
    /**
     * Constructs a new <code>CurTypeDegree</code>.
     * Project-specific lookup row defining a type of degree (title) a curriculum leads to.
     * @alias module:model/CurTypeDegree
     * @param fkProject {Number} Reference to the owning project this degree type belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        CurTypeDegree.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>CurTypeDegree</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurTypeDegree} obj Optional instance to populate.
     * @return {module:model/CurTypeDegree} The populated <code>CurTypeDegree</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurTypeDegree();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('tsl_shortcut')) {
                obj['tsl_shortcut'] = ApiClient.convertToType(data['tsl_shortcut'], 'Number');
            }
            if (data.hasOwnProperty('tsl_title')) {
                obj['tsl_title'] = ApiClient.convertToType(data['tsl_title'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_shortcut_txt')) {
                obj['tsl_shortcut_txt'] = ApiClient.convertToType(data['tsl_shortcut_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_title_txt')) {
                obj['tsl_title_txt'] = ApiClient.convertToType(data['tsl_title_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurTypeDegree</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurTypeDegree</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CurTypeDegree.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_shortcut_txt'] && !(typeof data['tsl_shortcut_txt'] === 'string' || data['tsl_shortcut_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_shortcut_txt` to be a primitive type in the JSON string but got " + data['tsl_shortcut_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_title_txt'] && !(typeof data['tsl_title_txt'] === 'string' || data['tsl_title_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_title_txt` to be a primitive type in the JSON string but got " + data['tsl_title_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }

        return true;
    }


}

CurTypeDegree.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the degree type row.
 * @member {Number} pk
 */
CurTypeDegree.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
CurTypeDegree.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this degree type belongs to; cascades on delete.
 * @member {Number} fk_project
 */
CurTypeDegree.prototype['fk_project'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual shortcut of the degree type; logical reference, not a database foreign key.
 * @member {Number} tsl_shortcut
 */
CurTypeDegree.prototype['tsl_shortcut'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual name of the degree type; logical reference, not a database foreign key.
 * @member {Number} tsl_title
 */
CurTypeDegree.prototype['tsl_title'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of the degree type; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
CurTypeDegree.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
CurTypeDegree.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
CurTypeDegree.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
CurTypeDegree.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
CurTypeDegree.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
CurTypeDegree.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
CurTypeDegree.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
CurTypeDegree.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
CurTypeDegree.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
CurTypeDegree.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual shortcut of the degree type; logical reference, not a database foreign key.
 * @member {String} tsl_shortcut_txt
 */
CurTypeDegree.prototype['tsl_shortcut_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual name of the degree type; logical reference, not a database foreign key.
 * @member {String} tsl_title_txt
 */
CurTypeDegree.prototype['tsl_title_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of the degree type; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
CurTypeDegree.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
CurTypeDegree.prototype['tsl_language'] = undefined;






export { CurTypeDegree };

/**
 * The CurTypeDegreeQuery model module.
 * @module model/CurTypeDegreeQuery
 * @version 0.9.0
 */
class CurTypeDegreeQuery {
    /**
     * Constructs a new <code>CurTypeDegreeQuery</code>.
     * Query payload for degree type rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/CurTypeDegreeQuery
     */
    constructor() { 
        
        CurTypeDegreeQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>CurTypeDegreeQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurTypeDegreeQuery} obj Optional instance to populate.
     * @return {module:model/CurTypeDegreeQuery} The populated <code>CurTypeDegreeQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurTypeDegreeQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurTypeDegreeQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurTypeDegreeQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
CurTypeDegreeQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
CurTypeDegreeQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
CurTypeDegreeQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
CurTypeDegreeQuery.prototype['language'] = undefined;






export { CurTypeDegreeQuery };

/**
 * The CurTypeDegrees model module.
 * @module model/CurTypeDegrees
 * @version 0.9.0
 */
class CurTypeDegrees {
    /**
     * Constructs a new <code>CurTypeDegrees</code>.
     * CurTypeDegree result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/CurTypeDegrees
     */
    constructor() { 
        
        CurTypeDegrees.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>CurTypeDegrees</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurTypeDegrees} obj Optional instance to populate.
     * @return {module:model/CurTypeDegrees} The populated <code>CurTypeDegrees</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurTypeDegrees();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [CurTypeDegree]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurTypeDegrees</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurTypeDegrees</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                CurTypeDegree.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
CurTypeDegrees.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
CurTypeDegrees.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
CurTypeDegrees.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
CurTypeDegrees.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
CurTypeDegrees.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
CurTypeDegrees.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
CurTypeDegrees.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/CurTypeDegree>} rows
 */
CurTypeDegrees.prototype['rows'] = undefined;






export { CurTypeDegrees };

/**
 * The Curricula model module.
 * @module model/Curricula
 * @version 0.9.0
 */
class Curricula {
    /**
     * Constructs a new <code>Curricula</code>.
     * Curriculum result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/Curricula
     */
    constructor() { 
        
        Curricula.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Curricula</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Curricula} obj Optional instance to populate.
     * @return {module:model/Curricula} The populated <code>Curricula</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Curricula();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [Curriculum]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Curricula</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Curricula</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                Curriculum.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Curricula.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Curricula.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Curricula.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
Curricula.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
Curricula.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
Curricula.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
Curricula.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/Curriculum>} rows
 */
Curricula.prototype['rows'] = undefined;






export { Curricula };

/**
 * The Curriculum model module.
 * @module model/Curriculum
 * @version 0.9.0
 */
class Curriculum {
    /**
     * Constructs a new <code>Curriculum</code>.
     * Curriculum record within a skills project.
     * @alias module:model/Curriculum
     * @param fkProject {Number} Reference to the owning project this curriculum belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        Curriculum.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>Curriculum</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Curriculum} obj Optional instance to populate.
     * @return {module:model/Curriculum} The populated <code>Curriculum</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Curriculum();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('fk_type_degree')) {
                obj['fk_type_degree'] = ApiClient.convertToType(data['fk_type_degree'], 'Number');
            }
            if (data.hasOwnProperty('nr')) {
                obj['nr'] = ApiClient.convertToType(data['nr'], 'Number');
            }
            if (data.hasOwnProperty('tsl_educational_degree')) {
                obj['tsl_educational_degree'] = ApiClient.convertToType(data['tsl_educational_degree'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_1')) {
                obj['tsl_text_1'] = ApiClient.convertToType(data['tsl_text_1'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_2')) {
                obj['tsl_text_2'] = ApiClient.convertToType(data['tsl_text_2'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_3')) {
                obj['tsl_text_3'] = ApiClient.convertToType(data['tsl_text_3'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_4')) {
                obj['tsl_text_4'] = ApiClient.convertToType(data['tsl_text_4'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_5')) {
                obj['tsl_text_5'] = ApiClient.convertToType(data['tsl_text_5'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_6')) {
                obj['tsl_text_6'] = ApiClient.convertToType(data['tsl_text_6'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_educational_degree_txt')) {
                obj['tsl_educational_degree_txt'] = ApiClient.convertToType(data['tsl_educational_degree_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_1_txt')) {
                obj['tsl_text_1_txt'] = ApiClient.convertToType(data['tsl_text_1_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_2_txt')) {
                obj['tsl_text_2_txt'] = ApiClient.convertToType(data['tsl_text_2_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_3_txt')) {
                obj['tsl_text_3_txt'] = ApiClient.convertToType(data['tsl_text_3_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_4_txt')) {
                obj['tsl_text_4_txt'] = ApiClient.convertToType(data['tsl_text_4_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_5_txt')) {
                obj['tsl_text_5_txt'] = ApiClient.convertToType(data['tsl_text_5_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_6_txt')) {
                obj['tsl_text_6_txt'] = ApiClient.convertToType(data['tsl_text_6_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Curriculum</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Curriculum</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Curriculum.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_educational_degree_txt'] && !(typeof data['tsl_educational_degree_txt'] === 'string' || data['tsl_educational_degree_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_educational_degree_txt` to be a primitive type in the JSON string but got " + data['tsl_educational_degree_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_1_txt'] && !(typeof data['tsl_text_1_txt'] === 'string' || data['tsl_text_1_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_1_txt` to be a primitive type in the JSON string but got " + data['tsl_text_1_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_2_txt'] && !(typeof data['tsl_text_2_txt'] === 'string' || data['tsl_text_2_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_2_txt` to be a primitive type in the JSON string but got " + data['tsl_text_2_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_3_txt'] && !(typeof data['tsl_text_3_txt'] === 'string' || data['tsl_text_3_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_3_txt` to be a primitive type in the JSON string but got " + data['tsl_text_3_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_4_txt'] && !(typeof data['tsl_text_4_txt'] === 'string' || data['tsl_text_4_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_4_txt` to be a primitive type in the JSON string but got " + data['tsl_text_4_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_5_txt'] && !(typeof data['tsl_text_5_txt'] === 'string' || data['tsl_text_5_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_5_txt` to be a primitive type in the JSON string but got " + data['tsl_text_5_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_6_txt'] && !(typeof data['tsl_text_6_txt'] === 'string' || data['tsl_text_6_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_6_txt` to be a primitive type in the JSON string but got " + data['tsl_text_6_txt']);
        }

        return true;
    }


}

Curriculum.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the curriculum row.
 * @member {Number} pk
 */
Curriculum.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
Curriculum.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this curriculum belongs to; cascades on delete.
 * @member {Number} fk_project
 */
Curriculum.prototype['fk_project'] = undefined;

/**
 * Reference to the type of degree this curriculum leads to; set to NULL on delete.
 * @member {Number} fk_type_degree
 */
Curriculum.prototype['fk_type_degree'] = undefined;

/**
 * Number used to order and reference the curriculum.
 * @member {Number} nr
 */
Curriculum.prototype['nr'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual educational degree of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_educational_degree
 */
Curriculum.prototype['tsl_educational_degree'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 1 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_1
 */
Curriculum.prototype['tsl_text_1'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 2 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_2
 */
Curriculum.prototype['tsl_text_2'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 3 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_3
 */
Curriculum.prototype['tsl_text_3'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 4 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_4
 */
Curriculum.prototype['tsl_text_4'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 5 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_5
 */
Curriculum.prototype['tsl_text_5'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 6 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_6
 */
Curriculum.prototype['tsl_text_6'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
Curriculum.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
Curriculum.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
Curriculum.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
Curriculum.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
Curriculum.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
Curriculum.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
Curriculum.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
Curriculum.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
Curriculum.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual educational degree of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_educational_degree_txt
 */
Curriculum.prototype['tsl_educational_degree_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 1 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_1_txt
 */
Curriculum.prototype['tsl_text_1_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 2 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_2_txt
 */
Curriculum.prototype['tsl_text_2_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 3 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_3_txt
 */
Curriculum.prototype['tsl_text_3_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 4 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_4_txt
 */
Curriculum.prototype['tsl_text_4_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 5 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_5_txt
 */
Curriculum.prototype['tsl_text_5_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 6 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_6_txt
 */
Curriculum.prototype['tsl_text_6_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
Curriculum.prototype['tsl_language'] = undefined;






export { Curriculum };

/**
 * The CurriculumQuery model module.
 * @module model/CurriculumQuery
 * @version 0.9.0
 */
class CurriculumQuery {
    /**
     * Constructs a new <code>CurriculumQuery</code>.
     * Query payload for curriculum rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/CurriculumQuery
     */
    constructor() { 
        
        CurriculumQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>CurriculumQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurriculumQuery} obj Optional instance to populate.
     * @return {module:model/CurriculumQuery} The populated <code>CurriculumQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurriculumQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurriculumQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurriculumQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
CurriculumQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
CurriculumQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
CurriculumQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
CurriculumQuery.prototype['language'] = undefined;






export { CurriculumQuery };

/**
 * The CurriculumView model module.
 * @module model/CurriculumView
 * @version 0.9.0
 */
class CurriculumView {
    /**
     * Constructs a new <code>CurriculumView</code>.
     * Curriculum row extended with its nested degree type and aoe_2_cur junction rows.
     * @alias module:model/CurriculumView
     * @param fkProject {Number} Reference to the owning project this curriculum belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        CurriculumView.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>CurriculumView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurriculumView} obj Optional instance to populate.
     * @return {module:model/CurriculumView} The populated <code>CurriculumView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurriculumView();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('fk_type_degree')) {
                obj['fk_type_degree'] = ApiClient.convertToType(data['fk_type_degree'], 'Number');
            }
            if (data.hasOwnProperty('nr')) {
                obj['nr'] = ApiClient.convertToType(data['nr'], 'Number');
            }
            if (data.hasOwnProperty('tsl_educational_degree')) {
                obj['tsl_educational_degree'] = ApiClient.convertToType(data['tsl_educational_degree'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_1')) {
                obj['tsl_text_1'] = ApiClient.convertToType(data['tsl_text_1'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_2')) {
                obj['tsl_text_2'] = ApiClient.convertToType(data['tsl_text_2'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_3')) {
                obj['tsl_text_3'] = ApiClient.convertToType(data['tsl_text_3'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_4')) {
                obj['tsl_text_4'] = ApiClient.convertToType(data['tsl_text_4'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_5')) {
                obj['tsl_text_5'] = ApiClient.convertToType(data['tsl_text_5'], 'Number');
            }
            if (data.hasOwnProperty('tsl_text_6')) {
                obj['tsl_text_6'] = ApiClient.convertToType(data['tsl_text_6'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_educational_degree_txt')) {
                obj['tsl_educational_degree_txt'] = ApiClient.convertToType(data['tsl_educational_degree_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_1_txt')) {
                obj['tsl_text_1_txt'] = ApiClient.convertToType(data['tsl_text_1_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_2_txt')) {
                obj['tsl_text_2_txt'] = ApiClient.convertToType(data['tsl_text_2_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_3_txt')) {
                obj['tsl_text_3_txt'] = ApiClient.convertToType(data['tsl_text_3_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_4_txt')) {
                obj['tsl_text_4_txt'] = ApiClient.convertToType(data['tsl_text_4_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_5_txt')) {
                obj['tsl_text_5_txt'] = ApiClient.convertToType(data['tsl_text_5_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_6_txt')) {
                obj['tsl_text_6_txt'] = ApiClient.convertToType(data['tsl_text_6_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
            if (data.hasOwnProperty('cur_type_degree')) {
                obj['cur_type_degree'] = ApiClient.convertToType(data['cur_type_degree'], [CurTypeDegree]);
            }
            if (data.hasOwnProperty('aoe_2_cur')) {
                obj['aoe_2_cur'] = ApiClient.convertToType(data['aoe_2_cur'], [Aoe2CurView]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurriculumView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurriculumView</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CurriculumView.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_educational_degree_txt'] && !(typeof data['tsl_educational_degree_txt'] === 'string' || data['tsl_educational_degree_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_educational_degree_txt` to be a primitive type in the JSON string but got " + data['tsl_educational_degree_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_1_txt'] && !(typeof data['tsl_text_1_txt'] === 'string' || data['tsl_text_1_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_1_txt` to be a primitive type in the JSON string but got " + data['tsl_text_1_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_2_txt'] && !(typeof data['tsl_text_2_txt'] === 'string' || data['tsl_text_2_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_2_txt` to be a primitive type in the JSON string but got " + data['tsl_text_2_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_3_txt'] && !(typeof data['tsl_text_3_txt'] === 'string' || data['tsl_text_3_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_3_txt` to be a primitive type in the JSON string but got " + data['tsl_text_3_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_4_txt'] && !(typeof data['tsl_text_4_txt'] === 'string' || data['tsl_text_4_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_4_txt` to be a primitive type in the JSON string but got " + data['tsl_text_4_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_5_txt'] && !(typeof data['tsl_text_5_txt'] === 'string' || data['tsl_text_5_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_5_txt` to be a primitive type in the JSON string but got " + data['tsl_text_5_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_text_6_txt'] && !(typeof data['tsl_text_6_txt'] === 'string' || data['tsl_text_6_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_6_txt` to be a primitive type in the JSON string but got " + data['tsl_text_6_txt']);
        }
        if (data['cur_type_degree']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['cur_type_degree'])) {
                throw new Error("Expected the field `cur_type_degree` to be an array in the JSON data but got " + data['cur_type_degree']);
            }
            // validate the optional field `cur_type_degree` (array)
            for (const item of data['cur_type_degree']) {
                CurTypeDegree.validateJSON(item);
            };
        }
        if (data['aoe_2_cur']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['aoe_2_cur'])) {
                throw new Error("Expected the field `aoe_2_cur` to be an array in the JSON data but got " + data['aoe_2_cur']);
            }
            // validate the optional field `aoe_2_cur` (array)
            for (const item of data['aoe_2_cur']) {
                Aoe2CurView.validateJSON(item);
            };
        }

        return true;
    }


}

CurriculumView.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the curriculum row.
 * @member {Number} pk
 */
CurriculumView.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
CurriculumView.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this curriculum belongs to; cascades on delete.
 * @member {Number} fk_project
 */
CurriculumView.prototype['fk_project'] = undefined;

/**
 * Reference to the type of degree this curriculum leads to; set to NULL on delete.
 * @member {Number} fk_type_degree
 */
CurriculumView.prototype['fk_type_degree'] = undefined;

/**
 * Number used to order and reference the curriculum.
 * @member {Number} nr
 */
CurriculumView.prototype['nr'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual educational degree of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_educational_degree
 */
CurriculumView.prototype['tsl_educational_degree'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 1 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_1
 */
CurriculumView.prototype['tsl_text_1'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 2 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_2
 */
CurriculumView.prototype['tsl_text_2'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 3 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_3
 */
CurriculumView.prototype['tsl_text_3'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 4 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_4
 */
CurriculumView.prototype['tsl_text_4'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 5 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_5
 */
CurriculumView.prototype['tsl_text_5'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 6 of the curriculum; logical reference, not a database foreign key.
 * @member {Number} tsl_text_6
 */
CurriculumView.prototype['tsl_text_6'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
CurriculumView.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
CurriculumView.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
CurriculumView.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
CurriculumView.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
CurriculumView.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
CurriculumView.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
CurriculumView.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
CurriculumView.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
CurriculumView.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual educational degree of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_educational_degree_txt
 */
CurriculumView.prototype['tsl_educational_degree_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 1 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_1_txt
 */
CurriculumView.prototype['tsl_text_1_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 2 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_2_txt
 */
CurriculumView.prototype['tsl_text_2_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 3 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_3_txt
 */
CurriculumView.prototype['tsl_text_3_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 4 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_4_txt
 */
CurriculumView.prototype['tsl_text_4_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 5 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_5_txt
 */
CurriculumView.prototype['tsl_text_5_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual free text 6 of the curriculum; logical reference, not a database foreign key.
 * @member {String} tsl_text_6_txt
 */
CurriculumView.prototype['tsl_text_6_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
CurriculumView.prototype['tsl_language'] = undefined;

/**
 * Degree type rows
 * @member {Array.<module:model/CurTypeDegree>} cur_type_degree
 */
CurriculumView.prototype['cur_type_degree'] = undefined;

/**
 * Area of operational expertise to curriculum rows
 * @member {Array.<module:model/Aoe2CurView>} aoe_2_cur
 */
CurriculumView.prototype['aoe_2_cur'] = undefined;






export { CurriculumView };

/**
* Enum class DataStateType.
* @enum {}
* @readonly
*/
export class DataStateType {
    
        /**
         * value: "created"
         * @const
         */
        "CREATED" = "created";

    
        /**
         * value: "updated"
         * @const
         */
        "UPDATED" = "updated";

    
        /**
         * value: "deletion_preparation"
         * @const
         */
        "DELETION_PREPARATION" = "deletion_preparation";

    
        /**
         * value: "undefined"
         * @const
         */
        "UNDEFINED" = "undefined";

    

    /**
    * Returns a <code>DataStateType</code> enum value from a Javascript object name.
    * @param {Object} data The plain JavaScript object containing the name of the enum value.
    * @return {module:model/DataStateType} The enum <code>DataStateType</code> value.
    */
    static constructFromObject(object) {
        return object;
    }
}

/**
 * The EnumTranslation model module.
 * @module model/EnumTranslation
 * @version 0.9.0
 */
class EnumTranslation {
    /**
     * Constructs a new <code>EnumTranslation</code>.
     * Single enum member translation: the stored key plus its de/fr/it/en labels.
     * @alias module:model/EnumTranslation
     * @param key {String} Stored enum key for this translation entry
     * @param de {String} German label for the enum key
     * @param fr {String} French label for the enum key
     * @param it {String} Italian label for the enum key
     * @param en {String} English label for the enum key
     * @param reference {String} Name of the enum type this translation belongs to
     */
    constructor(key, de, fr, it, en, reference) { 
        
        EnumTranslation.initialize(this, key, de, fr, it, en, reference);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, key, de, fr, it, en, reference) { 
        obj['key'] = key;
        obj['de'] = de;
        obj['fr'] = fr;
        obj['it'] = it;
        obj['en'] = en;
        obj['reference'] = reference;
    }

    /**
     * Constructs a <code>EnumTranslation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EnumTranslation} obj Optional instance to populate.
     * @return {module:model/EnumTranslation} The populated <code>EnumTranslation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EnumTranslation();

            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('de')) {
                obj['de'] = ApiClient.convertToType(data['de'], 'String');
            }
            if (data.hasOwnProperty('fr')) {
                obj['fr'] = ApiClient.convertToType(data['fr'], 'String');
            }
            if (data.hasOwnProperty('it')) {
                obj['it'] = ApiClient.convertToType(data['it'], 'String');
            }
            if (data.hasOwnProperty('en')) {
                obj['en'] = ApiClient.convertToType(data['en'], 'String');
            }
            if (data.hasOwnProperty('reference')) {
                obj['reference'] = ApiClient.convertToType(data['reference'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EnumTranslation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EnumTranslation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EnumTranslation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['de'] && !(typeof data['de'] === 'string' || data['de'] instanceof String)) {
            throw new Error("Expected the field `de` to be a primitive type in the JSON string but got " + data['de']);
        }
        // ensure the json data is a string
        if (data['fr'] && !(typeof data['fr'] === 'string' || data['fr'] instanceof String)) {
            throw new Error("Expected the field `fr` to be a primitive type in the JSON string but got " + data['fr']);
        }
        // ensure the json data is a string
        if (data['it'] && !(typeof data['it'] === 'string' || data['it'] instanceof String)) {
            throw new Error("Expected the field `it` to be a primitive type in the JSON string but got " + data['it']);
        }
        // ensure the json data is a string
        if (data['en'] && !(typeof data['en'] === 'string' || data['en'] instanceof String)) {
            throw new Error("Expected the field `en` to be a primitive type in the JSON string but got " + data['en']);
        }
        // ensure the json data is a string
        if (data['reference'] && !(typeof data['reference'] === 'string' || data['reference'] instanceof String)) {
            throw new Error("Expected the field `reference` to be a primitive type in the JSON string but got " + data['reference']);
        }

        return true;
    }


}

EnumTranslation.RequiredProperties = ["key", "de", "fr", "it", "en", "reference"];

/**
 * Stored enum key for this translation entry
 * @member {String} key
 */
EnumTranslation.prototype['key'] = undefined;

/**
 * German label for the enum key
 * @member {String} de
 */
EnumTranslation.prototype['de'] = undefined;

/**
 * French label for the enum key
 * @member {String} fr
 */
EnumTranslation.prototype['fr'] = undefined;

/**
 * Italian label for the enum key
 * @member {String} it
 */
EnumTranslation.prototype['it'] = undefined;

/**
 * English label for the enum key
 * @member {String} en
 */
EnumTranslation.prototype['en'] = undefined;

/**
 * Name of the enum type this translation belongs to
 * @member {String} reference
 */
EnumTranslation.prototype['reference'] = undefined;






export { EnumTranslation };

/**
 * The File model module.
 * @module model/File
 * @version 0.9.0
 */
class File {
    /**
     * Constructs a new <code>File</code>.
     * Document file belonging to a skills project, with one file content column per language.
     * @alias module:model/File
     * @param fkProject {Number} Reference to the owning project this file belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        File.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>File</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/File} obj Optional instance to populate.
     * @return {module:model/File} The populated <code>File</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new File();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('file_name')) {
                obj['file_name'] = ApiClient.convertToType(data['file_name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_snippet')) {
                obj['tsl_text_snippet'] = ApiClient.convertToType(data['tsl_text_snippet'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('raw_data')) {
                obj['raw_data'] = ApiClient.convertToType(data['raw_data'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_snippet_txt')) {
                obj['tsl_text_snippet_txt'] = ApiClient.convertToType(data['tsl_text_snippet_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>File</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>File</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of File.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['file_name'] && !(typeof data['file_name'] === 'string' || data['file_name'] instanceof String)) {
            throw new Error("Expected the field `file_name` to be a primitive type in the JSON string but got " + data['file_name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['raw_data'] && !(typeof data['raw_data'] === 'string' || data['raw_data'] instanceof String)) {
            throw new Error("Expected the field `raw_data` to be a primitive type in the JSON string but got " + data['raw_data']);
        }
        // ensure the json data is a string
        if (data['tsl_text_snippet_txt'] && !(typeof data['tsl_text_snippet_txt'] === 'string' || data['tsl_text_snippet_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_snippet_txt` to be a primitive type in the JSON string but got " + data['tsl_text_snippet_txt']);
        }

        return true;
    }


}

File.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the file row.
 * @member {Number} pk
 */
File.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
File.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this file belongs to; cascades on delete.
 * @member {Number} fk_project
 */
File.prototype['fk_project'] = undefined;

/**
 * Human-readable unique business identifier of the file (e.g. \"FLS-0001\").
 * @member {String} id
 */
File.prototype['id'] = undefined;

/**
 * File name with extension.
 * @member {String} file_name
 */
File.prototype['file_name'] = undefined;

/**
 * Detailed description of the file and its purpose.
 * @member {String} description
 */
File.prototype['description'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual text of the snippet; logical reference, not a database foreign key.
 * @member {Number} tsl_text_snippet
 */
File.prototype['tsl_text_snippet'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
File.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
File.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
File.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
File.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
File.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
File.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
File.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
File.prototype['updated_at'] = undefined;

/**
 * Content of the file in the language selected by `tsl_language`, transported as a base64 string.
 * @member {String} raw_data
 */
File.prototype['raw_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual text of the snippet; logical reference, not a database foreign key.
 * @member {String} tsl_text_snippet_txt
 */
File.prototype['tsl_text_snippet_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
File.prototype['tsl_language'] = undefined;






export { File };

/**
 * The Files model module.
 * @module model/Files
 * @version 0.9.0
 */
class Files {
    /**
     * Constructs a new <code>Files</code>.
     * File result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/Files
     */
    constructor() { 
        
        Files.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Files</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Files} obj Optional instance to populate.
     * @return {module:model/Files} The populated <code>Files</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Files();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [File]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Files</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Files</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                File.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Files.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Files.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Files.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
Files.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
Files.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
Files.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
Files.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<File>} rows
 */
Files.prototype['rows'] = undefined;






export { Files };

/**
 * The FilesQuery model module.
 * @module model/FilesQuery
 * @version 0.9.0
 */
class FilesQuery {
    /**
     * Constructs a new <code>FilesQuery</code>.
     * Query payload for file rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/FilesQuery
     */
    constructor() { 
        
        FilesQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>FilesQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FilesQuery} obj Optional instance to populate.
     * @return {module:model/FilesQuery} The populated <code>FilesQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FilesQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FilesQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FilesQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
FilesQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
FilesQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
FilesQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
FilesQuery.prototype['language'] = undefined;






export { FilesQuery };

/**
 * The FullTextSearch model module.
 * @module model/FullTextSearch
 * @version 0.9.0
 */
class FullTextSearch {
    /**
     * Constructs a new <code>FullTextSearch</code>.
     * Full text search result set: echoes the query it was produced from and carries the matching rows, how many came back, how many matched in total, and how large the searchable pool is.
     * @alias module:model/FullTextSearch
     * @param query {String} Text to search for. It is turned into a tsquery by websearch_to_tsquery() in the language's text search configuration, so bare terms are ANDed, \"quoted text\" matches as a phrase, OR is available and a leading - excludes a term. Query and stored text are stemmed by that same configuration, so one word form finds the others -- but matching stays lexical, not semantic: a word whose stem occurs in no row cannot match it. Input the parser cannot read is not an error, it is ignored. Must not be blank.
     */
    constructor(query) { 
        
        FullTextSearch.initialize(this, query);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, query) { 
        obj['query'] = query;
        obj['where'] = 'tsl_translations.pk > 0';
        obj['limit'] = 25;
    }

    /**
     * Constructs a <code>FullTextSearch</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FullTextSearch} obj Optional instance to populate.
     * @return {module:model/FullTextSearch} The populated <code>FullTextSearch</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FullTextSearch();

            if (data.hasOwnProperty('query')) {
                obj['query'] = ApiClient.convertToType(data['query'], 'String');
            }
            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [AppApiSkillsCommandsFullTextSearchModelsDocument]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FullTextSearch</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FullTextSearch</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FullTextSearch.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['query'] && !(typeof data['query'] === 'string' || data['query'] instanceof String)) {
            throw new Error("Expected the field `query` to be a primitive type in the JSON string but got " + data['query']);
        }
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                AppApiSkillsCommandsFullTextSearchModelsDocument.validateJSON(item);
            };
        }

        return true;
    }


}

FullTextSearch.RequiredProperties = ["query"];

/**
 * Text to search for. It is turned into a tsquery by websearch_to_tsquery() in the language's text search configuration, so bare terms are ANDed, \"quoted text\" matches as a phrase, OR is available and a leading - excludes a term. Query and stored text are stemmed by that same configuration, so one word form finds the others -- but matching stays lexical, not semantic: a word whose stem occurs in no row cannot match it. Input the parser cannot read is not an error, it is ignored. Must not be blank.
 * @member {String} query
 */
FullTextSearch.prototype['query'] = undefined;

/**
 * SQL WHERE clause, applied before the full text search runs, so it decides which rows are candidates at all. It can name columns of the language-specific search text table (tsl_search_texts_<language>) and of the joined tsl_translations row, with or without the table name in front. A bare `pk`, `created_at` or `updated_at` is the search text table's; the tsl_translations ones are `tsl_pk`, `tsl_created_at` and `tsl_updated_at`, or spell them out as `tsl_translations.pk` and so on. Every other tsl_translations column keeps its own name either way. Three of them are what scopes a search, because together they say where each translated text came from. `tsl_translations.fk_table` is the name of the source table whose attribute is translated -- a logical reference, not a database foreign key -- so `tsl_translations.fk_table = 'cur_curricula'` searches the curricula texts only, while `tsl_translations.fk_table IN ('prj_projects', 'cur_curricula')` and `tsl_translations.fk_table LIKE 'ops%'` widen that to several. `tsl_translations.fk_table_pk` is the primary key (pk) of the row in that source table, so `tsl_translations.fk_table = 'cur_curricula' AND tsl_translations.fk_table_pk = 5` narrows the search to the texts of one single curriculum; it is only meaningful together with `fk_table`, because every source table numbers its rows from 1 and `fk_table_pk = 5` on its own would match one unrelated row per table. `tsl_translations.fk_attribute` is the name of the attribute (column) in the source table whose text is translated, so `tsl_translations.fk_attribute = 'tsl_title'` searches titles and ignores descriptions. The three combine freely, as in `tsl_translations.fk_table = 'ops_operational_skills' AND tsl_translations.fk_attribute = 'tsl_description'`.
 * @member {String} where
 * @default 'tsl_translations.pk > 0'
 */
FullTextSearch.prototype['where'] = 'tsl_translations.pk > 0';

/**
 * Maximum number of rows to return. Must be 1 or greater.
 * @member {Number} limit
 * @default 25
 */
FullTextSearch.prototype['limit'] = 25;

/**
 * Number of rows to skip off the top of the ranking before `limit` is taken. ts_rank is deterministic for a given query and text, so paging is stable as long as the underlying rows do not change.
 * @member {Number} offset
 */
FullTextSearch.prototype['offset'] = undefined;

/**
 * Language used for translated fields. It selects both which search text table is searched and which Postgres text search configuration (german, french, italian, english) stems the stored text and the query.
 * @member {module:model/TslLanguageType} language
 */
FullTextSearch.prototype['language'] = undefined;

/**
 * Number of rows matching both the query and `where`, before `limit` and `offset` are applied: how many hits the ranking has to offer, not how many were returned. Unlike a vector search this is not the candidate set -- a row whose text shares no stem with the query is not a weak hit but no hit at all, so `total` is usually far below `all`.
 * @member {Number} total
 */
FullTextSearch.prototype['total'] = undefined;

/**
 * Number of rows actually returned in `rows`, never more than `limit`.
 * @member {Number} count
 */
FullTextSearch.prototype['count'] = undefined;

/**
 * Number of rows the language search text table offers to search at all, ignoring `where`: the pool `total` was narrowed down from. Rows without a stored search text are left out here too, since the search could never return them, so `all` equals `total` when `where` matches everything.
 * @member {Number} all
 */
FullTextSearch.prototype['all'] = undefined;

/**
 * The matching rows, ordered by descending `rank`, so the best match comes first. None when nothing matched.
 * @member {Array.<module:model/AppApiSkillsCommandsFullTextSearchModelsDocument>} rows
 */
FullTextSearch.prototype['rows'] = undefined;






export { FullTextSearch };

/**
 * The FullTextSearchQuery model module.
 * @module model/FullTextSearchQuery
 * @version 0.9.0
 */
class FullTextSearchQuery {
    /**
     * Constructs a new <code>FullTextSearchQuery</code>.
     * Query payload for a full text search over the translation search texts, run with Postgres&#39; own tsvector matching: the text to search for, a SQL WHERE clause narrowing which rows are searched, a row limit, an offset, and the language whose search text table and text search configuration are used.
     * @alias module:model/FullTextSearchQuery
     * @param query {String} Text to search for. It is turned into a tsquery by websearch_to_tsquery() in the language's text search configuration, so bare terms are ANDed, \"quoted text\" matches as a phrase, OR is available and a leading - excludes a term. Query and stored text are stemmed by that same configuration, so one word form finds the others -- but matching stays lexical, not semantic: a word whose stem occurs in no row cannot match it. Input the parser cannot read is not an error, it is ignored. Must not be blank.
     */
    constructor(query) { 
        
        FullTextSearchQuery.initialize(this, query);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, query) { 
        obj['query'] = query;
        obj['where'] = 'tsl_translations.pk > 0';
        obj['limit'] = 25;
    }

    /**
     * Constructs a <code>FullTextSearchQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FullTextSearchQuery} obj Optional instance to populate.
     * @return {module:model/FullTextSearchQuery} The populated <code>FullTextSearchQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FullTextSearchQuery();

            if (data.hasOwnProperty('query')) {
                obj['query'] = ApiClient.convertToType(data['query'], 'String');
            }
            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FullTextSearchQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FullTextSearchQuery</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FullTextSearchQuery.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['query'] && !(typeof data['query'] === 'string' || data['query'] instanceof String)) {
            throw new Error("Expected the field `query` to be a primitive type in the JSON string but got " + data['query']);
        }
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}

FullTextSearchQuery.RequiredProperties = ["query"];

/**
 * Text to search for. It is turned into a tsquery by websearch_to_tsquery() in the language's text search configuration, so bare terms are ANDed, \"quoted text\" matches as a phrase, OR is available and a leading - excludes a term. Query and stored text are stemmed by that same configuration, so one word form finds the others -- but matching stays lexical, not semantic: a word whose stem occurs in no row cannot match it. Input the parser cannot read is not an error, it is ignored. Must not be blank.
 * @member {String} query
 */
FullTextSearchQuery.prototype['query'] = undefined;

/**
 * SQL WHERE clause, applied before the full text search runs, so it decides which rows are candidates at all. It can name columns of the language-specific search text table (tsl_search_texts_<language>) and of the joined tsl_translations row, with or without the table name in front. A bare `pk`, `created_at` or `updated_at` is the search text table's; the tsl_translations ones are `tsl_pk`, `tsl_created_at` and `tsl_updated_at`, or spell them out as `tsl_translations.pk` and so on. Every other tsl_translations column keeps its own name either way. Three of them are what scopes a search, because together they say where each translated text came from. `tsl_translations.fk_table` is the name of the source table whose attribute is translated -- a logical reference, not a database foreign key -- so `tsl_translations.fk_table = 'cur_curricula'` searches the curricula texts only, while `tsl_translations.fk_table IN ('prj_projects', 'cur_curricula')` and `tsl_translations.fk_table LIKE 'ops%'` widen that to several. `tsl_translations.fk_table_pk` is the primary key (pk) of the row in that source table, so `tsl_translations.fk_table = 'cur_curricula' AND tsl_translations.fk_table_pk = 5` narrows the search to the texts of one single curriculum; it is only meaningful together with `fk_table`, because every source table numbers its rows from 1 and `fk_table_pk = 5` on its own would match one unrelated row per table. `tsl_translations.fk_attribute` is the name of the attribute (column) in the source table whose text is translated, so `tsl_translations.fk_attribute = 'tsl_title'` searches titles and ignores descriptions. The three combine freely, as in `tsl_translations.fk_table = 'ops_operational_skills' AND tsl_translations.fk_attribute = 'tsl_description'`.
 * @member {String} where
 * @default 'tsl_translations.pk > 0'
 */
FullTextSearchQuery.prototype['where'] = 'tsl_translations.pk > 0';

/**
 * Maximum number of rows to return. Must be 1 or greater.
 * @member {Number} limit
 * @default 25
 */
FullTextSearchQuery.prototype['limit'] = 25;

/**
 * Number of rows to skip off the top of the ranking before `limit` is taken. ts_rank is deterministic for a given query and text, so paging is stable as long as the underlying rows do not change.
 * @member {Number} offset
 */
FullTextSearchQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields. It selects both which search text table is searched and which Postgres text search configuration (german, french, italian, english) stems the stored text and the query.
 * @member {module:model/TslLanguageType} language
 */
FullTextSearchQuery.prototype['language'] = undefined;






export { FullTextSearchQuery };

/**
 * The HTTPValidationError model module.
 * @module model/HTTPValidationError
 * @version 0.9.0
 */
class HTTPValidationError {
    /**
     * Constructs a new <code>HTTPValidationError</code>.
     * @alias module:model/HTTPValidationError
     */
    constructor() { 
        
        HTTPValidationError.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>HTTPValidationError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/HTTPValidationError} obj Optional instance to populate.
     * @return {module:model/HTTPValidationError} The populated <code>HTTPValidationError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new HTTPValidationError();

            if (data.hasOwnProperty('detail')) {
                obj['detail'] = ApiClient.convertToType(data['detail'], [ValidationError]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>HTTPValidationError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>HTTPValidationError</code>.
     */
    static validateJSON(data) {
        if (data['detail']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['detail'])) {
                throw new Error("Expected the field `detail` to be an array in the JSON data but got " + data['detail']);
            }
            // validate the optional field `detail` (array)
            for (const item of data['detail']) {
                ValidationError.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/ValidationError>} detail
 */
HTTPValidationError.prototype['detail'] = undefined;






export { HTTPValidationError };

// TODO: add anyof model support

/**
 * The LogEntries model module.
 * @module model/LogEntries
 * @version 0.9.0
 */
class LogEntries {
    /**
     * Constructs a new <code>LogEntries</code>.
     * LogEntry result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/LogEntries
     */
    constructor() { 
        
        LogEntries.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>LogEntries</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LogEntries} obj Optional instance to populate.
     * @return {module:model/LogEntries} The populated <code>LogEntries</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LogEntries();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [LogEntry]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LogEntries</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LogEntries</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                LogEntry.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
LogEntries.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
LogEntries.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
LogEntries.prototype['offset'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
LogEntries.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
LogEntries.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
LogEntries.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/LogEntry>} rows
 */
LogEntries.prototype['rows'] = undefined;






export { LogEntries };

/**
 * The LogEntriesSystem model module.
 * @module model/LogEntriesSystem
 * @version 0.9.0
 */
class LogEntriesSystem {
    /**
     * Constructs a new <code>LogEntriesSystem</code>.
     * Log entry result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/LogEntriesSystem
     */
    constructor() { 
        
        LogEntriesSystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>LogEntriesSystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LogEntriesSystem} obj Optional instance to populate.
     * @return {module:model/LogEntriesSystem} The populated <code>LogEntriesSystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LogEntriesSystem();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [LogEntrySystem]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LogEntriesSystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LogEntriesSystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                LogEntrySystem.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
LogEntriesSystem.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
LogEntriesSystem.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
LogEntriesSystem.prototype['offset'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
LogEntriesSystem.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
LogEntriesSystem.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
LogEntriesSystem.prototype['all'] = undefined;

/**
 * Log entry rows
 * @member {Array.<module:model/LogEntrySystem>} rows
 */
LogEntriesSystem.prototype['rows'] = undefined;






export { LogEntriesSystem };

/**
 * The LogEntry model module.
 * @module model/LogEntry
 * @version 0.9.0
 */
class LogEntry {
    /**
     * Constructs a new <code>LogEntry</code>.
     * Append-only audit trail of all data changes in the &#x60;skills&#x60; schema.  One row is written for every INSERT, UPDATE and DELETE on the audited base tables by the generic &#x60;&#x60;skills.log_change()&#x60;&#x60; trigger (defined in &#x60;&#x60;pg_migrations/skills/pg_db_cmd.py::get_sql_logging_trigger&#x60;&#x60;). Each entry captures which table and row changed, the kind of change, who made it, when it happened, and the full row state before and/or after the change as JSONB.  The trigger fires AFTER the row operation, so &#x60;&#x60;new_val&#x60;&#x60; reflects the final persisted row (including the &#x60;&#x60;updated_at&#x60;&#x60; set by the BEFORE-UPDATE &#x60;&#x60;set_updated_at()&#x60;&#x60; trigger).  Idea: https://www.cybertec-postgresql.com/en/tracking-changes-in-postgresql/
     * @alias module:model/LogEntry
     */
    constructor() { 
        
        LogEntry.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>LogEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LogEntry} obj Optional instance to populate.
     * @return {module:model/LogEntry} The populated <code>LogEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LogEntry();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('schemaname')) {
                obj['schemaname'] = ApiClient.convertToType(data['schemaname'], 'String');
            }
            if (data.hasOwnProperty('tabname')) {
                obj['tabname'] = ApiClient.convertToType(data['tabname'], 'String');
            }
            if (data.hasOwnProperty('operation')) {
                obj['operation'] = ApiClient.convertToType(data['operation'], 'String');
            }
            if (data.hasOwnProperty('fk_table')) {
                obj['fk_table'] = ApiClient.convertToType(data['fk_table'], 'Number');
            }
            if (data.hasOwnProperty('new_val')) {
                obj['new_val'] = ApiClient.convertToType(data['new_val'], {'String': Object});
            }
            if (data.hasOwnProperty('old_val')) {
                obj['old_val'] = ApiClient.convertToType(data['old_val'], {'String': Object});
            }
            if (data.hasOwnProperty('who_api')) {
                obj['who_api'] = ApiClient.convertToType(data['who_api'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LogEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LogEntry</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['schemaname'] && !(typeof data['schemaname'] === 'string' || data['schemaname'] instanceof String)) {
            throw new Error("Expected the field `schemaname` to be a primitive type in the JSON string but got " + data['schemaname']);
        }
        // ensure the json data is a string
        if (data['tabname'] && !(typeof data['tabname'] === 'string' || data['tabname'] instanceof String)) {
            throw new Error("Expected the field `tabname` to be a primitive type in the JSON string but got " + data['tabname']);
        }
        // ensure the json data is a string
        if (data['operation'] && !(typeof data['operation'] === 'string' || data['operation'] instanceof String)) {
            throw new Error("Expected the field `operation` to be a primitive type in the JSON string but got " + data['operation']);
        }
        // ensure the json data is a string
        if (data['who_api'] && !(typeof data['who_api'] === 'string' || data['who_api'] instanceof String)) {
            throw new Error("Expected the field `who_api` to be a primitive type in the JSON string but got " + data['who_api']);
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying this log entry.
 * @member {Number} pk
 */
LogEntry.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
LogEntry.prototype['uuid'] = undefined;

/**
 * Name of the schema containing the changed table, captured from the trigger context (TG_TABLE_SCHEMA).
 * @member {String} schemaname
 */
LogEntry.prototype['schemaname'] = undefined;

/**
 * Name of the table whose row changed, captured from the trigger context (TG_TABLE_NAME).
 * @member {String} tabname
 */
LogEntry.prototype['tabname'] = undefined;

/**
 * Kind of change that occurred: 'INSERT', 'UPDATE' or 'DELETE' (the trigger's TG_OP).
 * @member {String} operation
 */
LogEntry.prototype['operation'] = undefined;

/**
 * Primary key (pk) of the affected row in the source table — NEW.pk for INSERT/UPDATE, OLD.pk for DELETE. A logical reference only; not a database foreign key, since it may point into any audited table.
 * @member {Number} fk_table
 */
LogEntry.prototype['fk_table'] = undefined;

/**
 * Full state of the row after the change, serialized as JSONB (to_jsonb(NEW)). NULL for DELETE operations.
 * @member {Object.<String, Object>} new_val
 */
LogEntry.prototype['new_val'] = undefined;

/**
 * Full state of the row before the change, serialized as JSONB (to_jsonb(OLD)). NULL for INSERT operations.
 * @member {Object.<String, Object>} old_val
 */
LogEntry.prototype['old_val'] = undefined;

/**
 * API role that performed the change, captured via current_user at the time of the operation.
 * @member {String} who_api
 */
LogEntry.prototype['who_api'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
LogEntry.prototype['created_at'] = undefined;






export { LogEntry };

/**
 * The LogEntryQuery model module.
 * @module model/LogEntryQuery
 * @version 0.9.0
 */
class LogEntryQuery {
    /**
     * Constructs a new <code>LogEntryQuery</code>.
     * Query payload for log entry rows: SQL WHERE clause plus limit/offset pagination.
     * @alias module:model/LogEntryQuery
     */
    constructor() { 
        
        LogEntryQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>LogEntryQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LogEntryQuery} obj Optional instance to populate.
     * @return {module:model/LogEntryQuery} The populated <code>LogEntryQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LogEntryQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LogEntryQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LogEntryQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
LogEntryQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
LogEntryQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
LogEntryQuery.prototype['offset'] = undefined;






export { LogEntryQuery };

/**
 * The LogEntryQuerySystem model module.
 * @module model/LogEntryQuerySystem
 * @version 0.9.0
 */
class LogEntryQuerySystem {
    /**
     * Constructs a new <code>LogEntryQuerySystem</code>.
     * Query payload for log entry rows: SQL WHERE clause plus limit/offset pagination.
     * @alias module:model/LogEntryQuerySystem
     */
    constructor() { 
        
        LogEntryQuerySystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>LogEntryQuerySystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LogEntryQuerySystem} obj Optional instance to populate.
     * @return {module:model/LogEntryQuerySystem} The populated <code>LogEntryQuerySystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LogEntryQuerySystem();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LogEntryQuerySystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LogEntryQuerySystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
LogEntryQuerySystem.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
LogEntryQuerySystem.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
LogEntryQuerySystem.prototype['offset'] = undefined;






export { LogEntryQuerySystem };

/**
 * The LogEntrySystem model module.
 * @module model/LogEntrySystem
 * @version 0.9.0
 */
class LogEntrySystem {
    /**
     * Constructs a new <code>LogEntrySystem</code>.
     * Append-only audit trail of all data changes in the &#x60;system&#x60; schema.  One row is written for every INSERT, UPDATE and DELETE on the audited base tables (&#x60;&#x60;usr_users&#x60;&#x60;, &#x60;&#x60;org_organizations&#x60;&#x60;, &#x60;&#x60;usr_2_org&#x60;&#x60; — see &#x60;&#x60;TRIGGER_TABLES&#x60;&#x60;) by the generic &#x60;&#x60;system.log_change()&#x60;&#x60; trigger (defined in &#x60;&#x60;pg_migrations/system/pg_db_cmd.py::get_sql_logging_trigger&#x60;&#x60;). Each entry captures which table and row changed, the kind of change, who made it, when it happened, and the full row state before and/or after the change as JSONB.  The trigger fires AFTER the row operation, so &#x60;&#x60;new_val&#x60;&#x60; reflects the final persisted row (including the &#x60;&#x60;updated_at&#x60;&#x60; set by the BEFORE-UPDATE &#x60;&#x60;set_updated_at()&#x60;&#x60; trigger).  Rows are only ever inserted — the application never updates or deletes them, which is why this is the one table in the schema without an &#x60;&#x60;updated_at&#x60;&#x60;.  Idea: https://www.cybertec-postgresql.com/en/tracking-changes-in-postgresql/
     * @alias module:model/LogEntrySystem
     */
    constructor() { 
        
        LogEntrySystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>LogEntrySystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LogEntrySystem} obj Optional instance to populate.
     * @return {module:model/LogEntrySystem} The populated <code>LogEntrySystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LogEntrySystem();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('schemaname')) {
                obj['schemaname'] = ApiClient.convertToType(data['schemaname'], 'String');
            }
            if (data.hasOwnProperty('tabname')) {
                obj['tabname'] = ApiClient.convertToType(data['tabname'], 'String');
            }
            if (data.hasOwnProperty('operation')) {
                obj['operation'] = ApiClient.convertToType(data['operation'], 'String');
            }
            if (data.hasOwnProperty('fk_table')) {
                obj['fk_table'] = ApiClient.convertToType(data['fk_table'], 'Number');
            }
            if (data.hasOwnProperty('new_val')) {
                obj['new_val'] = ApiClient.convertToType(data['new_val'], {'String': Object});
            }
            if (data.hasOwnProperty('old_val')) {
                obj['old_val'] = ApiClient.convertToType(data['old_val'], {'String': Object});
            }
            if (data.hasOwnProperty('who')) {
                obj['who'] = ApiClient.convertToType(data['who'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LogEntrySystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LogEntrySystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['schemaname'] && !(typeof data['schemaname'] === 'string' || data['schemaname'] instanceof String)) {
            throw new Error("Expected the field `schemaname` to be a primitive type in the JSON string but got " + data['schemaname']);
        }
        // ensure the json data is a string
        if (data['tabname'] && !(typeof data['tabname'] === 'string' || data['tabname'] instanceof String)) {
            throw new Error("Expected the field `tabname` to be a primitive type in the JSON string but got " + data['tabname']);
        }
        // ensure the json data is a string
        if (data['operation'] && !(typeof data['operation'] === 'string' || data['operation'] instanceof String)) {
            throw new Error("Expected the field `operation` to be a primitive type in the JSON string but got " + data['operation']);
        }
        // ensure the json data is a string
        if (data['who'] && !(typeof data['who'] === 'string' || data['who'] instanceof String)) {
            throw new Error("Expected the field `who` to be a primitive type in the JSON string but got " + data['who']);
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying this log entry.
 * @member {Number} pk
 */
LogEntrySystem.prototype['pk'] = undefined;

/**
 * Name of the schema containing the changed table, captured from the trigger context (TG_TABLE_SCHEMA).
 * @member {String} schemaname
 */
LogEntrySystem.prototype['schemaname'] = undefined;

/**
 * Name of the table whose row changed, captured from the trigger context (TG_TABLE_NAME).
 * @member {String} tabname
 */
LogEntrySystem.prototype['tabname'] = undefined;

/**
 * Kind of change that occurred, written by the trigger as 'create' (INSERT), 'update' (UPDATE) or 'delete' (DELETE).
 * @member {String} operation
 */
LogEntrySystem.prototype['operation'] = undefined;

/**
 * Primary key (pk) of the affected row in the source table — NEW.pk for INSERT/UPDATE, OLD.pk for DELETE. A logical reference only; not a database foreign key, since it may point into any audited table.
 * @member {Number} fk_table
 */
LogEntrySystem.prototype['fk_table'] = undefined;

/**
 * Full state of the row after the change, serialized as JSONB (to_jsonb(NEW)). NULL for DELETE operations.
 * @member {Object.<String, Object>} new_val
 */
LogEntrySystem.prototype['new_val'] = undefined;

/**
 * Full state of the row before the change, serialized as JSONB (to_jsonb(OLD)). NULL for INSERT operations.
 * @member {Object.<String, Object>} old_val
 */
LogEntrySystem.prototype['old_val'] = undefined;

/**
 * Database role that performed the change, captured via current_user at the time of the operation. This is the connection role, not the application user.
 * @member {String} who
 */
LogEntrySystem.prototype['who'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the log entry was written, i.e. when the audited change was committed.
 * @member {Date} created_at
 */
LogEntrySystem.prototype['created_at'] = undefined;






export { LogEntrySystem };

/**
* Enum class McpDataStateType.
* @enum {}
* @readonly
*/
export class McpDataStateType {
    
        /**
         * value: "created"
         * @const
         */
        "CREATED" = "created";

    
        /**
         * value: "updated"
         * @const
         */
        "UPDATED" = "updated";

    
        /**
         * value: "deletion_preparation"
         * @const
         */
        "DELETION_PREPARATION" = "deletion_preparation";

    
        /**
         * value: "undefined"
         * @const
         */
        "UNDEFINED" = "undefined";

    

    /**
    * Returns a <code>McpDataStateType</code> enum value from a Javascript object name.
    * @param {Object} data The plain JavaScript object containing the name of the enum value.
    * @return {module:model/McpDataStateType} The enum <code>McpDataStateType</code> value.
    */
    static constructFromObject(object) {
        return object;
    }
}

/**
 * The OperationalSkill model module.
 * @module model/OperationalSkill
 * @version 0.9.0
 */
class OperationalSkill {
    /**
     * Constructs a new <code>OperationalSkill</code>.
     * Operational skill record associated with areas of operational expertise.
     * @alias module:model/OperationalSkill
     * @param fkProject {Number} Reference to the owning project this operational skill belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        OperationalSkill.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>OperationalSkill</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OperationalSkill} obj Optional instance to populate.
     * @return {module:model/OperationalSkill} The populated <code>OperationalSkill</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OperationalSkill();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('tsl_title')) {
                obj['tsl_title'] = ApiClient.convertToType(data['tsl_title'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_title_txt')) {
                obj['tsl_title_txt'] = ApiClient.convertToType(data['tsl_title_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OperationalSkill</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OperationalSkill</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OperationalSkill.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_title_txt'] && !(typeof data['tsl_title_txt'] === 'string' || data['tsl_title_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_title_txt` to be a primitive type in the JSON string but got " + data['tsl_title_txt']);
        }

        return true;
    }


}

OperationalSkill.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the operational skill row.
 * @member {Number} pk
 */
OperationalSkill.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
OperationalSkill.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this operational skill belongs to; cascades on delete.
 * @member {Number} fk_project
 */
OperationalSkill.prototype['fk_project'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual title of the operational skill; logical reference, not a database foreign key.
 * @member {Number} tsl_title
 */
OperationalSkill.prototype['tsl_title'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
OperationalSkill.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
OperationalSkill.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
OperationalSkill.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
OperationalSkill.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
OperationalSkill.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
OperationalSkill.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
OperationalSkill.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
OperationalSkill.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
OperationalSkill.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual title of the operational skill; logical reference, not a database foreign key.
 * @member {String} tsl_title_txt
 */
OperationalSkill.prototype['tsl_title_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
OperationalSkill.prototype['tsl_language'] = undefined;






export { OperationalSkill };

/**
 * The OperationalSkillView model module.
 * @module model/OperationalSkillView
 * @version 0.9.0
 */
class OperationalSkillView {
    /**
     * Constructs a new <code>OperationalSkillView</code>.
     * OperationalSkill row extended with its nested pec_2_ops_2_aoe_2_cur junction rows.
     * @alias module:model/OperationalSkillView
     * @param fkProject {Number} Reference to the owning project this operational skill belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        OperationalSkillView.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>OperationalSkillView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OperationalSkillView} obj Optional instance to populate.
     * @return {module:model/OperationalSkillView} The populated <code>OperationalSkillView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OperationalSkillView();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('tsl_title')) {
                obj['tsl_title'] = ApiClient.convertToType(data['tsl_title'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_title_txt')) {
                obj['tsl_title_txt'] = ApiClient.convertToType(data['tsl_title_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
            if (data.hasOwnProperty('pec_2_ops_2_aoe_2_cur')) {
                obj['pec_2_ops_2_aoe_2_cur'] = ApiClient.convertToType(data['pec_2_ops_2_aoe_2_cur'], [Pec2Ops2Aoe2CurView]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OperationalSkillView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OperationalSkillView</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OperationalSkillView.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_title_txt'] && !(typeof data['tsl_title_txt'] === 'string' || data['tsl_title_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_title_txt` to be a primitive type in the JSON string but got " + data['tsl_title_txt']);
        }
        if (data['pec_2_ops_2_aoe_2_cur']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pec_2_ops_2_aoe_2_cur'])) {
                throw new Error("Expected the field `pec_2_ops_2_aoe_2_cur` to be an array in the JSON data but got " + data['pec_2_ops_2_aoe_2_cur']);
            }
            // validate the optional field `pec_2_ops_2_aoe_2_cur` (array)
            for (const item of data['pec_2_ops_2_aoe_2_cur']) {
                Pec2Ops2Aoe2CurView.validateJSON(item);
            };
        }

        return true;
    }


}

OperationalSkillView.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the operational skill row.
 * @member {Number} pk
 */
OperationalSkillView.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
OperationalSkillView.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this operational skill belongs to; cascades on delete.
 * @member {Number} fk_project
 */
OperationalSkillView.prototype['fk_project'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual title of the operational skill; logical reference, not a database foreign key.
 * @member {Number} tsl_title
 */
OperationalSkillView.prototype['tsl_title'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
OperationalSkillView.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
OperationalSkillView.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
OperationalSkillView.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
OperationalSkillView.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
OperationalSkillView.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
OperationalSkillView.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
OperationalSkillView.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
OperationalSkillView.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
OperationalSkillView.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual title of the operational skill; logical reference, not a database foreign key.
 * @member {String} tsl_title_txt
 */
OperationalSkillView.prototype['tsl_title_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
OperationalSkillView.prototype['tsl_language'] = undefined;

/**
 * Performance criterion to operational skill rows
 * @member {Array.<module:model/Pec2Ops2Aoe2CurView>} pec_2_ops_2_aoe_2_cur
 */
OperationalSkillView.prototype['pec_2_ops_2_aoe_2_cur'] = undefined;






export { OperationalSkillView };

/**
 * The OperationalSkills model module.
 * @module model/OperationalSkills
 * @version 0.9.0
 */
class OperationalSkills {
    /**
     * Constructs a new <code>OperationalSkills</code>.
     * OperationalSkill result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/OperationalSkills
     */
    constructor() { 
        
        OperationalSkills.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>OperationalSkills</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OperationalSkills} obj Optional instance to populate.
     * @return {module:model/OperationalSkills} The populated <code>OperationalSkills</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OperationalSkills();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [OperationalSkill]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OperationalSkills</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OperationalSkills</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                OperationalSkill.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
OperationalSkills.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
OperationalSkills.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
OperationalSkills.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
OperationalSkills.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
OperationalSkills.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
OperationalSkills.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
OperationalSkills.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/OperationalSkill>} rows
 */
OperationalSkills.prototype['rows'] = undefined;






export { OperationalSkills };

/**
 * The Ops2Aoe2Cur model module.
 * @module model/Ops2Aoe2Cur
 * @version 0.9.0
 */
class Ops2Aoe2Cur {
    /**
     * Constructs a new <code>Ops2Aoe2Cur</code>.
     * Link table connecting operational skills to area-of-operational-expertise-to-curriculum links.
     * @alias module:model/Ops2Aoe2Cur
     * @param fkProject {Number} Reference to the owning project this link belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        Ops2Aoe2Cur.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>Ops2Aoe2Cur</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Ops2Aoe2Cur} obj Optional instance to populate.
     * @return {module:model/Ops2Aoe2Cur} The populated <code>Ops2Aoe2Cur</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Ops2Aoe2Cur();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('fk_aoe_2_cur')) {
                obj['fk_aoe_2_cur'] = ApiClient.convertToType(data['fk_aoe_2_cur'], 'Number');
            }
            if (data.hasOwnProperty('fk_ops')) {
                obj['fk_ops'] = ApiClient.convertToType(data['fk_ops'], 'Number');
            }
            if (data.hasOwnProperty('fk_ops_level')) {
                obj['fk_ops_level'] = ApiClient.convertToType(data['fk_ops_level'], 'Number');
            }
            if (data.hasOwnProperty('fk_ops_use')) {
                obj['fk_ops_use'] = ApiClient.convertToType(data['fk_ops_use'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur')) {
                obj['fk_cur'] = ApiClient.convertToType(data['fk_cur'], 'Number');
            }
            if (data.hasOwnProperty('fk_aoe')) {
                obj['fk_aoe'] = ApiClient.convertToType(data['fk_aoe'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur_title')) {
                obj['fk_cur_title'] = ApiClient.convertToType(data['fk_cur_title'], 'Number');
            }
            if (data.hasOwnProperty('tsl_label')) {
                obj['tsl_label'] = ApiClient.convertToType(data['tsl_label'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_label_txt')) {
                obj['tsl_label_txt'] = ApiClient.convertToType(data['tsl_label_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Ops2Aoe2Cur</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Ops2Aoe2Cur</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Ops2Aoe2Cur.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_label_txt'] && !(typeof data['tsl_label_txt'] === 'string' || data['tsl_label_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_label_txt` to be a primitive type in the JSON string but got " + data['tsl_label_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }

        return true;
    }


}

Ops2Aoe2Cur.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying this operational-skill-to-area-of-expertise link row.
 * @member {Number} pk
 */
Ops2Aoe2Cur.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
Ops2Aoe2Cur.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this link belongs to; cascades on delete.
 * @member {Number} fk_project
 */
Ops2Aoe2Cur.prototype['fk_project'] = undefined;

/**
 * Reference to the linked area-of-operational-expertise-to-curriculum row; cascades on delete.
 * @member {Number} fk_aoe_2_cur
 */
Ops2Aoe2Cur.prototype['fk_aoe_2_cur'] = undefined;

/**
 * Reference to the linked operational skill; cascades on delete.
 * @member {Number} fk_ops
 */
Ops2Aoe2Cur.prototype['fk_ops'] = undefined;

/**
 * Reference to the operational skill level assigned to this link; set to NULL on delete.
 * @member {Number} fk_ops_level
 */
Ops2Aoe2Cur.prototype['fk_ops_level'] = undefined;

/**
 * Reference to the operational skill use assigned to this link; set to NULL on delete.
 * @member {Number} fk_ops_use
 */
Ops2Aoe2Cur.prototype['fk_ops_use'] = undefined;

/**
 * Calculated copy of the fk_cur of the linked aoe_2_cur row; maintained by a database trigger, values sent by the client are ignored.
 * @member {Number} fk_cur
 */
Ops2Aoe2Cur.prototype['fk_cur'] = undefined;

/**
 * Calculated copy of the fk_aoe of the linked aoe_2_cur row; maintained by a database trigger, values sent by the client are ignored.
 * @member {Number} fk_aoe
 */
Ops2Aoe2Cur.prototype['fk_aoe'] = undefined;

/**
 * Reference to the linked curriculum title; set to NULL on delete.
 * @member {Number} fk_cur_title
 */
Ops2Aoe2Cur.prototype['fk_cur_title'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this operational-skill-to-area-of-expertise link; logical reference, not a database foreign key.
 * @member {Number} tsl_label
 */
Ops2Aoe2Cur.prototype['tsl_label'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this operational-skill-to-area-of-expertise link; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
Ops2Aoe2Cur.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
Ops2Aoe2Cur.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
Ops2Aoe2Cur.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
Ops2Aoe2Cur.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
Ops2Aoe2Cur.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
Ops2Aoe2Cur.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
Ops2Aoe2Cur.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
Ops2Aoe2Cur.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
Ops2Aoe2Cur.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
Ops2Aoe2Cur.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this operational-skill-to-area-of-expertise link; logical reference, not a database foreign key.
 * @member {String} tsl_label_txt
 */
Ops2Aoe2Cur.prototype['tsl_label_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this operational-skill-to-area-of-expertise link; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
Ops2Aoe2Cur.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
Ops2Aoe2Cur.prototype['tsl_language'] = undefined;






export { Ops2Aoe2Cur };

/**
 * The Ops2Aoe2CurQuery model module.
 * @module model/Ops2Aoe2CurQuery
 * @version 0.9.0
 */
class Ops2Aoe2CurQuery {
    /**
     * Constructs a new <code>Ops2Aoe2CurQuery</code>.
     * Query payload for operational-skill-to-area-of-operational-expertise-to-curriculum junction rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/Ops2Aoe2CurQuery
     */
    constructor() { 
        
        Ops2Aoe2CurQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Ops2Aoe2CurQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Ops2Aoe2CurQuery} obj Optional instance to populate.
     * @return {module:model/Ops2Aoe2CurQuery} The populated <code>Ops2Aoe2CurQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Ops2Aoe2CurQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Ops2Aoe2CurQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Ops2Aoe2CurQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Ops2Aoe2CurQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Ops2Aoe2CurQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Ops2Aoe2CurQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
Ops2Aoe2CurQuery.prototype['language'] = undefined;






export { Ops2Aoe2CurQuery };

/**
 * The Ops2Aoe2CurView model module.
 * @module model/Ops2Aoe2CurView
 * @version 0.9.0
 */
class Ops2Aoe2CurView {
    /**
     * Constructs a new <code>Ops2Aoe2CurView</code>.
     * Ops2Aoe2Cur junction row extended with its nested operational skill, use and level rows.
     * @alias module:model/Ops2Aoe2CurView
     * @param fkProject {Number} Reference to the owning project this link belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        Ops2Aoe2CurView.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>Ops2Aoe2CurView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Ops2Aoe2CurView} obj Optional instance to populate.
     * @return {module:model/Ops2Aoe2CurView} The populated <code>Ops2Aoe2CurView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Ops2Aoe2CurView();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('fk_aoe_2_cur')) {
                obj['fk_aoe_2_cur'] = ApiClient.convertToType(data['fk_aoe_2_cur'], 'Number');
            }
            if (data.hasOwnProperty('fk_ops')) {
                obj['fk_ops'] = ApiClient.convertToType(data['fk_ops'], 'Number');
            }
            if (data.hasOwnProperty('fk_ops_level')) {
                obj['fk_ops_level'] = ApiClient.convertToType(data['fk_ops_level'], 'Number');
            }
            if (data.hasOwnProperty('fk_ops_use')) {
                obj['fk_ops_use'] = ApiClient.convertToType(data['fk_ops_use'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur')) {
                obj['fk_cur'] = ApiClient.convertToType(data['fk_cur'], 'Number');
            }
            if (data.hasOwnProperty('fk_aoe')) {
                obj['fk_aoe'] = ApiClient.convertToType(data['fk_aoe'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur_title')) {
                obj['fk_cur_title'] = ApiClient.convertToType(data['fk_cur_title'], 'Number');
            }
            if (data.hasOwnProperty('tsl_label')) {
                obj['tsl_label'] = ApiClient.convertToType(data['tsl_label'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_label_txt')) {
                obj['tsl_label_txt'] = ApiClient.convertToType(data['tsl_label_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
            if (data.hasOwnProperty('operational_skills')) {
                obj['operational_skills'] = ApiClient.convertToType(data['operational_skills'], [OperationalSkillView]);
            }
            if (data.hasOwnProperty('ops_uses')) {
                obj['ops_uses'] = ApiClient.convertToType(data['ops_uses'], [OpsUse]);
            }
            if (data.hasOwnProperty('ops_levels')) {
                obj['ops_levels'] = ApiClient.convertToType(data['ops_levels'], [OpsLevel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Ops2Aoe2CurView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Ops2Aoe2CurView</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Ops2Aoe2CurView.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_label_txt'] && !(typeof data['tsl_label_txt'] === 'string' || data['tsl_label_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_label_txt` to be a primitive type in the JSON string but got " + data['tsl_label_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }
        if (data['operational_skills']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['operational_skills'])) {
                throw new Error("Expected the field `operational_skills` to be an array in the JSON data but got " + data['operational_skills']);
            }
            // validate the optional field `operational_skills` (array)
            for (const item of data['operational_skills']) {
                OperationalSkillView.validateJSON(item);
            };
        }
        if (data['ops_uses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ops_uses'])) {
                throw new Error("Expected the field `ops_uses` to be an array in the JSON data but got " + data['ops_uses']);
            }
            // validate the optional field `ops_uses` (array)
            for (const item of data['ops_uses']) {
                OpsUse.validateJSON(item);
            };
        }
        if (data['ops_levels']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ops_levels'])) {
                throw new Error("Expected the field `ops_levels` to be an array in the JSON data but got " + data['ops_levels']);
            }
            // validate the optional field `ops_levels` (array)
            for (const item of data['ops_levels']) {
                OpsLevel.validateJSON(item);
            };
        }

        return true;
    }


}

Ops2Aoe2CurView.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying this operational-skill-to-area-of-expertise link row.
 * @member {Number} pk
 */
Ops2Aoe2CurView.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
Ops2Aoe2CurView.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this link belongs to; cascades on delete.
 * @member {Number} fk_project
 */
Ops2Aoe2CurView.prototype['fk_project'] = undefined;

/**
 * Reference to the linked area-of-operational-expertise-to-curriculum row; cascades on delete.
 * @member {Number} fk_aoe_2_cur
 */
Ops2Aoe2CurView.prototype['fk_aoe_2_cur'] = undefined;

/**
 * Reference to the linked operational skill; cascades on delete.
 * @member {Number} fk_ops
 */
Ops2Aoe2CurView.prototype['fk_ops'] = undefined;

/**
 * Reference to the operational skill level assigned to this link; set to NULL on delete.
 * @member {Number} fk_ops_level
 */
Ops2Aoe2CurView.prototype['fk_ops_level'] = undefined;

/**
 * Reference to the operational skill use assigned to this link; set to NULL on delete.
 * @member {Number} fk_ops_use
 */
Ops2Aoe2CurView.prototype['fk_ops_use'] = undefined;

/**
 * Calculated copy of the fk_cur of the linked aoe_2_cur row; maintained by a database trigger, values sent by the client are ignored.
 * @member {Number} fk_cur
 */
Ops2Aoe2CurView.prototype['fk_cur'] = undefined;

/**
 * Calculated copy of the fk_aoe of the linked aoe_2_cur row; maintained by a database trigger, values sent by the client are ignored.
 * @member {Number} fk_aoe
 */
Ops2Aoe2CurView.prototype['fk_aoe'] = undefined;

/**
 * Reference to the linked curriculum title; set to NULL on delete.
 * @member {Number} fk_cur_title
 */
Ops2Aoe2CurView.prototype['fk_cur_title'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this operational-skill-to-area-of-expertise link; logical reference, not a database foreign key.
 * @member {Number} tsl_label
 */
Ops2Aoe2CurView.prototype['tsl_label'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this operational-skill-to-area-of-expertise link; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
Ops2Aoe2CurView.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
Ops2Aoe2CurView.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
Ops2Aoe2CurView.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
Ops2Aoe2CurView.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
Ops2Aoe2CurView.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
Ops2Aoe2CurView.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
Ops2Aoe2CurView.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
Ops2Aoe2CurView.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
Ops2Aoe2CurView.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
Ops2Aoe2CurView.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this operational-skill-to-area-of-expertise link; logical reference, not a database foreign key.
 * @member {String} tsl_label_txt
 */
Ops2Aoe2CurView.prototype['tsl_label_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this operational-skill-to-area-of-expertise link; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
Ops2Aoe2CurView.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
Ops2Aoe2CurView.prototype['tsl_language'] = undefined;

/**
 * Operational skill rows
 * @member {Array.<module:model/OperationalSkillView>} operational_skills
 */
Ops2Aoe2CurView.prototype['operational_skills'] = undefined;

/**
 * Operational skill use rows
 * @member {Array.<module:model/OpsUse>} ops_uses
 */
Ops2Aoe2CurView.prototype['ops_uses'] = undefined;

/**
 * Operational skill level rows
 * @member {Array.<module:model/OpsLevel>} ops_levels
 */
Ops2Aoe2CurView.prototype['ops_levels'] = undefined;






export { Ops2Aoe2CurView };

/**
 * The Ops2Aoe2Curs model module.
 * @module model/Ops2Aoe2Curs
 * @version 0.9.0
 */
class Ops2Aoe2Curs {
    /**
     * Constructs a new <code>Ops2Aoe2Curs</code>.
     * Ops2Aoe2Cur result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/Ops2Aoe2Curs
     */
    constructor() { 
        
        Ops2Aoe2Curs.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Ops2Aoe2Curs</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Ops2Aoe2Curs} obj Optional instance to populate.
     * @return {module:model/Ops2Aoe2Curs} The populated <code>Ops2Aoe2Curs</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Ops2Aoe2Curs();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [Ops2Aoe2Cur]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Ops2Aoe2Curs</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Ops2Aoe2Curs</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                Ops2Aoe2Cur.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Ops2Aoe2Curs.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Ops2Aoe2Curs.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Ops2Aoe2Curs.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
Ops2Aoe2Curs.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
Ops2Aoe2Curs.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
Ops2Aoe2Curs.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
Ops2Aoe2Curs.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/Ops2Aoe2Cur>} rows
 */
Ops2Aoe2Curs.prototype['rows'] = undefined;






export { Ops2Aoe2Curs };

/**
 * The OpsLevel model module.
 * @module model/OpsLevel
 * @version 0.9.0
 */
class OpsLevel {
    /**
     * Constructs a new <code>OpsLevel</code>.
     * Project-specific lookup row defining an operational skill level.
     * @alias module:model/OpsLevel
     * @param fkProject {Number} Reference to the owning project this operational skill level belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        OpsLevel.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>OpsLevel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OpsLevel} obj Optional instance to populate.
     * @return {module:model/OpsLevel} The populated <code>OpsLevel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OpsLevel();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('tsl_label')) {
                obj['tsl_label'] = ApiClient.convertToType(data['tsl_label'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_label_txt')) {
                obj['tsl_label_txt'] = ApiClient.convertToType(data['tsl_label_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OpsLevel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OpsLevel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OpsLevel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['tsl_label_txt'] && !(typeof data['tsl_label_txt'] === 'string' || data['tsl_label_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_label_txt` to be a primitive type in the JSON string but got " + data['tsl_label_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }

        return true;
    }


}

OpsLevel.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the operational skill level row.
 * @member {Number} pk
 */
OpsLevel.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
OpsLevel.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this operational skill level belongs to; cascades on delete.
 * @member {Number} fk_project
 */
OpsLevel.prototype['fk_project'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual operational skill level label; logical reference, not a database foreign key.
 * @member {Number} tsl_label
 */
OpsLevel.prototype['tsl_label'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual operational skill level description; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
OpsLevel.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
OpsLevel.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
OpsLevel.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
OpsLevel.prototype['mcp_data_state'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
OpsLevel.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
OpsLevel.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
OpsLevel.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual operational skill level label; logical reference, not a database foreign key.
 * @member {String} tsl_label_txt
 */
OpsLevel.prototype['tsl_label_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual operational skill level description; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
OpsLevel.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
OpsLevel.prototype['tsl_language'] = undefined;






export { OpsLevel };

/**
 * The OpsLevelQuery model module.
 * @module model/OpsLevelQuery
 * @version 0.9.0
 */
class OpsLevelQuery {
    /**
     * Constructs a new <code>OpsLevelQuery</code>.
     * Query payload for operational skill level rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/OpsLevelQuery
     */
    constructor() { 
        
        OpsLevelQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>OpsLevelQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OpsLevelQuery} obj Optional instance to populate.
     * @return {module:model/OpsLevelQuery} The populated <code>OpsLevelQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OpsLevelQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OpsLevelQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OpsLevelQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
OpsLevelQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
OpsLevelQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
OpsLevelQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
OpsLevelQuery.prototype['language'] = undefined;






export { OpsLevelQuery };

/**
 * The OpsLevels model module.
 * @module model/OpsLevels
 * @version 0.9.0
 */
class OpsLevels {
    /**
     * Constructs a new <code>OpsLevels</code>.
     * OpsLevel result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/OpsLevels
     */
    constructor() { 
        
        OpsLevels.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>OpsLevels</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OpsLevels} obj Optional instance to populate.
     * @return {module:model/OpsLevels} The populated <code>OpsLevels</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OpsLevels();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [OpsLevel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OpsLevels</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OpsLevels</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                OpsLevel.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
OpsLevels.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
OpsLevels.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
OpsLevels.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
OpsLevels.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
OpsLevels.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
OpsLevels.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
OpsLevels.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/OpsLevel>} rows
 */
OpsLevels.prototype['rows'] = undefined;






export { OpsLevels };

/**
 * The OpsQuery model module.
 * @module model/OpsQuery
 * @version 0.9.0
 */
class OpsQuery {
    /**
     * Constructs a new <code>OpsQuery</code>.
     * Query payload for operational skill rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/OpsQuery
     */
    constructor() { 
        
        OpsQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>OpsQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OpsQuery} obj Optional instance to populate.
     * @return {module:model/OpsQuery} The populated <code>OpsQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OpsQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OpsQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OpsQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
OpsQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
OpsQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
OpsQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
OpsQuery.prototype['language'] = undefined;






export { OpsQuery };

/**
 * The OpsUse model module.
 * @module model/OpsUse
 * @version 0.9.0
 */
class OpsUse {
    /**
     * Constructs a new <code>OpsUse</code>.
     * Project-specific lookup row defining how an operational skill is used.
     * @alias module:model/OpsUse
     * @param fkProject {Number} Reference to the owning project this operational skill use belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        OpsUse.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>OpsUse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OpsUse} obj Optional instance to populate.
     * @return {module:model/OpsUse} The populated <code>OpsUse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OpsUse();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('tsl_label')) {
                obj['tsl_label'] = ApiClient.convertToType(data['tsl_label'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_label_txt')) {
                obj['tsl_label_txt'] = ApiClient.convertToType(data['tsl_label_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OpsUse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OpsUse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OpsUse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['tsl_label_txt'] && !(typeof data['tsl_label_txt'] === 'string' || data['tsl_label_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_label_txt` to be a primitive type in the JSON string but got " + data['tsl_label_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }

        return true;
    }


}

OpsUse.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the operational skill use row.
 * @member {Number} pk
 */
OpsUse.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
OpsUse.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this operational skill use belongs to; cascades on delete.
 * @member {Number} fk_project
 */
OpsUse.prototype['fk_project'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual operational skill use label; logical reference, not a database foreign key.
 * @member {Number} tsl_label
 */
OpsUse.prototype['tsl_label'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual operational skill use description; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
OpsUse.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
OpsUse.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
OpsUse.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
OpsUse.prototype['mcp_data_state'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
OpsUse.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
OpsUse.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
OpsUse.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual operational skill use label; logical reference, not a database foreign key.
 * @member {String} tsl_label_txt
 */
OpsUse.prototype['tsl_label_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual operational skill use description; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
OpsUse.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
OpsUse.prototype['tsl_language'] = undefined;






export { OpsUse };

/**
 * The OpsUseQuery model module.
 * @module model/OpsUseQuery
 * @version 0.9.0
 */
class OpsUseQuery {
    /**
     * Constructs a new <code>OpsUseQuery</code>.
     * Query payload for operational skill use rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/OpsUseQuery
     */
    constructor() { 
        
        OpsUseQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>OpsUseQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OpsUseQuery} obj Optional instance to populate.
     * @return {module:model/OpsUseQuery} The populated <code>OpsUseQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OpsUseQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OpsUseQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OpsUseQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
OpsUseQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
OpsUseQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
OpsUseQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
OpsUseQuery.prototype['language'] = undefined;






export { OpsUseQuery };

/**
 * The OpsUses model module.
 * @module model/OpsUses
 * @version 0.9.0
 */
class OpsUses {
    /**
     * Constructs a new <code>OpsUses</code>.
     * OpsUse result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/OpsUses
     */
    constructor() { 
        
        OpsUses.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>OpsUses</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OpsUses} obj Optional instance to populate.
     * @return {module:model/OpsUses} The populated <code>OpsUses</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OpsUses();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [OpsUse]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OpsUses</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OpsUses</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                OpsUse.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
OpsUses.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
OpsUses.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
OpsUses.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
OpsUses.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
OpsUses.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
OpsUses.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
OpsUses.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/OpsUse>} rows
 */
OpsUses.prototype['rows'] = undefined;






export { OpsUses };

/**
* Enum class OrgOrganizationType.
* @enum {}
* @readonly
*/
export class OrgOrganizationType {
    
        /**
         * value: "company"
         * @const
         */
        "COMPANY" = "company";

    
        /**
         * value: "school"
         * @const
         */
        "SCHOOL" = "school";

    

    /**
    * Returns a <code>OrgOrganizationType</code> enum value from a Javascript object name.
    * @param {Object} data The plain JavaScript object containing the name of the enum value.
    * @return {module:model/OrgOrganizationType} The enum <code>OrgOrganizationType</code> value.
    */
    static constructFromObject(object) {
        return object;
    }
}

/**
 * The OrganizationQuerySystem model module.
 * @module model/OrganizationQuerySystem
 * @version 0.9.0
 */
class OrganizationQuerySystem {
    /**
     * Constructs a new <code>OrganizationQuerySystem</code>.
     * Query payload for organization rows: SQL WHERE clause plus limit/offset pagination.
     * @alias module:model/OrganizationQuerySystem
     */
    constructor() { 
        
        OrganizationQuerySystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>OrganizationQuerySystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrganizationQuerySystem} obj Optional instance to populate.
     * @return {module:model/OrganizationQuerySystem} The populated <code>OrganizationQuerySystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrganizationQuerySystem();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrganizationQuerySystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrganizationQuerySystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
OrganizationQuerySystem.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
OrganizationQuerySystem.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
OrganizationQuerySystem.prototype['offset'] = undefined;






export { OrganizationQuerySystem };

/**
 * The OrganizationSystem model module.
 * @module model/OrganizationSystem
 * @version 0.9.0
 */
class OrganizationSystem {
    /**
     * Constructs a new <code>OrganizationSystem</code>.
     * An organization (company or school) the platform is operated for.  Organizations are the top-level tenant of the system: users are attached to them through the &#x60;&#x60;usr_2_org&#x60;&#x60; link table, and downstream schemas scope their data by the resulting membership. Every INSERT, UPDATE and DELETE on this table is captured in &#x60;&#x60;log_entries&#x60;&#x60; by the &#x60;&#x60;system.log_change()&#x60;&#x60; trigger.
     * @alias module:model/OrganizationSystem
     */
    constructor() { 
        
        OrganizationSystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OrganizationSystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrganizationSystem} obj Optional instance to populate.
     * @return {module:model/OrganizationSystem} The populated <code>OrganizationSystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrganizationSystem();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], OrgOrganizationType);
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], {'String': Object});
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrganizationSystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrganizationSystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying this organization.
 * @member {Number} pk
 */
OrganizationSystem.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created. Use this instead of pk when referencing the organization outside the database.
 * @member {String} uuid
 */
OrganizationSystem.prototype['uuid'] = undefined;

/**
 * Human-readable name of the organization as it is shown in the user interface.
 * @member {String} name
 */
OrganizationSystem.prototype['name'] = undefined;

/**
 * Kind of organization: 'company' or 'school'. NULL when the kind has not been classified yet.
 * @member {module:model/OrgOrganizationType} type
 */
OrganizationSystem.prototype['type'] = undefined;

/**
 * Marks the row as written through an API endpoint and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/DataStateType} data_state
 */
OrganizationSystem.prototype['data_state'] = undefined;

/**
 * Marks the row as written by an MCP agent and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
OrganizationSystem.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about this organization, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} comments
 */
OrganizationSystem.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes about this organization, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} notes
 */
OrganizationSystem.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata about this organization, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} metadatas
 */
OrganizationSystem.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
OrganizationSystem.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update by the set_updated_at() trigger. Used for optimistic locking by the API.
 * @member {Date} updated_at
 */
OrganizationSystem.prototype['updated_at'] = undefined;






export { OrganizationSystem };

/**
 * The OrganizationView model module.
 * @module model/OrganizationView
 * @version 0.9.0
 */
class OrganizationView {
    /**
     * Constructs a new <code>OrganizationView</code>.
     * Organization row extended with its nested users.
     * @alias module:model/OrganizationView
     */
    constructor() { 
        
        OrganizationView.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OrganizationView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrganizationView} obj Optional instance to populate.
     * @return {module:model/OrganizationView} The populated <code>OrganizationView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrganizationView();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], OrgOrganizationType);
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], {'String': Object});
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('users')) {
                obj['users'] = ApiClient.convertToType(data['users'], [UserView]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrganizationView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrganizationView</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['users']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['users'])) {
                throw new Error("Expected the field `users` to be an array in the JSON data but got " + data['users']);
            }
            // validate the optional field `users` (array)
            for (const item of data['users']) {
                UserView.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying this organization.
 * @member {Number} pk
 */
OrganizationView.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created. Use this instead of pk when referencing the organization outside the database.
 * @member {String} uuid
 */
OrganizationView.prototype['uuid'] = undefined;

/**
 * Human-readable name of the organization as it is shown in the user interface.
 * @member {String} name
 */
OrganizationView.prototype['name'] = undefined;

/**
 * Kind of organization: 'company' or 'school'. NULL when the kind has not been classified yet.
 * @member {module:model/OrgOrganizationType} type
 */
OrganizationView.prototype['type'] = undefined;

/**
 * Marks the row as written through an API endpoint and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/DataStateType} data_state
 */
OrganizationView.prototype['data_state'] = undefined;

/**
 * Marks the row as written by an MCP agent and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
OrganizationView.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about this organization, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} comments
 */
OrganizationView.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes about this organization, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} notes
 */
OrganizationView.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata about this organization, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} metadatas
 */
OrganizationView.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
OrganizationView.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update by the set_updated_at() trigger. Used for optimistic locking by the API.
 * @member {Date} updated_at
 */
OrganizationView.prototype['updated_at'] = undefined;

/**
 * Users assigned to the organization
 * @member {Array.<module:model/UserView>} users
 */
OrganizationView.prototype['users'] = undefined;






export { OrganizationView };

/**
 * The OrganizationViewQuery model module.
 * @module model/OrganizationViewQuery
 * @version 0.9.0
 */
class OrganizationViewQuery {
    /**
     * Constructs a new <code>OrganizationViewQuery</code>.
     * Query parameters for the organization view. The &#x60;where&#x60; filter applies directly to the organizations table.
     * @alias module:model/OrganizationViewQuery
     */
    constructor() { 
        
        OrganizationViewQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'org_organizations.pk > 0';
    }

    /**
     * Constructs a <code>OrganizationViewQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrganizationViewQuery} obj Optional instance to populate.
     * @return {module:model/OrganizationViewQuery} The populated <code>OrganizationViewQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrganizationViewQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrganizationViewQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrganizationViewQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause, applied directly to the organizations table
 * @member {String} where
 * @default 'org_organizations.pk > 0'
 */
OrganizationViewQuery.prototype['where'] = 'org_organizations.pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
OrganizationViewQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
OrganizationViewQuery.prototype['offset'] = undefined;






export { OrganizationViewQuery };

/**
 * The OrganizationsSystem model module.
 * @module model/OrganizationsSystem
 * @version 0.9.0
 */
class OrganizationsSystem {
    /**
     * Constructs a new <code>OrganizationsSystem</code>.
     * Organization result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/OrganizationsSystem
     */
    constructor() { 
        
        OrganizationsSystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>OrganizationsSystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrganizationsSystem} obj Optional instance to populate.
     * @return {module:model/OrganizationsSystem} The populated <code>OrganizationsSystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrganizationsSystem();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [OrganizationSystem]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrganizationsSystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrganizationsSystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                OrganizationSystem.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
OrganizationsSystem.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
OrganizationsSystem.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
OrganizationsSystem.prototype['offset'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
OrganizationsSystem.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
OrganizationsSystem.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
OrganizationsSystem.prototype['all'] = undefined;

/**
 * Organization rows
 * @member {Array.<module:model/OrganizationSystem>} rows
 */
OrganizationsSystem.prototype['rows'] = undefined;






export { OrganizationsSystem };

/**
 * The OrganizationsView model module.
 * @module model/OrganizationsView
 * @version 0.9.0
 */
class OrganizationsView {
    /**
     * Constructs a new <code>OrganizationsView</code>.
     * Organization view result set: echoes the query and carries total/count plus the matching view rows.
     * @alias module:model/OrganizationsView
     */
    constructor() { 
        
        OrganizationsView.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'org_organizations.pk > 0';
    }

    /**
     * Constructs a <code>OrganizationsView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrganizationsView} obj Optional instance to populate.
     * @return {module:model/OrganizationsView} The populated <code>OrganizationsView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrganizationsView();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [OrganizationView]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrganizationsView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrganizationsView</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                OrganizationView.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause, applied directly to the organizations table
 * @member {String} where
 * @default 'org_organizations.pk > 0'
 */
OrganizationsView.prototype['where'] = 'org_organizations.pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
OrganizationsView.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
OrganizationsView.prototype['offset'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
OrganizationsView.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
OrganizationsView.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
OrganizationsView.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/OrganizationView>} rows
 */
OrganizationsView.prototype['rows'] = undefined;






export { OrganizationsView };

/**
 * The PasswordChange model module.
 * @module model/PasswordChange
 * @version 0.9.0
 */
class PasswordChange {
    /**
     * Constructs a new <code>PasswordChange</code>.
     * Request body of /auth/password-change: the caller&#39;s current password and the one to replace it with. The username is deliberately absent - it is taken from the bearer token, so the endpoint can only ever change the password of the account that is calling it.
     * @alias module:model/PasswordChange
     * @param currentPassword {String} The password in force right now, re-checked here even though the request is already authenticated: a stolen token alone must not be enough to lock the owner out of their own account.
     * @param newPassword {String} The password to set. Only a length bound is enforced - no composition rules, which push users towards predictable substitutions rather than longer secrets.
     */
    constructor(currentPassword, newPassword) { 
        
        PasswordChange.initialize(this, currentPassword, newPassword);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, currentPassword, newPassword) { 
        obj['current_password'] = currentPassword;
        obj['new_password'] = newPassword;
    }

    /**
     * Constructs a <code>PasswordChange</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PasswordChange} obj Optional instance to populate.
     * @return {module:model/PasswordChange} The populated <code>PasswordChange</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PasswordChange();

            if (data.hasOwnProperty('current_password')) {
                obj['current_password'] = ApiClient.convertToType(data['current_password'], 'String');
            }
            if (data.hasOwnProperty('new_password')) {
                obj['new_password'] = ApiClient.convertToType(data['new_password'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PasswordChange</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PasswordChange</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PasswordChange.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['current_password'] && !(typeof data['current_password'] === 'string' || data['current_password'] instanceof String)) {
            throw new Error("Expected the field `current_password` to be a primitive type in the JSON string but got " + data['current_password']);
        }
        // ensure the json data is a string
        if (data['new_password'] && !(typeof data['new_password'] === 'string' || data['new_password'] instanceof String)) {
            throw new Error("Expected the field `new_password` to be a primitive type in the JSON string but got " + data['new_password']);
        }

        return true;
    }


}

PasswordChange.RequiredProperties = ["current_password", "new_password"];

/**
 * The password in force right now, re-checked here even though the request is already authenticated: a stolen token alone must not be enough to lock the owner out of their own account.
 * @member {String} current_password
 */
PasswordChange.prototype['current_password'] = undefined;

/**
 * The password to set. Only a length bound is enforced - no composition rules, which push users towards predictable substitutions rather than longer secrets.
 * @member {String} new_password
 */
PasswordChange.prototype['new_password'] = undefined;






export { PasswordChange };

/**
 * The PasswordChangeResult model module.
 * @module model/PasswordChangeResult
 * @version 0.9.0
 */
class PasswordChangeResult {
    /**
     * Constructs a new <code>PasswordChangeResult</code>.
     * Response body of /auth/password-change. Reached only when the password was actually written; a rejected change is an HTTP error, never &#x60;changed: false&#x60;. &#x60;usr_users.require_password_change&#x60; is cleared along with the write, so a caller that was gating on that flag can stop.
     * @alias module:model/PasswordChangeResult
     * @param changed {Boolean} Always true. Present so the response is a self-describing JSON object rather than an empty body. Also means `require_password_change` is now false for this account.
     */
    constructor(changed) { 
        
        PasswordChangeResult.initialize(this, changed);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, changed) { 
        obj['changed'] = changed;
    }

    /**
     * Constructs a <code>PasswordChangeResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PasswordChangeResult} obj Optional instance to populate.
     * @return {module:model/PasswordChangeResult} The populated <code>PasswordChangeResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PasswordChangeResult();

            if (data.hasOwnProperty('changed')) {
                obj['changed'] = ApiClient.convertToType(data['changed'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PasswordChangeResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PasswordChangeResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PasswordChangeResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

PasswordChangeResult.RequiredProperties = ["changed"];

/**
 * Always true. Present so the response is a self-describing JSON object rather than an empty body. Also means `require_password_change` is now false for this account.
 * @member {Boolean} changed
 */
PasswordChangeResult.prototype['changed'] = undefined;






export { PasswordChangeResult };

/**
 * The PasswordCheck model module.
 * @module model/PasswordCheck
 * @version 0.9.0
 */
class PasswordCheck {
    /**
     * Constructs a new <code>PasswordCheck</code>.
     * Request body of /auth/password-check: the one credential pair to verify. A body rather than query parameters, so the password does not end up in a URL, a proxy log or a browser history.
     * @alias module:model/PasswordCheck
     * @param username {String} Login name to check. An unknown name is answered like a wrong password, so the response cannot be used to find out which usernames exist.
     * @param password {String} Plain-text password, verified against the Argon2id hash stored for the user.
     */
    constructor(username, password) { 
        
        PasswordCheck.initialize(this, username, password);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username, password) { 
        obj['username'] = username;
        obj['password'] = password;
    }

    /**
     * Constructs a <code>PasswordCheck</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PasswordCheck} obj Optional instance to populate.
     * @return {module:model/PasswordCheck} The populated <code>PasswordCheck</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PasswordCheck();

            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PasswordCheck</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PasswordCheck</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PasswordCheck.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }

        return true;
    }


}

PasswordCheck.RequiredProperties = ["username", "password"];

/**
 * Login name to check. An unknown name is answered like a wrong password, so the response cannot be used to find out which usernames exist.
 * @member {String} username
 */
PasswordCheck.prototype['username'] = undefined;

/**
 * Plain-text password, verified against the Argon2id hash stored for the user.
 * @member {String} password
 */
PasswordCheck.prototype['password'] = undefined;






export { PasswordCheck };

/**
 * The PasswordCheckResult model module.
 * @module model/PasswordCheckResult
 * @version 0.9.0
 */
class PasswordCheckResult {
    /**
     * Constructs a new <code>PasswordCheckResult</code>.
     * Response body of /auth/password-check: the verdict on one credential pair, plus the interface level the account is meant to be shown. Still no token and no user record, and a rejected check gives no hint as to which part was wrong - &#x60;ui_level&#x60; reads &#x60;none&#x60; there, which is also what a row that has no level set carries, so the two cannot be told apart.
     * @alias module:model/PasswordCheckResult
     * @param valid {Boolean} True only when the username exists, the password matches and the account is enabled. False in every other case, without saying which of the three failed.
     * @param uiLevel {module:model/UsrUiLevelType} The `usr_users.ui_level` of the checked account: the interface level it is meant to be presented at, so a caller can pick the interface in the same round trip it verifies the credentials in. `none` whenever `valid` is false, and also whenever the account itself has no level set. Presentation only - it grants nothing, and what a token may do is decided by the `token_scope_*` columns alone.
     * @param requirePasswordChange {Boolean} True when the account has to replace its password, and the caller is not doing so. False when the account does not have to replace its password, or the caller is doing so. Presentation only - it grants nothing, and what a token may do is decided by the `token_scope_*` columns alone.
     */
    constructor(valid, uiLevel, requirePasswordChange) { 
        
        PasswordCheckResult.initialize(this, valid, uiLevel, requirePasswordChange);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, valid, uiLevel, requirePasswordChange) { 
        obj['valid'] = valid;
        obj['ui_level'] = uiLevel;
        obj['require_password_change'] = requirePasswordChange;
    }

    /**
     * Constructs a <code>PasswordCheckResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PasswordCheckResult} obj Optional instance to populate.
     * @return {module:model/PasswordCheckResult} The populated <code>PasswordCheckResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PasswordCheckResult();

            if (data.hasOwnProperty('valid')) {
                obj['valid'] = ApiClient.convertToType(data['valid'], 'Boolean');
            }
            if (data.hasOwnProperty('ui_level')) {
                obj['ui_level'] = ApiClient.convertToType(data['ui_level'], UsrUiLevelType);
            }
            if (data.hasOwnProperty('require_password_change')) {
                obj['require_password_change'] = ApiClient.convertToType(data['require_password_change'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PasswordCheckResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PasswordCheckResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PasswordCheckResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

PasswordCheckResult.RequiredProperties = ["valid", "ui_level", "require_password_change"];

/**
 * True only when the username exists, the password matches and the account is enabled. False in every other case, without saying which of the three failed.
 * @member {Boolean} valid
 */
PasswordCheckResult.prototype['valid'] = undefined;

/**
 * The `usr_users.ui_level` of the checked account: the interface level it is meant to be presented at, so a caller can pick the interface in the same round trip it verifies the credentials in. `none` whenever `valid` is false, and also whenever the account itself has no level set. Presentation only - it grants nothing, and what a token may do is decided by the `token_scope_*` columns alone.
 * @member {module:model/UsrUiLevelType} ui_level
 */
PasswordCheckResult.prototype['ui_level'] = undefined;

/**
 * True when the account has to replace its password, and the caller is not doing so. False when the account does not have to replace its password, or the caller is doing so. Presentation only - it grants nothing, and what a token may do is decided by the `token_scope_*` columns alone.
 * @member {Boolean} require_password_change
 */
PasswordCheckResult.prototype['require_password_change'] = undefined;






export { PasswordCheckResult };

/**
 * The PasswordGenerated model module.
 * @module model/PasswordGenerated
 * @version 0.9.0
 */
class PasswordGenerated {
    /**
     * Constructs a new <code>PasswordGenerated</code>.
     * Response body of /auth/password-generator: a suggested password and nothing else. It is not stored, not remembered and not tied to any account - it only becomes a password once the caller passes it to /auth/password-change.
     * @alias module:model/PasswordGenerated
     * @param password {String} A freshly generated password of the requested length, drawn from Python's `secrets` module by cacao-password-generator, mixing upper case, lower case, digits and punctuation. A different value on every call.
     */
    constructor(password) { 
        
        PasswordGenerated.initialize(this, password);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, password) { 
        obj['password'] = password;
    }

    /**
     * Constructs a <code>PasswordGenerated</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PasswordGenerated} obj Optional instance to populate.
     * @return {module:model/PasswordGenerated} The populated <code>PasswordGenerated</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PasswordGenerated();

            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PasswordGenerated</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PasswordGenerated</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PasswordGenerated.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }

        return true;
    }


}

PasswordGenerated.RequiredProperties = ["password"];

/**
 * A freshly generated password of the requested length, drawn from Python's `secrets` module by cacao-password-generator, mixing upper case, lower case, digits and punctuation. A different value on every call.
 * @member {String} password
 */
PasswordGenerated.prototype['password'] = undefined;






export { PasswordGenerated };

/**
 * The PasswordReset model module.
 * @module model/PasswordReset
 * @version 0.9.0
 */
class PasswordReset {
    /**
     * Constructs a new <code>PasswordReset</code>.
     * Request body of /auth/password-reset: the account whose password is to be replaced. Named by &#x60;username&#x60; rather than by email because &#x60;usr_users.username&#x60; is unique while &#x60;usr_users.email&#x60; is not - several accounts may share one address, so an address does not identify an account to reset.  No password field, and deliberately no proof of anything: this is the endpoint for somebody who cannot log in, so the only thing it can ask for is the name of the account. What protects the account is that the new password goes to the address on the row and nowhere else.
     * @alias module:model/PasswordReset
     * @param username {String} Login name of the account to reset. An unknown name is answered exactly like a known one, so the response cannot be used to find out which usernames exist.
     */
    constructor(username) { 
        
        PasswordReset.initialize(this, username);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username) { 
        obj['username'] = username;
    }

    /**
     * Constructs a <code>PasswordReset</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PasswordReset} obj Optional instance to populate.
     * @return {module:model/PasswordReset} The populated <code>PasswordReset</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PasswordReset();

            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PasswordReset</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PasswordReset</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PasswordReset.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }

        return true;
    }


}

PasswordReset.RequiredProperties = ["username"];

/**
 * Login name of the account to reset. An unknown name is answered exactly like a known one, so the response cannot be used to find out which usernames exist.
 * @member {String} username
 */
PasswordReset.prototype['username'] = undefined;






export { PasswordReset };

/**
 * The PasswordResetResult model module.
 * @module model/PasswordResetResult
 * @version 0.9.0
 */
class PasswordResetResult {
    /**
     * Constructs a new <code>PasswordResetResult</code>.
     * Response body of /auth/password-reset. &#x60;accepted&#x60; says the request was understood and acted on as far as it could be - not that a password was mailed, which is a thing the caller is deliberately not told, since it would reveal whether the account exists.
     * @alias module:model/PasswordResetResult
     * @param accepted {Boolean} Always true. Present so the response is a self-describing JSON object rather than an empty body. It is not a confirmation that a mail went out: an unknown username, a disabled account and an account without an email address are all answered this way, having changed nothing.
     */
    constructor(accepted) { 
        
        PasswordResetResult.initialize(this, accepted);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accepted) { 
        obj['accepted'] = accepted;
    }

    /**
     * Constructs a <code>PasswordResetResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PasswordResetResult} obj Optional instance to populate.
     * @return {module:model/PasswordResetResult} The populated <code>PasswordResetResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PasswordResetResult();

            if (data.hasOwnProperty('accepted')) {
                obj['accepted'] = ApiClient.convertToType(data['accepted'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PasswordResetResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PasswordResetResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PasswordResetResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

PasswordResetResult.RequiredProperties = ["accepted"];

/**
 * Always true. Present so the response is a self-describing JSON object rather than an empty body. It is not a confirmation that a mail went out: an unknown username, a disabled account and an account without an email address are all answered this way, having changed nothing.
 * @member {Boolean} accepted
 */
PasswordResetResult.prototype['accepted'] = undefined;






export { PasswordResetResult };

/**
 * The Pec2Ops2Aoe2Cur model module.
 * @module model/Pec2Ops2Aoe2Cur
 * @version 0.9.0
 */
class Pec2Ops2Aoe2Cur {
    /**
     * Constructs a new <code>Pec2Ops2Aoe2Cur</code>.
     * Link table connecting performance criteria to operational-skill-to-area-of-expertise links.
     * @alias module:model/Pec2Ops2Aoe2Cur
     * @param fkProject {Number} Reference to the owning project this link belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        Pec2Ops2Aoe2Cur.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>Pec2Ops2Aoe2Cur</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Pec2Ops2Aoe2Cur} obj Optional instance to populate.
     * @return {module:model/Pec2Ops2Aoe2Cur} The populated <code>Pec2Ops2Aoe2Cur</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Pec2Ops2Aoe2Cur();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('fk_ops_2_aoe_2_cur')) {
                obj['fk_ops_2_aoe_2_cur'] = ApiClient.convertToType(data['fk_ops_2_aoe_2_cur'], 'Number');
            }
            if (data.hasOwnProperty('fk_pec')) {
                obj['fk_pec'] = ApiClient.convertToType(data['fk_pec'], 'Number');
            }
            if (data.hasOwnProperty('fk_pec_level')) {
                obj['fk_pec_level'] = ApiClient.convertToType(data['fk_pec_level'], 'Number');
            }
            if (data.hasOwnProperty('fk_pec_learning_location')) {
                obj['fk_pec_learning_location'] = ApiClient.convertToType(data['fk_pec_learning_location'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur_title')) {
                obj['fk_cur_title'] = ApiClient.convertToType(data['fk_cur_title'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur')) {
                obj['fk_cur'] = ApiClient.convertToType(data['fk_cur'], 'Number');
            }
            if (data.hasOwnProperty('fk_aoe')) {
                obj['fk_aoe'] = ApiClient.convertToType(data['fk_aoe'], 'Number');
            }
            if (data.hasOwnProperty('fk_ops')) {
                obj['fk_ops'] = ApiClient.convertToType(data['fk_ops'], 'Number');
            }
            if (data.hasOwnProperty('pec_sequence')) {
                obj['pec_sequence'] = ApiClient.convertToType(data['pec_sequence'], 'Number');
            }
            if (data.hasOwnProperty('tsl_label')) {
                obj['tsl_label'] = ApiClient.convertToType(data['tsl_label'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_label_txt')) {
                obj['tsl_label_txt'] = ApiClient.convertToType(data['tsl_label_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Pec2Ops2Aoe2Cur</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Pec2Ops2Aoe2Cur</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Pec2Ops2Aoe2Cur.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_label_txt'] && !(typeof data['tsl_label_txt'] === 'string' || data['tsl_label_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_label_txt` to be a primitive type in the JSON string but got " + data['tsl_label_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }

        return true;
    }


}

Pec2Ops2Aoe2Cur.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying this performance-criterion-to-operational-skill link row.
 * @member {Number} pk
 */
Pec2Ops2Aoe2Cur.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
Pec2Ops2Aoe2Cur.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this link belongs to; cascades on delete.
 * @member {Number} fk_project
 */
Pec2Ops2Aoe2Cur.prototype['fk_project'] = undefined;

/**
 * Reference to the linked operational-skill-to-area-of-operational-expertise row; cascades on delete.
 * @member {Number} fk_ops_2_aoe_2_cur
 */
Pec2Ops2Aoe2Cur.prototype['fk_ops_2_aoe_2_cur'] = undefined;

/**
 * Reference to the linked performance criterion; cascades on delete.
 * @member {Number} fk_pec
 */
Pec2Ops2Aoe2Cur.prototype['fk_pec'] = undefined;

/**
 * Reference to the performance criterion level assigned to this link; set to NULL on delete.
 * @member {Number} fk_pec_level
 */
Pec2Ops2Aoe2Cur.prototype['fk_pec_level'] = undefined;

/**
 * Reference to the learning location assigned to this performance criterion link; set to NULL on delete.
 * @member {Number} fk_pec_learning_location
 */
Pec2Ops2Aoe2Cur.prototype['fk_pec_learning_location'] = undefined;

/**
 * Reference to the linked curriculum title; set to NULL on delete.
 * @member {Number} fk_cur_title
 */
Pec2Ops2Aoe2Cur.prototype['fk_cur_title'] = undefined;

/**
 * Calculated copy of the fk_cur of the linked ops_2_aoe_2_cur row; maintained by a database trigger, values sent by the client are ignored.
 * @member {Number} fk_cur
 */
Pec2Ops2Aoe2Cur.prototype['fk_cur'] = undefined;

/**
 * Calculated copy of the fk_aoe of the linked ops_2_aoe_2_cur row; maintained by a database trigger, values sent by the client are ignored.
 * @member {Number} fk_aoe
 */
Pec2Ops2Aoe2Cur.prototype['fk_aoe'] = undefined;

/**
 * Calculated copy of the fk_ops of the linked ops_2_aoe_2_cur row; maintained by a database trigger, values sent by the client are ignored.
 * @member {Number} fk_ops
 */
Pec2Ops2Aoe2Cur.prototype['fk_ops'] = undefined;

/**
 * Sequence of the performance criteria.
 * @member {Number} pec_sequence
 */
Pec2Ops2Aoe2Cur.prototype['pec_sequence'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this performance-criterion-to-operational-skill link; logical reference, not a database foreign key.
 * @member {Number} tsl_label
 */
Pec2Ops2Aoe2Cur.prototype['tsl_label'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this performance-criterion-to-operational-skill link; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
Pec2Ops2Aoe2Cur.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
Pec2Ops2Aoe2Cur.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
Pec2Ops2Aoe2Cur.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
Pec2Ops2Aoe2Cur.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
Pec2Ops2Aoe2Cur.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
Pec2Ops2Aoe2Cur.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
Pec2Ops2Aoe2Cur.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
Pec2Ops2Aoe2Cur.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
Pec2Ops2Aoe2Cur.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
Pec2Ops2Aoe2Cur.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this performance-criterion-to-operational-skill link; logical reference, not a database foreign key.
 * @member {String} tsl_label_txt
 */
Pec2Ops2Aoe2Cur.prototype['tsl_label_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this performance-criterion-to-operational-skill link; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
Pec2Ops2Aoe2Cur.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
Pec2Ops2Aoe2Cur.prototype['tsl_language'] = undefined;






export { Pec2Ops2Aoe2Cur };

/**
 * The Pec2Ops2Aoe2CurQuery model module.
 * @module model/Pec2Ops2Aoe2CurQuery
 * @version 0.9.0
 */
class Pec2Ops2Aoe2CurQuery {
    /**
     * Constructs a new <code>Pec2Ops2Aoe2CurQuery</code>.
     * Query payload for performance-criterion-to-operational-skill junction rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/Pec2Ops2Aoe2CurQuery
     */
    constructor() { 
        
        Pec2Ops2Aoe2CurQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Pec2Ops2Aoe2CurQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Pec2Ops2Aoe2CurQuery} obj Optional instance to populate.
     * @return {module:model/Pec2Ops2Aoe2CurQuery} The populated <code>Pec2Ops2Aoe2CurQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Pec2Ops2Aoe2CurQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Pec2Ops2Aoe2CurQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Pec2Ops2Aoe2CurQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Pec2Ops2Aoe2CurQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Pec2Ops2Aoe2CurQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Pec2Ops2Aoe2CurQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
Pec2Ops2Aoe2CurQuery.prototype['language'] = undefined;






export { Pec2Ops2Aoe2CurQuery };

/**
 * The Pec2Ops2Aoe2CurView model module.
 * @module model/Pec2Ops2Aoe2CurView
 * @version 0.9.0
 */
class Pec2Ops2Aoe2CurView {
    /**
     * Constructs a new <code>Pec2Ops2Aoe2CurView</code>.
     * Pec2Ops2Aoe2Cur junction row extended with its nested performance criterion, learning location and level rows.
     * @alias module:model/Pec2Ops2Aoe2CurView
     * @param fkProject {Number} Reference to the owning project this link belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        Pec2Ops2Aoe2CurView.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>Pec2Ops2Aoe2CurView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Pec2Ops2Aoe2CurView} obj Optional instance to populate.
     * @return {module:model/Pec2Ops2Aoe2CurView} The populated <code>Pec2Ops2Aoe2CurView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Pec2Ops2Aoe2CurView();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('fk_ops_2_aoe_2_cur')) {
                obj['fk_ops_2_aoe_2_cur'] = ApiClient.convertToType(data['fk_ops_2_aoe_2_cur'], 'Number');
            }
            if (data.hasOwnProperty('fk_pec')) {
                obj['fk_pec'] = ApiClient.convertToType(data['fk_pec'], 'Number');
            }
            if (data.hasOwnProperty('fk_pec_level')) {
                obj['fk_pec_level'] = ApiClient.convertToType(data['fk_pec_level'], 'Number');
            }
            if (data.hasOwnProperty('fk_pec_learning_location')) {
                obj['fk_pec_learning_location'] = ApiClient.convertToType(data['fk_pec_learning_location'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur_title')) {
                obj['fk_cur_title'] = ApiClient.convertToType(data['fk_cur_title'], 'Number');
            }
            if (data.hasOwnProperty('fk_cur')) {
                obj['fk_cur'] = ApiClient.convertToType(data['fk_cur'], 'Number');
            }
            if (data.hasOwnProperty('fk_aoe')) {
                obj['fk_aoe'] = ApiClient.convertToType(data['fk_aoe'], 'Number');
            }
            if (data.hasOwnProperty('fk_ops')) {
                obj['fk_ops'] = ApiClient.convertToType(data['fk_ops'], 'Number');
            }
            if (data.hasOwnProperty('pec_sequence')) {
                obj['pec_sequence'] = ApiClient.convertToType(data['pec_sequence'], 'Number');
            }
            if (data.hasOwnProperty('tsl_label')) {
                obj['tsl_label'] = ApiClient.convertToType(data['tsl_label'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_label_txt')) {
                obj['tsl_label_txt'] = ApiClient.convertToType(data['tsl_label_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
            if (data.hasOwnProperty('performance_criteria')) {
                obj['performance_criteria'] = ApiClient.convertToType(data['performance_criteria'], [PerformanceCriterion]);
            }
            if (data.hasOwnProperty('pec_learning_locations')) {
                obj['pec_learning_locations'] = ApiClient.convertToType(data['pec_learning_locations'], [PecLearningLocation]);
            }
            if (data.hasOwnProperty('pec_levels')) {
                obj['pec_levels'] = ApiClient.convertToType(data['pec_levels'], [PecLevel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Pec2Ops2Aoe2CurView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Pec2Ops2Aoe2CurView</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Pec2Ops2Aoe2CurView.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_label_txt'] && !(typeof data['tsl_label_txt'] === 'string' || data['tsl_label_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_label_txt` to be a primitive type in the JSON string but got " + data['tsl_label_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }
        if (data['performance_criteria']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['performance_criteria'])) {
                throw new Error("Expected the field `performance_criteria` to be an array in the JSON data but got " + data['performance_criteria']);
            }
            // validate the optional field `performance_criteria` (array)
            for (const item of data['performance_criteria']) {
                PerformanceCriterion.validateJSON(item);
            };
        }
        if (data['pec_learning_locations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pec_learning_locations'])) {
                throw new Error("Expected the field `pec_learning_locations` to be an array in the JSON data but got " + data['pec_learning_locations']);
            }
            // validate the optional field `pec_learning_locations` (array)
            for (const item of data['pec_learning_locations']) {
                PecLearningLocation.validateJSON(item);
            };
        }
        if (data['pec_levels']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['pec_levels'])) {
                throw new Error("Expected the field `pec_levels` to be an array in the JSON data but got " + data['pec_levels']);
            }
            // validate the optional field `pec_levels` (array)
            for (const item of data['pec_levels']) {
                PecLevel.validateJSON(item);
            };
        }

        return true;
    }


}

Pec2Ops2Aoe2CurView.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying this performance-criterion-to-operational-skill link row.
 * @member {Number} pk
 */
Pec2Ops2Aoe2CurView.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
Pec2Ops2Aoe2CurView.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this link belongs to; cascades on delete.
 * @member {Number} fk_project
 */
Pec2Ops2Aoe2CurView.prototype['fk_project'] = undefined;

/**
 * Reference to the linked operational-skill-to-area-of-operational-expertise row; cascades on delete.
 * @member {Number} fk_ops_2_aoe_2_cur
 */
Pec2Ops2Aoe2CurView.prototype['fk_ops_2_aoe_2_cur'] = undefined;

/**
 * Reference to the linked performance criterion; cascades on delete.
 * @member {Number} fk_pec
 */
Pec2Ops2Aoe2CurView.prototype['fk_pec'] = undefined;

/**
 * Reference to the performance criterion level assigned to this link; set to NULL on delete.
 * @member {Number} fk_pec_level
 */
Pec2Ops2Aoe2CurView.prototype['fk_pec_level'] = undefined;

/**
 * Reference to the learning location assigned to this performance criterion link; set to NULL on delete.
 * @member {Number} fk_pec_learning_location
 */
Pec2Ops2Aoe2CurView.prototype['fk_pec_learning_location'] = undefined;

/**
 * Reference to the linked curriculum title; set to NULL on delete.
 * @member {Number} fk_cur_title
 */
Pec2Ops2Aoe2CurView.prototype['fk_cur_title'] = undefined;

/**
 * Calculated copy of the fk_cur of the linked ops_2_aoe_2_cur row; maintained by a database trigger, values sent by the client are ignored.
 * @member {Number} fk_cur
 */
Pec2Ops2Aoe2CurView.prototype['fk_cur'] = undefined;

/**
 * Calculated copy of the fk_aoe of the linked ops_2_aoe_2_cur row; maintained by a database trigger, values sent by the client are ignored.
 * @member {Number} fk_aoe
 */
Pec2Ops2Aoe2CurView.prototype['fk_aoe'] = undefined;

/**
 * Calculated copy of the fk_ops of the linked ops_2_aoe_2_cur row; maintained by a database trigger, values sent by the client are ignored.
 * @member {Number} fk_ops
 */
Pec2Ops2Aoe2CurView.prototype['fk_ops'] = undefined;

/**
 * Sequence of the performance criteria.
 * @member {Number} pec_sequence
 */
Pec2Ops2Aoe2CurView.prototype['pec_sequence'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this performance-criterion-to-operational-skill link; logical reference, not a database foreign key.
 * @member {Number} tsl_label
 */
Pec2Ops2Aoe2CurView.prototype['tsl_label'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this performance-criterion-to-operational-skill link; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
Pec2Ops2Aoe2CurView.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
Pec2Ops2Aoe2CurView.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
Pec2Ops2Aoe2CurView.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
Pec2Ops2Aoe2CurView.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
Pec2Ops2Aoe2CurView.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
Pec2Ops2Aoe2CurView.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
Pec2Ops2Aoe2CurView.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
Pec2Ops2Aoe2CurView.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
Pec2Ops2Aoe2CurView.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
Pec2Ops2Aoe2CurView.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual label of this performance-criterion-to-operational-skill link; logical reference, not a database foreign key.
 * @member {String} tsl_label_txt
 */
Pec2Ops2Aoe2CurView.prototype['tsl_label_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of this performance-criterion-to-operational-skill link; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
Pec2Ops2Aoe2CurView.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
Pec2Ops2Aoe2CurView.prototype['tsl_language'] = undefined;

/**
 * Performance criteria rows
 * @member {Array.<module:model/PerformanceCriterion>} performance_criteria
 */
Pec2Ops2Aoe2CurView.prototype['performance_criteria'] = undefined;

/**
 * Performance criterion learning location rows
 * @member {Array.<module:model/PecLearningLocation>} pec_learning_locations
 */
Pec2Ops2Aoe2CurView.prototype['pec_learning_locations'] = undefined;

/**
 * Performance criterion level rows
 * @member {Array.<module:model/PecLevel>} pec_levels
 */
Pec2Ops2Aoe2CurView.prototype['pec_levels'] = undefined;






export { Pec2Ops2Aoe2CurView };

/**
 * The Pec2Ops2Aoe2Curs model module.
 * @module model/Pec2Ops2Aoe2Curs
 * @version 0.9.0
 */
class Pec2Ops2Aoe2Curs {
    /**
     * Constructs a new <code>Pec2Ops2Aoe2Curs</code>.
     * Pec2Ops2Aoe2Cur result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/Pec2Ops2Aoe2Curs
     */
    constructor() { 
        
        Pec2Ops2Aoe2Curs.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Pec2Ops2Aoe2Curs</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Pec2Ops2Aoe2Curs} obj Optional instance to populate.
     * @return {module:model/Pec2Ops2Aoe2Curs} The populated <code>Pec2Ops2Aoe2Curs</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Pec2Ops2Aoe2Curs();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [Pec2Ops2Aoe2Cur]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Pec2Ops2Aoe2Curs</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Pec2Ops2Aoe2Curs</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                Pec2Ops2Aoe2Cur.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Pec2Ops2Aoe2Curs.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Pec2Ops2Aoe2Curs.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Pec2Ops2Aoe2Curs.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
Pec2Ops2Aoe2Curs.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
Pec2Ops2Aoe2Curs.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
Pec2Ops2Aoe2Curs.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
Pec2Ops2Aoe2Curs.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/Pec2Ops2Aoe2Cur>} rows
 */
Pec2Ops2Aoe2Curs.prototype['rows'] = undefined;






export { Pec2Ops2Aoe2Curs };

/**
 * The PecLearningLocation model module.
 * @module model/PecLearningLocation
 * @version 0.9.0
 */
class PecLearningLocation {
    /**
     * Constructs a new <code>PecLearningLocation</code>.
     * Project-specific lookup row defining a performance criterion learning location.
     * @alias module:model/PecLearningLocation
     * @param fkProject {Number} Reference to the owning project this learning location belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        PecLearningLocation.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>PecLearningLocation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PecLearningLocation} obj Optional instance to populate.
     * @return {module:model/PecLearningLocation} The populated <code>PecLearningLocation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PecLearningLocation();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('tsl_label')) {
                obj['tsl_label'] = ApiClient.convertToType(data['tsl_label'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_label_txt')) {
                obj['tsl_label_txt'] = ApiClient.convertToType(data['tsl_label_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PecLearningLocation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PecLearningLocation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PecLearningLocation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['tsl_label_txt'] && !(typeof data['tsl_label_txt'] === 'string' || data['tsl_label_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_label_txt` to be a primitive type in the JSON string but got " + data['tsl_label_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }

        return true;
    }


}

PecLearningLocation.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the performance criterion learning location row.
 * @member {Number} pk
 */
PecLearningLocation.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
PecLearningLocation.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this learning location belongs to; cascades on delete.
 * @member {Number} fk_project
 */
PecLearningLocation.prototype['fk_project'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual learning location label; logical reference, not a database foreign key.
 * @member {Number} tsl_label
 */
PecLearningLocation.prototype['tsl_label'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual learning location description; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
PecLearningLocation.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
PecLearningLocation.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
PecLearningLocation.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
PecLearningLocation.prototype['mcp_data_state'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
PecLearningLocation.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
PecLearningLocation.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
PecLearningLocation.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual learning location label; logical reference, not a database foreign key.
 * @member {String} tsl_label_txt
 */
PecLearningLocation.prototype['tsl_label_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual learning location description; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
PecLearningLocation.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
PecLearningLocation.prototype['tsl_language'] = undefined;






export { PecLearningLocation };

/**
 * The PecLearningLocationQuery model module.
 * @module model/PecLearningLocationQuery
 * @version 0.9.0
 */
class PecLearningLocationQuery {
    /**
     * Constructs a new <code>PecLearningLocationQuery</code>.
     * Query payload for performance criterion learning location rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/PecLearningLocationQuery
     */
    constructor() { 
        
        PecLearningLocationQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>PecLearningLocationQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PecLearningLocationQuery} obj Optional instance to populate.
     * @return {module:model/PecLearningLocationQuery} The populated <code>PecLearningLocationQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PecLearningLocationQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PecLearningLocationQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PecLearningLocationQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
PecLearningLocationQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
PecLearningLocationQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
PecLearningLocationQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
PecLearningLocationQuery.prototype['language'] = undefined;






export { PecLearningLocationQuery };

/**
 * The PecLearningLocations model module.
 * @module model/PecLearningLocations
 * @version 0.9.0
 */
class PecLearningLocations {
    /**
     * Constructs a new <code>PecLearningLocations</code>.
     * PecLearningLocation result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/PecLearningLocations
     */
    constructor() { 
        
        PecLearningLocations.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>PecLearningLocations</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PecLearningLocations} obj Optional instance to populate.
     * @return {module:model/PecLearningLocations} The populated <code>PecLearningLocations</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PecLearningLocations();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [PecLearningLocation]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PecLearningLocations</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PecLearningLocations</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                PecLearningLocation.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
PecLearningLocations.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
PecLearningLocations.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
PecLearningLocations.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
PecLearningLocations.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
PecLearningLocations.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
PecLearningLocations.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
PecLearningLocations.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/PecLearningLocation>} rows
 */
PecLearningLocations.prototype['rows'] = undefined;






export { PecLearningLocations };

/**
 * The PecLevel model module.
 * @module model/PecLevel
 * @version 0.9.0
 */
class PecLevel {
    /**
     * Constructs a new <code>PecLevel</code>.
     * Project-specific lookup row defining a performance criterion level.
     * @alias module:model/PecLevel
     * @param fkProject {Number} Reference to the owning project this performance criterion level belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        PecLevel.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>PecLevel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PecLevel} obj Optional instance to populate.
     * @return {module:model/PecLevel} The populated <code>PecLevel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PecLevel();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('tsl_label')) {
                obj['tsl_label'] = ApiClient.convertToType(data['tsl_label'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_label_txt')) {
                obj['tsl_label_txt'] = ApiClient.convertToType(data['tsl_label_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PecLevel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PecLevel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PecLevel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['tsl_label_txt'] && !(typeof data['tsl_label_txt'] === 'string' || data['tsl_label_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_label_txt` to be a primitive type in the JSON string but got " + data['tsl_label_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }

        return true;
    }


}

PecLevel.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the performance criterion level row.
 * @member {Number} pk
 */
PecLevel.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
PecLevel.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this performance criterion level belongs to; cascades on delete.
 * @member {Number} fk_project
 */
PecLevel.prototype['fk_project'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual performance criterion level label; logical reference, not a database foreign key.
 * @member {Number} tsl_label
 */
PecLevel.prototype['tsl_label'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual performance criterion level description; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
PecLevel.prototype['tsl_description'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
PecLevel.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
PecLevel.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
PecLevel.prototype['mcp_data_state'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
PecLevel.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
PecLevel.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
PecLevel.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual performance criterion level label; logical reference, not a database foreign key.
 * @member {String} tsl_label_txt
 */
PecLevel.prototype['tsl_label_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual performance criterion level description; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
PecLevel.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
PecLevel.prototype['tsl_language'] = undefined;






export { PecLevel };

/**
 * The PecLevelQuery model module.
 * @module model/PecLevelQuery
 * @version 0.9.0
 */
class PecLevelQuery {
    /**
     * Constructs a new <code>PecLevelQuery</code>.
     * Query payload for performance criterion level rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/PecLevelQuery
     */
    constructor() { 
        
        PecLevelQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>PecLevelQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PecLevelQuery} obj Optional instance to populate.
     * @return {module:model/PecLevelQuery} The populated <code>PecLevelQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PecLevelQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PecLevelQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PecLevelQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
PecLevelQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
PecLevelQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
PecLevelQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
PecLevelQuery.prototype['language'] = undefined;






export { PecLevelQuery };

/**
 * The PecLevels model module.
 * @module model/PecLevels
 * @version 0.9.0
 */
class PecLevels {
    /**
     * Constructs a new <code>PecLevels</code>.
     * PecLevel result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/PecLevels
     */
    constructor() { 
        
        PecLevels.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>PecLevels</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PecLevels} obj Optional instance to populate.
     * @return {module:model/PecLevels} The populated <code>PecLevels</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PecLevels();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [PecLevel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PecLevels</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PecLevels</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                PecLevel.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
PecLevels.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
PecLevels.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
PecLevels.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
PecLevels.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
PecLevels.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
PecLevels.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
PecLevels.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/PecLevel>} rows
 */
PecLevels.prototype['rows'] = undefined;






export { PecLevels };

/**
 * The PecQuery model module.
 * @module model/PecQuery
 * @version 0.9.0
 */
class PecQuery {
    /**
     * Constructs a new <code>PecQuery</code>.
     * Query payload for performance criterion rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/PecQuery
     */
    constructor() { 
        
        PecQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>PecQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PecQuery} obj Optional instance to populate.
     * @return {module:model/PecQuery} The populated <code>PecQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PecQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PecQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PecQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
PecQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
PecQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
PecQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
PecQuery.prototype['language'] = undefined;






export { PecQuery };

/**
 * The PerformanceCriteria model module.
 * @module model/PerformanceCriteria
 * @version 0.9.0
 */
class PerformanceCriteria {
    /**
     * Constructs a new <code>PerformanceCriteria</code>.
     * PerformanceCriterion result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/PerformanceCriteria
     */
    constructor() { 
        
        PerformanceCriteria.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>PerformanceCriteria</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PerformanceCriteria} obj Optional instance to populate.
     * @return {module:model/PerformanceCriteria} The populated <code>PerformanceCriteria</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PerformanceCriteria();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [PerformanceCriterion]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PerformanceCriteria</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PerformanceCriteria</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                PerformanceCriterion.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
PerformanceCriteria.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
PerformanceCriteria.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
PerformanceCriteria.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
PerformanceCriteria.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
PerformanceCriteria.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
PerformanceCriteria.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
PerformanceCriteria.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/PerformanceCriterion>} rows
 */
PerformanceCriteria.prototype['rows'] = undefined;






export { PerformanceCriteria };

/**
 * The PerformanceCriterion model module.
 * @module model/PerformanceCriterion
 * @version 0.9.0
 */
class PerformanceCriterion {
    /**
     * Constructs a new <code>PerformanceCriterion</code>.
     * Performance criterion record associated with operational skills.
     * @alias module:model/PerformanceCriterion
     * @param fkProject {Number} Reference to the owning project this performance criterion belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        PerformanceCriterion.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>PerformanceCriterion</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PerformanceCriterion} obj Optional instance to populate.
     * @return {module:model/PerformanceCriterion} The populated <code>PerformanceCriterion</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PerformanceCriterion();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('tsl_title')) {
                obj['tsl_title'] = ApiClient.convertToType(data['tsl_title'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_title_txt')) {
                obj['tsl_title_txt'] = ApiClient.convertToType(data['tsl_title_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PerformanceCriterion</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PerformanceCriterion</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PerformanceCriterion.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_title_txt'] && !(typeof data['tsl_title_txt'] === 'string' || data['tsl_title_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_title_txt` to be a primitive type in the JSON string but got " + data['tsl_title_txt']);
        }

        return true;
    }


}

PerformanceCriterion.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the performance criterion row.
 * @member {Number} pk
 */
PerformanceCriterion.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
PerformanceCriterion.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this performance criterion belongs to; cascades on delete.
 * @member {Number} fk_project
 */
PerformanceCriterion.prototype['fk_project'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual title of the performance criterion; logical reference, not a database foreign key.
 * @member {Number} tsl_title
 */
PerformanceCriterion.prototype['tsl_title'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
PerformanceCriterion.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
PerformanceCriterion.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
PerformanceCriterion.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
PerformanceCriterion.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
PerformanceCriterion.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
PerformanceCriterion.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
PerformanceCriterion.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
PerformanceCriterion.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
PerformanceCriterion.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual title of the performance criterion; logical reference, not a database foreign key.
 * @member {String} tsl_title_txt
 */
PerformanceCriterion.prototype['tsl_title_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
PerformanceCriterion.prototype['tsl_language'] = undefined;






export { PerformanceCriterion };

/**
 * The Prj2UsrQuerySkills model module.
 * @module model/Prj2UsrQuerySkills
 * @version 0.9.0
 */
class Prj2UsrQuerySkills {
    /**
     * Constructs a new <code>Prj2UsrQuerySkills</code>.
     * Query payload for project-to-user junction rows: SQL WHERE clause plus limit/offset pagination.
     * @alias module:model/Prj2UsrQuerySkills
     */
    constructor() { 
        
        Prj2UsrQuerySkills.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Prj2UsrQuerySkills</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Prj2UsrQuerySkills} obj Optional instance to populate.
     * @return {module:model/Prj2UsrQuerySkills} The populated <code>Prj2UsrQuerySkills</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Prj2UsrQuerySkills();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Prj2UsrQuerySkills</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Prj2UsrQuerySkills</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Prj2UsrQuerySkills.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Prj2UsrQuerySkills.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Prj2UsrQuerySkills.prototype['offset'] = undefined;






export { Prj2UsrQuerySkills };

/**
 * The Prj2UsrSkills model module.
 * @module model/Prj2UsrSkills
 * @version 0.9.0
 */
class Prj2UsrSkills {
    /**
     * Constructs a new <code>Prj2UsrSkills</code>.
     * Link table assigning users to skills projects.
     * @alias module:model/Prj2UsrSkills
     */
    constructor() { 
        
        Prj2UsrSkills.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Prj2UsrSkills</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Prj2UsrSkills} obj Optional instance to populate.
     * @return {module:model/Prj2UsrSkills} The populated <code>Prj2UsrSkills</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Prj2UsrSkills();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('fk_user')) {
                obj['fk_user'] = ApiClient.convertToType(data['fk_user'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Prj2UsrSkills</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Prj2UsrSkills</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying this project-to-user link row.
 * @member {Number} pk
 */
Prj2UsrSkills.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
Prj2UsrSkills.prototype['uuid'] = undefined;

/**
 * Reference to the linked project; cascades on delete.
 * @member {Number} fk_project
 */
Prj2UsrSkills.prototype['fk_project'] = undefined;

/**
 * Reference to the linked user (logical reference into the `system` schema table `usr_users`, not a database foreign key).
 * @member {Number} fk_user
 */
Prj2UsrSkills.prototype['fk_user'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
Prj2UsrSkills.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
Prj2UsrSkills.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
Prj2UsrSkills.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
Prj2UsrSkills.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
Prj2UsrSkills.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
Prj2UsrSkills.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
Prj2UsrSkills.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
Prj2UsrSkills.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
Prj2UsrSkills.prototype['migration_data'] = undefined;






export { Prj2UsrSkills };

/**
 * The Prj2UsrsSkills model module.
 * @module model/Prj2UsrsSkills
 * @version 0.9.0
 */
class Prj2UsrsSkills {
    /**
     * Constructs a new <code>Prj2UsrsSkills</code>.
     * Prj2UsrSkills result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/Prj2UsrsSkills
     */
    constructor() { 
        
        Prj2UsrsSkills.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Prj2UsrsSkills</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Prj2UsrsSkills} obj Optional instance to populate.
     * @return {module:model/Prj2UsrsSkills} The populated <code>Prj2UsrsSkills</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Prj2UsrsSkills();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [Prj2UsrSkills]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Prj2UsrsSkills</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Prj2UsrsSkills</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                Prj2UsrSkills.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Prj2UsrsSkills.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Prj2UsrsSkills.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Prj2UsrsSkills.prototype['offset'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
Prj2UsrsSkills.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
Prj2UsrsSkills.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
Prj2UsrsSkills.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/Prj2UsrSkills>} rows
 */
Prj2UsrsSkills.prototype['rows'] = undefined;






export { Prj2UsrsSkills };

/**
 * The ProjectQuerySkills model module.
 * @module model/ProjectQuerySkills
 * @version 0.9.0
 */
class ProjectQuerySkills {
    /**
     * Constructs a new <code>ProjectQuerySkills</code>.
     * Query payload for project rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/ProjectQuerySkills
     */
    constructor() { 
        
        ProjectQuerySkills.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>ProjectQuerySkills</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProjectQuerySkills} obj Optional instance to populate.
     * @return {module:model/ProjectQuerySkills} The populated <code>ProjectQuerySkills</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProjectQuerySkills();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProjectQuerySkills</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProjectQuerySkills</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
ProjectQuerySkills.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
ProjectQuerySkills.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
ProjectQuerySkills.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
ProjectQuerySkills.prototype['language'] = undefined;






export { ProjectQuerySkills };

/**
 * The ProjectSkills model module.
 * @module model/ProjectSkills
 * @version 0.9.0
 */
class ProjectSkills {
    /**
     * Constructs a new <code>ProjectSkills</code>.
     * Skills project containing curricula, operational expertise, skills, and criteria.
     * @alias module:model/ProjectSkills
     */
    constructor() { 
        
        ProjectSkills.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ProjectSkills</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProjectSkills} obj Optional instance to populate.
     * @return {module:model/ProjectSkills} The populated <code>ProjectSkills</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProjectSkills();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('tsl_name')) {
                obj['tsl_name'] = ApiClient.convertToType(data['tsl_name'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_name_txt')) {
                obj['tsl_name_txt'] = ApiClient.convertToType(data['tsl_name_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProjectSkills</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProjectSkills</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_name_txt'] && !(typeof data['tsl_name_txt'] === 'string' || data['tsl_name_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_name_txt` to be a primitive type in the JSON string but got " + data['tsl_name_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying the project row.
 * @member {Number} pk
 */
ProjectSkills.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
ProjectSkills.prototype['uuid'] = undefined;

/**
 * Human-readable unique business identifier of the project (e.g. \"PRJ-0001\").
 * @member {String} id
 */
ProjectSkills.prototype['id'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual name of the project; logical reference, not a database foreign key.
 * @member {Number} tsl_name
 */
ProjectSkills.prototype['tsl_name'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of the project; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
ProjectSkills.prototype['tsl_description'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
ProjectSkills.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
ProjectSkills.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
ProjectSkills.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
ProjectSkills.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
ProjectSkills.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
ProjectSkills.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
ProjectSkills.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
ProjectSkills.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual name of the project; logical reference, not a database foreign key.
 * @member {String} tsl_name_txt
 */
ProjectSkills.prototype['tsl_name_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of the project; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
ProjectSkills.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
ProjectSkills.prototype['tsl_language'] = undefined;






export { ProjectSkills };

/**
 * The ProjectView model module.
 * @module model/ProjectView
 * @version 0.9.0
 */
class ProjectView {
    /**
     * Constructs a new <code>ProjectView</code>.
     * Project row extended with its nested curriculum rows.
     * @alias module:model/ProjectView
     */
    constructor() { 
        
        ProjectView.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ProjectView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProjectView} obj Optional instance to populate.
     * @return {module:model/ProjectView} The populated <code>ProjectView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProjectView();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('tsl_name')) {
                obj['tsl_name'] = ApiClient.convertToType(data['tsl_name'], 'Number');
            }
            if (data.hasOwnProperty('tsl_description')) {
                obj['tsl_description'] = ApiClient.convertToType(data['tsl_description'], 'Number');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('migration_data')) {
                obj['migration_data'] = ApiClient.convertToType(data['migration_data'], {'String': Object});
            }
            if (data.hasOwnProperty('tsl_name_txt')) {
                obj['tsl_name_txt'] = ApiClient.convertToType(data['tsl_name_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_description_txt')) {
                obj['tsl_description_txt'] = ApiClient.convertToType(data['tsl_description_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
            if (data.hasOwnProperty('curricula')) {
                obj['curricula'] = ApiClient.convertToType(data['curricula'], [CurriculumView]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProjectView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProjectView</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_name_txt'] && !(typeof data['tsl_name_txt'] === 'string' || data['tsl_name_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_name_txt` to be a primitive type in the JSON string but got " + data['tsl_name_txt']);
        }
        // ensure the json data is a string
        if (data['tsl_description_txt'] && !(typeof data['tsl_description_txt'] === 'string' || data['tsl_description_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_description_txt` to be a primitive type in the JSON string but got " + data['tsl_description_txt']);
        }
        if (data['curricula']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['curricula'])) {
                throw new Error("Expected the field `curricula` to be an array in the JSON data but got " + data['curricula']);
            }
            // validate the optional field `curricula` (array)
            for (const item of data['curricula']) {
                CurriculumView.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying the project row.
 * @member {Number} pk
 */
ProjectView.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
ProjectView.prototype['uuid'] = undefined;

/**
 * Human-readable unique business identifier of the project (e.g. \"PRJ-0001\").
 * @member {String} id
 */
ProjectView.prototype['id'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual name of the project; logical reference, not a database foreign key.
 * @member {Number} tsl_name
 */
ProjectView.prototype['tsl_name'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of the project; logical reference, not a database foreign key.
 * @member {Number} tsl_description
 */
ProjectView.prototype['tsl_description'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
ProjectView.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
ProjectView.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
ProjectView.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
ProjectView.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
ProjectView.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
ProjectView.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
ProjectView.prototype['updated_at'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} migration_data
 */
ProjectView.prototype['migration_data'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual name of the project; logical reference, not a database foreign key.
 * @member {String} tsl_name_txt
 */
ProjectView.prototype['tsl_name_txt'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual description of the project; logical reference, not a database foreign key.
 * @member {String} tsl_description_txt
 */
ProjectView.prototype['tsl_description_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
ProjectView.prototype['tsl_language'] = undefined;

/**
 * Curriculum rows
 * @member {Array.<module:model/CurriculumView>} curricula
 */
ProjectView.prototype['curricula'] = undefined;






export { ProjectView };

/**
 * The ProjectsSkills model module.
 * @module model/ProjectsSkills
 * @version 0.9.0
 */
class ProjectsSkills {
    /**
     * Constructs a new <code>ProjectsSkills</code>.
     * ProjectSkills result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/ProjectsSkills
     */
    constructor() { 
        
        ProjectsSkills.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>ProjectsSkills</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProjectsSkills} obj Optional instance to populate.
     * @return {module:model/ProjectsSkills} The populated <code>ProjectsSkills</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProjectsSkills();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [ProjectSkills]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProjectsSkills</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProjectsSkills</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                ProjectSkills.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
ProjectsSkills.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
ProjectsSkills.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
ProjectsSkills.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
ProjectsSkills.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
ProjectsSkills.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
ProjectsSkills.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
ProjectsSkills.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/ProjectSkills>} rows
 */
ProjectsSkills.prototype['rows'] = undefined;






export { ProjectsSkills };

/**
* Enum class Scope.
* @enum {}
* @readonly
*/
export class Scope {
    
        /**
         * value: "pending"
         * @const
         */
        "pending" = "pending";

    
        /**
         * value: "public"
         * @const
         */
        "public" = "public";

    
        /**
         * value: "read"
         * @const
         */
        "read" = "read";

    
        /**
         * value: "write"
         * @const
         */
        "write" = "write";

    
        /**
         * value: "project"
         * @const
         */
        "project" = "project";

    
        /**
         * value: "admin"
         * @const
         */
        "admin" = "admin";

    
        /**
         * value: "dev"
         * @const
         */
        "dev" = "dev";

    

    /**
    * Returns a <code>Scope</code> enum value from a Javascript object name.
    * @param {Object} data The plain JavaScript object containing the name of the enum value.
    * @return {module:model/Scope} The enum <code>Scope</code> value.
    */
    static constructFromObject(object) {
        return object;
    }
}

/**
 * The SemanticSearch model module.
 * @module model/SemanticSearch
 * @version 0.9.0
 */
class SemanticSearch {
    /**
     * Constructs a new <code>SemanticSearch</code>.
     * Semantic search result set: echoes the query it was produced from and carries the matching rows, how many came back, how many matched in total, and optionally the query vector.
     * @alias module:model/SemanticSearch
     * @param query {String} Text to search for. It is embedded with the same model and dimension as the stored vectors, and the hits are the rows closest to it. Must not be blank.
     */
    constructor(query) { 
        
        SemanticSearch.initialize(this, query);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, query) { 
        obj['query'] = query;
        obj['where'] = 'tsl_translations.pk > 0';
        obj['limit'] = 25;
        obj['rerank_result'] = true;
        obj['return_vectors'] = false;
    }

    /**
     * Constructs a <code>SemanticSearch</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SemanticSearch} obj Optional instance to populate.
     * @return {module:model/SemanticSearch} The populated <code>SemanticSearch</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SemanticSearch();

            if (data.hasOwnProperty('query')) {
                obj['query'] = ApiClient.convertToType(data['query'], 'String');
            }
            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('rerank_result')) {
                obj['rerank_result'] = ApiClient.convertToType(data['rerank_result'], 'Boolean');
            }
            if (data.hasOwnProperty('return_vectors')) {
                obj['return_vectors'] = ApiClient.convertToType(data['return_vectors'], 'Boolean');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('query_vector')) {
                obj['query_vector'] = ApiClient.convertToType(data['query_vector'], ['Number']);
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [AppApiSkillsCommandsSemanticSearchModelsDocument]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SemanticSearch</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SemanticSearch</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SemanticSearch.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['query'] && !(typeof data['query'] === 'string' || data['query'] instanceof String)) {
            throw new Error("Expected the field `query` to be a primitive type in the JSON string but got " + data['query']);
        }
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['query_vector'])) {
            throw new Error("Expected the field `query_vector` to be an array in the JSON data but got " + data['query_vector']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                AppApiSkillsCommandsSemanticSearchModelsDocument.validateJSON(item);
            };
        }

        return true;
    }


}

SemanticSearch.RequiredProperties = ["query"];

/**
 * Text to search for. It is embedded with the same model and dimension as the stored vectors, and the hits are the rows closest to it. Must not be blank.
 * @member {String} query
 */
SemanticSearch.prototype['query'] = undefined;

/**
 * SQL WHERE clause, applied before the full text search runs, so it decides which rows are candidates at all. It can name columns of the language-specific search text table (tsl_search_texts_<language>) and of the joined tsl_translations row, with or without the table name in front. A bare `pk`, `created_at` or `updated_at` is the search text table's; the tsl_translations ones are `tsl_pk`, `tsl_created_at` and `tsl_updated_at`, or spell them out as `tsl_translations.pk` and so on. Every other tsl_translations column keeps its own name either way. || Three of them are what scopes a search, because together they say where each translated text came from. `tsl_translations.fk_table` is the name of the source table whose attribute is translated -- a logical reference, not a database foreign key -- so `tsl_translations.fk_table = 'cur_curricula'` searches the curricula texts only, and `tsl_translations.fk_table IN ('prj_projects', 'cur_curricula')` or `tsl_translations.fk_table LIKE 'ops%'` widen that to several. || `tsl_translations.fk_table_pk` is the primary key (pk) of the row in that source table, so `tsl_translations.fk_table = 'cur_curricula' AND tsl_translations.fk_table_pk = 5` narrows the search to the texts of one single curriculum. It is only meaningful together with `fk_table`: every source table numbers its rows from 1, so `fk_table_pk = 5` on its own matches one unrelated row per table. || `tsl_translations.fk_attribute` is the name of the attribute (column) in the source table whose text is translated, so `tsl_translations.fk_attribute = 'tsl_title'` searches titles and ignores descriptions. || The three combine freely, e.g. `tsl_translations.fk_table = 'ops_operational_skills' AND tsl_translations.fk_attribute = 'tsl_description'`.
 * @member {String} where
 * @default 'tsl_translations.pk > 0'
 */
SemanticSearch.prototype['where'] = 'tsl_translations.pk > 0';

/**
 * Maximum number of rows to return. Must be 1 or greater.
 * @member {Number} limit
 * @default 25
 */
SemanticSearch.prototype['limit'] = 25;

/**
 * Number of rows to skip off the top of the ranking before `limit` is taken. Note that the ranking is recomputed per request from a freshly embedded query, and two embeddings of the same text are not bit-identical, so rows with near-equal scores can change places between pages.
 * @member {Number} offset
 */
SemanticSearch.prototype['offset'] = undefined;

/**
 * Whether to reorder the hits with the rerank model. The vector search selects the rows first (so `limit` still decides how many are considered), then the rerank model scores the query against each row text and the rows come back in that order, each carrying its `rerank_similarity`. Costs one extra API call.
 * @member {Boolean} rerank_result
 * @default true
 */
SemanticSearch.prototype['rerank_result'] = true;

/**
 * Whether to include the embedding vector of each hit plus the query vector in the response; the vectors are large, so they are left out unless this is set.
 * @member {Boolean} return_vectors
 * @default false
 */
SemanticSearch.prototype['return_vectors'] = false;

/**
 * Language used for translated fields. It also selects which vector table is searched.
 * @member {module:model/TslLanguageType} language
 */
SemanticSearch.prototype['language'] = undefined;

/**
 * Number of rows in the vector table matching `where`, before `limit` is applied: the size of the candidate set the search ran over, not the number of rows returned. Reranking does not change it.
 * @member {Number} total
 */
SemanticSearch.prototype['total'] = undefined;

/**
 * Number of rows actually returned in `rows`, never more than `limit`.
 * @member {Number} count
 */
SemanticSearch.prototype['count'] = undefined;

/**
 * Number of rows the language vector table offers to search at all, ignoring `where`: the pool `total` was narrowed down from. Rows without a stored vector are left out here too, since the search could never return them, so `all` equals `total` when `where` matches everything.
 * @member {Number} all
 */
SemanticSearch.prototype['all'] = undefined;

/**
 * Embedding vector of the query text, 2048 dimensions -- the probe each row vector was compared against. Only present when `return_vectors` was requested.
 * @member {Array.<Number>} query_vector
 */
SemanticSearch.prototype['query_vector'] = undefined;

/**
 * The matching rows, ordered by descending `similarity` -- or by descending `rerank_similarity` when `rerank_result` was requested. None when nothing matched.
 * @member {Array.<module:model/AppApiSkillsCommandsSemanticSearchModelsDocument>} rows
 */
SemanticSearch.prototype['rows'] = undefined;






export { SemanticSearch };

/**
 * The SemanticSearchQuery model module.
 * @module model/SemanticSearchQuery
 * @version 0.9.0
 */
class SemanticSearchQuery {
    /**
     * Constructs a new <code>SemanticSearchQuery</code>.
     * Query payload for a semantic search over the translation vectors: the text to search for, a SQL WHERE clause narrowing which rows are searched, a row limit, the language whose vector table is used, and the switches for reranking and for returning the vectors.
     * @alias module:model/SemanticSearchQuery
     * @param query {String} Text to search for. It is embedded with the same model and dimension as the stored vectors, and the hits are the rows closest to it. Must not be blank.
     */
    constructor(query) { 
        
        SemanticSearchQuery.initialize(this, query);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, query) { 
        obj['query'] = query;
        obj['where'] = 'tsl_translations.pk > 0';
        obj['limit'] = 25;
        obj['rerank_result'] = true;
        obj['return_vectors'] = false;
    }

    /**
     * Constructs a <code>SemanticSearchQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SemanticSearchQuery} obj Optional instance to populate.
     * @return {module:model/SemanticSearchQuery} The populated <code>SemanticSearchQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SemanticSearchQuery();

            if (data.hasOwnProperty('query')) {
                obj['query'] = ApiClient.convertToType(data['query'], 'String');
            }
            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('rerank_result')) {
                obj['rerank_result'] = ApiClient.convertToType(data['rerank_result'], 'Boolean');
            }
            if (data.hasOwnProperty('return_vectors')) {
                obj['return_vectors'] = ApiClient.convertToType(data['return_vectors'], 'Boolean');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SemanticSearchQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SemanticSearchQuery</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SemanticSearchQuery.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['query'] && !(typeof data['query'] === 'string' || data['query'] instanceof String)) {
            throw new Error("Expected the field `query` to be a primitive type in the JSON string but got " + data['query']);
        }
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}

SemanticSearchQuery.RequiredProperties = ["query"];

/**
 * Text to search for. It is embedded with the same model and dimension as the stored vectors, and the hits are the rows closest to it. Must not be blank.
 * @member {String} query
 */
SemanticSearchQuery.prototype['query'] = undefined;

/**
 * SQL WHERE clause, applied before the full text search runs, so it decides which rows are candidates at all. It can name columns of the language-specific search text table (tsl_search_texts_<language>) and of the joined tsl_translations row, with or without the table name in front. A bare `pk`, `created_at` or `updated_at` is the search text table's; the tsl_translations ones are `tsl_pk`, `tsl_created_at` and `tsl_updated_at`, or spell them out as `tsl_translations.pk` and so on. Every other tsl_translations column keeps its own name either way. || Three of them are what scopes a search, because together they say where each translated text came from. `tsl_translations.fk_table` is the name of the source table whose attribute is translated -- a logical reference, not a database foreign key -- so `tsl_translations.fk_table = 'cur_curricula'` searches the curricula texts only, and `tsl_translations.fk_table IN ('prj_projects', 'cur_curricula')` or `tsl_translations.fk_table LIKE 'ops%'` widen that to several. || `tsl_translations.fk_table_pk` is the primary key (pk) of the row in that source table, so `tsl_translations.fk_table = 'cur_curricula' AND tsl_translations.fk_table_pk = 5` narrows the search to the texts of one single curriculum. It is only meaningful together with `fk_table`: every source table numbers its rows from 1, so `fk_table_pk = 5` on its own matches one unrelated row per table. || `tsl_translations.fk_attribute` is the name of the attribute (column) in the source table whose text is translated, so `tsl_translations.fk_attribute = 'tsl_title'` searches titles and ignores descriptions. || The three combine freely, e.g. `tsl_translations.fk_table = 'ops_operational_skills' AND tsl_translations.fk_attribute = 'tsl_description'`.
 * @member {String} where
 * @default 'tsl_translations.pk > 0'
 */
SemanticSearchQuery.prototype['where'] = 'tsl_translations.pk > 0';

/**
 * Maximum number of rows to return. Must be 1 or greater.
 * @member {Number} limit
 * @default 25
 */
SemanticSearchQuery.prototype['limit'] = 25;

/**
 * Number of rows to skip off the top of the ranking before `limit` is taken. Note that the ranking is recomputed per request from a freshly embedded query, and two embeddings of the same text are not bit-identical, so rows with near-equal scores can change places between pages.
 * @member {Number} offset
 */
SemanticSearchQuery.prototype['offset'] = undefined;

/**
 * Whether to reorder the hits with the rerank model. The vector search selects the rows first (so `limit` still decides how many are considered), then the rerank model scores the query against each row text and the rows come back in that order, each carrying its `rerank_similarity`. Costs one extra API call.
 * @member {Boolean} rerank_result
 * @default true
 */
SemanticSearchQuery.prototype['rerank_result'] = true;

/**
 * Whether to include the embedding vector of each hit plus the query vector in the response; the vectors are large, so they are left out unless this is set.
 * @member {Boolean} return_vectors
 * @default false
 */
SemanticSearchQuery.prototype['return_vectors'] = false;

/**
 * Language used for translated fields. It also selects which vector table is searched.
 * @member {module:model/TslLanguageType} language
 */
SemanticSearchQuery.prototype['language'] = undefined;






export { SemanticSearchQuery };

/**
 * The SignUp model module.
 * @module model/SignUp
 * @version 0.9.0
 */
class SignUp {
    /**
     * Constructs a new <code>SignUp</code>.
     * Request body of /auth/sign-up: who is registering. Deliberately without a password field - the account is given a generated one and told to replace it, so no password travels in on this route and the policy cannot be sidestepped at the one moment an account comes into existence.
     * @alias module:model/SignUp
     * @param firstName {String} Given name of the person registering, written to `usr_users.first_name` and used to address them in the mail carrying their password.
     * @param surname {String} Family name of the person registering, written to `usr_users.surname`.
     * @param username {String} Login name to register, unique across all users. A name already in use is refused with 409 rather than silently ignored, so the caller can offer another one.
     * @param email {String} Address the generated password is sent to, and unique across all users - an address already registered is refused with 409. It is the only channel the password is ever delivered on, so an address that cannot receive mail means no usable account.
     */
    constructor(firstName, surname, username, email) { 
        
        SignUp.initialize(this, firstName, surname, username, email);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, firstName, surname, username, email) { 
        obj['first_name'] = firstName;
        obj['surname'] = surname;
        obj['username'] = username;
        obj['email'] = email;
    }

    /**
     * Constructs a <code>SignUp</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SignUp} obj Optional instance to populate.
     * @return {module:model/SignUp} The populated <code>SignUp</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SignUp();

            if (data.hasOwnProperty('first_name')) {
                obj['first_name'] = ApiClient.convertToType(data['first_name'], 'String');
            }
            if (data.hasOwnProperty('surname')) {
                obj['surname'] = ApiClient.convertToType(data['surname'], 'String');
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SignUp</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SignUp</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SignUp.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['first_name'] && !(typeof data['first_name'] === 'string' || data['first_name'] instanceof String)) {
            throw new Error("Expected the field `first_name` to be a primitive type in the JSON string but got " + data['first_name']);
        }
        // ensure the json data is a string
        if (data['surname'] && !(typeof data['surname'] === 'string' || data['surname'] instanceof String)) {
            throw new Error("Expected the field `surname` to be a primitive type in the JSON string but got " + data['surname']);
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }

        return true;
    }


}

SignUp.RequiredProperties = ["first_name", "surname", "username", "email"];

/**
 * Given name of the person registering, written to `usr_users.first_name` and used to address them in the mail carrying their password.
 * @member {String} first_name
 */
SignUp.prototype['first_name'] = undefined;

/**
 * Family name of the person registering, written to `usr_users.surname`.
 * @member {String} surname
 */
SignUp.prototype['surname'] = undefined;

/**
 * Login name to register, unique across all users. A name already in use is refused with 409 rather than silently ignored, so the caller can offer another one.
 * @member {String} username
 */
SignUp.prototype['username'] = undefined;

/**
 * Address the generated password is sent to, and unique across all users - an address already registered is refused with 409. It is the only channel the password is ever delivered on, so an address that cannot receive mail means no usable account.
 * @member {String} email
 */
SignUp.prototype['email'] = undefined;






export { SignUp };

/**
 * The SignUpResult model module.
 * @module model/SignUpResult
 * @version 0.9.0
 */
class SignUpResult {
    /**
     * Constructs a new <code>SignUpResult</code>.
     * Response body of /auth/sign-up. Reached only when the row was committed and the mail accepted for delivery; every other outcome is an HTTP error, never &#x60;created: false&#x60;. The generated password is deliberately absent - it exists in the mail alone.
     * @alias module:model/SignUpResult
     * @param created {Boolean} Always true. Present so the response is a self-describing JSON object rather than an empty body.
     */
    constructor(created) { 
        
        SignUpResult.initialize(this, created);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, created) { 
        obj['created'] = created;
    }

    /**
     * Constructs a <code>SignUpResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SignUpResult} obj Optional instance to populate.
     * @return {module:model/SignUpResult} The populated <code>SignUpResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SignUpResult();

            if (data.hasOwnProperty('created')) {
                obj['created'] = ApiClient.convertToType(data['created'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SignUpResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SignUpResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SignUpResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

SignUpResult.RequiredProperties = ["created"];

/**
 * Always true. Present so the response is a self-describing JSON object rather than an empty body.
 * @member {Boolean} created
 */
SignUpResult.prototype['created'] = undefined;






export { SignUpResult };

/**
 * The SkillsEnums model module.
 * @module model/SkillsEnums
 * @version 0.9.0
 */
class SkillsEnums {
    /**
     * Constructs a new <code>SkillsEnums</code>.
     * Translation catalogue for the skills-domain enums: one EnumTranslation list per enum type.
     * @alias module:model/SkillsEnums
     */
    constructor() { 
        
        SkillsEnums.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SkillsEnums</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SkillsEnums} obj Optional instance to populate.
     * @return {module:model/SkillsEnums} The populated <code>SkillsEnums</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SkillsEnums();

            if (data.hasOwnProperty('tsl_state_type')) {
                obj['tsl_state_type'] = ApiClient.convertToType(data['tsl_state_type'], [EnumTranslation]);
            }
            if (data.hasOwnProperty('tsl_master_language_type')) {
                obj['tsl_master_language_type'] = ApiClient.convertToType(data['tsl_master_language_type'], [EnumTranslation]);
            }
            if (data.hasOwnProperty('tsl_language_type')) {
                obj['tsl_language_type'] = ApiClient.convertToType(data['tsl_language_type'], [EnumTranslation]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SkillsEnums</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SkillsEnums</code>.
     */
    static validateJSON(data) {
        if (data['tsl_state_type']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tsl_state_type'])) {
                throw new Error("Expected the field `tsl_state_type` to be an array in the JSON data but got " + data['tsl_state_type']);
            }
            // validate the optional field `tsl_state_type` (array)
            for (const item of data['tsl_state_type']) {
                EnumTranslation.validateJSON(item);
            };
        }
        if (data['tsl_master_language_type']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tsl_master_language_type'])) {
                throw new Error("Expected the field `tsl_master_language_type` to be an array in the JSON data but got " + data['tsl_master_language_type']);
            }
            // validate the optional field `tsl_master_language_type` (array)
            for (const item of data['tsl_master_language_type']) {
                EnumTranslation.validateJSON(item);
            };
        }
        if (data['tsl_language_type']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tsl_language_type'])) {
                throw new Error("Expected the field `tsl_language_type` to be an array in the JSON data but got " + data['tsl_language_type']);
            }
            // validate the optional field `tsl_language_type` (array)
            for (const item of data['tsl_language_type']) {
                EnumTranslation.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Translation state enum translations
 * @member {Array.<module:model/EnumTranslation>} tsl_state_type
 */
SkillsEnums.prototype['tsl_state_type'] = undefined;

/**
 * Translation master language enum translations
 * @member {Array.<module:model/EnumTranslation>} tsl_master_language_type
 */
SkillsEnums.prototype['tsl_master_language_type'] = undefined;

/**
 * Translation language enum translations
 * @member {Array.<module:model/EnumTranslation>} tsl_language_type
 */
SkillsEnums.prototype['tsl_language_type'] = undefined;






export { SkillsEnums };

/**
 * The SkillsView model module.
 * @module model/SkillsView
 * @version 0.9.0
 */
class SkillsView {
    /**
     * Constructs a new <code>SkillsView</code>.
     * Skills view result set: echoes the query and carries total/count plus the matching view rows and the resolved translation language.
     * @alias module:model/SkillsView
     */
    constructor() { 
        
        SkillsView.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'prj_projects.pk > 0';
    }

    /**
     * Constructs a <code>SkillsView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SkillsView} obj Optional instance to populate.
     * @return {module:model/SkillsView} The populated <code>SkillsView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SkillsView();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [ProjectView]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SkillsView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SkillsView</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                ProjectView.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause selecting which branches of the hierarchy are returned. Any table of the hierarchy may be filtered on, not only prj_projects: cur_curricula, cur_type_degree, aoe_2_cur, aoe_area_of_operational_expertises, ops_2_aoe_2_cur, ops_operational_skills, ops_uses, ops_levels, pec_2_ops_2_aoe_2_cur, pec_performance_criteria, pec_learning_locations and pec_levels. Every column must be qualified with its table name - an unqualified name is ambiguous across the hierarchy and is rejected. Conditions combine with AND, OR and NOT, may be grouped with parentheses, and use the usual SQL operators. A condition on a deeper table prunes the whole tree to the branches satisfying it: filtering on a curriculum returns only the projects holding it, and only that curriculum inside them. Two limits apply. The translated *_txt fields are resolved from tsl_translations at read time and are not columns, so filter the stored tsl_* key instead. And statement keywords (SELECT, DELETE, CREATE, ...) are stripped from the clause before it is used, including inside quoted literals, so a string value containing one of them is silently altered. Examples: - `prj_projects.pk = 1` - one project with everything below it - `prj_projects.pk = 1 AND cur_curricula.pk = 32` - a single curriculum of that project - `prj_projects.pk = 1 AND (cur_curricula.pk = 30 OR cur_curricula.pk = 32)` - two of them - `cur_curricula.pk IN (30, 32)` - the same, with the project derived from the curricula - `aoe_area_of_operational_expertises.pk = 342` - every branch leading to that area - `cur_curricula.pk = 32 AND ops_operational_skills.tsl_title IS NOT NULL` - levels combined - `prj_projects.pk = 1 AND cur_curricula.created_at > '2020-01-01'` - non-key columns
 * @member {String} where
 * @default 'prj_projects.pk > 0'
 */
SkillsView.prototype['where'] = 'prj_projects.pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
SkillsView.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
SkillsView.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
SkillsView.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
SkillsView.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
SkillsView.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
SkillsView.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/ProjectView>} rows
 */
SkillsView.prototype['rows'] = undefined;






export { SkillsView };

/**
 * The SkillsViewQuery model module.
 * @module model/SkillsViewQuery
 * @version 0.9.0
 */
class SkillsViewQuery {
    /**
     * Constructs a new <code>SkillsViewQuery</code>.
     * Query payload for the skills view: SQL WHERE clause, limit/offset pagination, and translation language.
     * @alias module:model/SkillsViewQuery
     */
    constructor() { 
        
        SkillsViewQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'prj_projects.pk > 0';
    }

    /**
     * Constructs a <code>SkillsViewQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SkillsViewQuery} obj Optional instance to populate.
     * @return {module:model/SkillsViewQuery} The populated <code>SkillsViewQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SkillsViewQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SkillsViewQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SkillsViewQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause selecting which branches of the hierarchy are returned. Any table of the hierarchy may be filtered on, not only prj_projects: cur_curricula, cur_type_degree, aoe_2_cur, aoe_area_of_operational_expertises, ops_2_aoe_2_cur, ops_operational_skills, ops_uses, ops_levels, pec_2_ops_2_aoe_2_cur, pec_performance_criteria, pec_learning_locations and pec_levels. Every column must be qualified with its table name - an unqualified name is ambiguous across the hierarchy and is rejected. Conditions combine with AND, OR and NOT, may be grouped with parentheses, and use the usual SQL operators. A condition on a deeper table prunes the whole tree to the branches satisfying it: filtering on a curriculum returns only the projects holding it, and only that curriculum inside them. Two limits apply. The translated *_txt fields are resolved from tsl_translations at read time and are not columns, so filter the stored tsl_* key instead. And statement keywords (SELECT, DELETE, CREATE, ...) are stripped from the clause before it is used, including inside quoted literals, so a string value containing one of them is silently altered. Examples: - `prj_projects.pk = 1` - one project with everything below it - `prj_projects.pk = 1 AND cur_curricula.pk = 32` - a single curriculum of that project - `prj_projects.pk = 1 AND (cur_curricula.pk = 30 OR cur_curricula.pk = 32)` - two of them - `cur_curricula.pk IN (30, 32)` - the same, with the project derived from the curricula - `aoe_area_of_operational_expertises.pk = 342` - every branch leading to that area - `cur_curricula.pk = 32 AND ops_operational_skills.tsl_title IS NOT NULL` - levels combined - `prj_projects.pk = 1 AND cur_curricula.created_at > '2020-01-01'` - non-key columns
 * @member {String} where
 * @default 'prj_projects.pk > 0'
 */
SkillsViewQuery.prototype['where'] = 'prj_projects.pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
SkillsViewQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
SkillsViewQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
SkillsViewQuery.prototype['language'] = undefined;






export { SkillsViewQuery };

/**
 * The TextSnippet model module.
 * @module model/TextSnippet
 * @version 0.9.0
 */
class TextSnippet {
    /**
     * Constructs a new <code>TextSnippet</code>.
     * Reusable multilingual text snippet belonging to a skills project.
     * @alias module:model/TextSnippet
     * @param fkProject {Number} Reference to the owning project this text snippet belongs to; cascades on delete.
     */
    constructor(fkProject) { 
        
        TextSnippet.initialize(this, fkProject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fkProject) { 
        obj['fk_project'] = fkProject;
    }

    /**
     * Constructs a <code>TextSnippet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TextSnippet} obj Optional instance to populate.
     * @return {module:model/TextSnippet} The populated <code>TextSnippet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TextSnippet();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_project')) {
                obj['fk_project'] = ApiClient.convertToType(data['fk_project'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('tsl_text_snippet')) {
                obj['tsl_text_snippet'] = ApiClient.convertToType(data['tsl_text_snippet'], 'Number');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('tsl_text_snippet_txt')) {
                obj['tsl_text_snippet_txt'] = ApiClient.convertToType(data['tsl_text_snippet_txt'], 'String');
            }
            if (data.hasOwnProperty('tsl_language')) {
                obj['tsl_language'] = ApiClient.convertToType(data['tsl_language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TextSnippet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TextSnippet</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TextSnippet.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // ensure the json data is a string
        if (data['tsl_text_snippet_txt'] && !(typeof data['tsl_text_snippet_txt'] === 'string' || data['tsl_text_snippet_txt'] instanceof String)) {
            throw new Error("Expected the field `tsl_text_snippet_txt` to be a primitive type in the JSON string but got " + data['tsl_text_snippet_txt']);
        }

        return true;
    }


}

TextSnippet.RequiredProperties = ["fk_project"];

/**
 * Auto-generated surrogate primary key uniquely identifying the text snippet row.
 * @member {Number} pk
 */
TextSnippet.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
TextSnippet.prototype['uuid'] = undefined;

/**
 * Reference to the owning project this text snippet belongs to; cascades on delete.
 * @member {Number} fk_project
 */
TextSnippet.prototype['fk_project'] = undefined;

/**
 * Human-readable unique business identifier of the text snippet.
 * @member {String} id
 */
TextSnippet.prototype['id'] = undefined;

/**
 * Detailed description of the text snippet and its purpose.
 * @member {String} description
 */
TextSnippet.prototype['description'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual text of the snippet; logical reference, not a database foreign key.
 * @member {Number} tsl_text_snippet
 */
TextSnippet.prototype['tsl_text_snippet'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
TextSnippet.prototype['last_write_user'] = undefined;

/**
 * Marking the row as written by an Endpoint and pending human review.
 * @member {module:model/DataStateType} data_state
 */
TextSnippet.prototype['data_state'] = undefined;

/**
 * Marking the row as written by an MCP agent and pending human review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
TextSnippet.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
TextSnippet.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes stored as JSONB.
 * @member {Object.<String, Object>} notes
 */
TextSnippet.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata stored as JSONB.
 * @member {Object.<String, Object>} metadatas
 */
TextSnippet.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
TextSnippet.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
TextSnippet.prototype['updated_at'] = undefined;

/**
 * Reference to the tsl_translations row holding the multilingual text of the snippet; logical reference, not a database foreign key.
 * @member {String} tsl_text_snippet_txt
 */
TextSnippet.prototype['tsl_text_snippet_txt'] = undefined;

/**
 * Language used to resolve the `tsl_*_txt` fields.
 * @member {module:model/TslLanguageType} tsl_language
 */
TextSnippet.prototype['tsl_language'] = undefined;






export { TextSnippet };

/**
 * The TextSnippetQuery model module.
 * @module model/TextSnippetQuery
 * @version 0.9.0
 */
class TextSnippetQuery {
    /**
     * Constructs a new <code>TextSnippetQuery</code>.
     * Query payload for text snippet rows: SQL WHERE clause plus limit/offset pagination and language for translated fields.
     * @alias module:model/TextSnippetQuery
     */
    constructor() { 
        
        TextSnippetQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>TextSnippetQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TextSnippetQuery} obj Optional instance to populate.
     * @return {module:model/TextSnippetQuery} The populated <code>TextSnippetQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TextSnippetQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TextSnippetQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TextSnippetQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
TextSnippetQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
TextSnippetQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
TextSnippetQuery.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
TextSnippetQuery.prototype['language'] = undefined;






export { TextSnippetQuery };

/**
 * The TextSnippets model module.
 * @module model/TextSnippets
 * @version 0.9.0
 */
class TextSnippets {
    /**
     * Constructs a new <code>TextSnippets</code>.
     * TextSnippet result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/TextSnippets
     */
    constructor() { 
        
        TextSnippets.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>TextSnippets</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TextSnippets} obj Optional instance to populate.
     * @return {module:model/TextSnippets} The populated <code>TextSnippets</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TextSnippets();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('language')) {
                obj['language'] = ApiClient.convertToType(data['language'], TslLanguageType);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [TextSnippet]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TextSnippets</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TextSnippets</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                TextSnippet.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
TextSnippets.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
TextSnippets.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
TextSnippets.prototype['offset'] = undefined;

/**
 * Language used for translated fields
 * @member {module:model/TslLanguageType} language
 */
TextSnippets.prototype['language'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
TextSnippets.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
TextSnippets.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
TextSnippets.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/TextSnippet>} rows
 */
TextSnippets.prototype['rows'] = undefined;






export { TextSnippets };

/**
 * The Token model module.
 * @module model/Token
 * @version 0.9.0
 */
class Token {
    /**
     * Constructs a new <code>Token</code>.
     * The response body of /auth/token: an OAuth2 bearer token together with the scopes it was granted, so a client can see what the token allows without decoding it.
     * @alias module:model/Token
     * @param accessToken {String} Signed JWT to be sent on subsequent requests as `Authorization: Bearer <access_token>`.
     * @param tokenType {String} Authentication scheme the token is to be used with; always `bearer`.
     * @param scope {String} Space-separated scope names the token carries. Requesting no scope yields the user's full set; requesting more than the user holds is refused rather than narrowed.
     */
    constructor(accessToken, tokenType, scope) { 
        
        Token.initialize(this, accessToken, tokenType, scope);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accessToken, tokenType, scope) { 
        obj['access_token'] = accessToken;
        obj['token_type'] = tokenType;
        obj['scope'] = scope;
    }

    /**
     * Constructs a <code>Token</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Token} obj Optional instance to populate.
     * @return {module:model/Token} The populated <code>Token</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Token();

            if (data.hasOwnProperty('access_token')) {
                obj['access_token'] = ApiClient.convertToType(data['access_token'], 'String');
            }
            if (data.hasOwnProperty('token_type')) {
                obj['token_type'] = ApiClient.convertToType(data['token_type'], 'String');
            }
            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Token</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Token</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Token.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['access_token'] && !(typeof data['access_token'] === 'string' || data['access_token'] instanceof String)) {
            throw new Error("Expected the field `access_token` to be a primitive type in the JSON string but got " + data['access_token']);
        }
        // ensure the json data is a string
        if (data['token_type'] && !(typeof data['token_type'] === 'string' || data['token_type'] instanceof String)) {
            throw new Error("Expected the field `token_type` to be a primitive type in the JSON string but got " + data['token_type']);
        }
        // ensure the json data is a string
        if (data['scope'] && !(typeof data['scope'] === 'string' || data['scope'] instanceof String)) {
            throw new Error("Expected the field `scope` to be a primitive type in the JSON string but got " + data['scope']);
        }

        return true;
    }


}

Token.RequiredProperties = ["access_token", "token_type", "scope"];

/**
 * Signed JWT to be sent on subsequent requests as `Authorization: Bearer <access_token>`.
 * @member {String} access_token
 */
Token.prototype['access_token'] = undefined;

/**
 * Authentication scheme the token is to be used with; always `bearer`.
 * @member {String} token_type
 */
Token.prototype['token_type'] = undefined;

/**
 * Space-separated scope names the token carries. Requesting no scope yields the user's full set; requesting more than the user holds is refused rather than narrowed.
 * @member {String} scope
 */
Token.prototype['scope'] = undefined;






export { Token };

/**
 * The TokenQuerySystem model module.
 * @module model/TokenQuerySystem
 * @version 0.9.0
 */
class TokenQuerySystem {
    /**
     * Constructs a new <code>TokenQuerySystem</code>.
     * Query payload for token rows: SQL WHERE clause plus limit/offset pagination.
     * @alias module:model/TokenQuerySystem
     */
    constructor() { 
        
        TokenQuerySystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>TokenQuerySystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TokenQuerySystem} obj Optional instance to populate.
     * @return {module:model/TokenQuerySystem} The populated <code>TokenQuerySystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TokenQuerySystem();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TokenQuerySystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TokenQuerySystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
TokenQuerySystem.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
TokenQuerySystem.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
TokenQuerySystem.prototype['offset'] = undefined;






export { TokenQuerySystem };

/**
 * The TokenSystem model module.
 * @module model/TokenSystem
 * @version 0.9.0
 */
class TokenSystem {
    /**
     * Constructs a new <code>TokenSystem</code>.
     * The bearer tokens a user may present — the record every request is checked against.  A signed JWT proves on its own that it was minted by this application and is not yet expired, which is precisely the problem: nothing about it can be taken back before &#x60;&#x60;exp&#x60;&#x60; passes. This table is what makes that possible. &#x60;&#x60;auth_service.get_current_user&#x60;&#x60; looks up every presented token here after validating its signature, and refuses it unless a row matches and that row is enabled — so the row, not the signature, decides whether a token is still current. &#x60;&#x60;type&#x60;&#x60; records which of the three channels of &#x60;&#x60;UsrTokenType&#x60;&#x60; the token was issued for — &#x60;&#x60;ui&#x60;&#x60;, &#x60;&#x60;api&#x60;&#x60; or &#x60;&#x60;mcp&#x60;&#x60; — mirroring the per-channel credential columns on &#x60;&#x60;usr_users&#x60;&#x60;. It is more than a label: &#x60;&#x60;auth_service.create_access_token&#x60;&#x60; gives a &#x60;&#x60;ui&#x60;&#x60; token the fixed &#x60;&#x60;UI_ACCESS_TOKEN_TTL&#x60;&#x60; instead of the account&#39;s &#x60;&#x60;access_token_ttl&#x60;&#x60;, because the interface has no way to refresh one.  A user may hold several tokens, one per row. Only the SHA-256 digest of a token is kept, in &#x60;&#x60;token_hash&#x60;&#x60; — verification hashes what the client presented and looks that digest up, so resolving a token stays a single indexed query while the token itself never reaches the database, its indexes or &#x60;&#x60;log_entries&#x60;&#x60;. The digest is deliberately unsalted and deliberately not run through the Argon2id hasher used for &#x60;&#x60;usr_users.password_hash&#x60;&#x60;: a token is generated with 256 bits of entropy, so hashing speed buys an attacker nothing, whereas a per-row salt would force a scan over every row to find a match and a slow KDF would be paid on every single request. The price is that a token can be shown only once, when it is issued: nothing on the row can be read back to reconstruct it, and nothing on it makes one token recognisable from another afterwards either. &#x60;&#x60;token_scope&#x60;&#x60; is meant to say what a token was issued for, but is reserved — no code path writes it yet.  Only tokens whose &#x60;&#x60;enable&#x60;&#x60; flag is true are accepted, which makes revoking a token a single UPDATE instead of a DELETE and leaves the row in place for auditing. &#x60;&#x60;fk_user&#x60;&#x60; cascades, so deleting a user removes every token issued to it.
     * @alias module:model/TokenSystem
     */
    constructor() { 
        
        TokenSystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['enable'] = false;
    }

    /**
     * Constructs a <code>TokenSystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TokenSystem} obj Optional instance to populate.
     * @return {module:model/TokenSystem} The populated <code>TokenSystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TokenSystem();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_user')) {
                obj['fk_user'] = ApiClient.convertToType(data['fk_user'], 'Number');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], UsrTokenType);
            }
            if (data.hasOwnProperty('enable')) {
                obj['enable'] = ApiClient.convertToType(data['enable'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope')) {
                obj['token_scope'] = ApiClient.convertToType(data['token_scope'], 'String');
            }
            if (data.hasOwnProperty('token_expired_on')) {
                obj['token_expired_on'] = ApiClient.convertToType(data['token_expired_on'], 'Date');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TokenSystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TokenSystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['token_scope'] && !(typeof data['token_scope'] === 'string' || data['token_scope'] instanceof String)) {
            throw new Error("Expected the field `token_scope` to be a primitive type in the JSON string but got " + data['token_scope']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying this token.
 * @member {Number} pk
 */
TokenSystem.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created. Use this instead of pk when referencing the token outside the database.
 * @member {String} uuid
 */
TokenSystem.prototype['uuid'] = undefined;

/**
 * Foreign key referencing the user the token was issued to; the token is deleted when that user is deleted.
 * @member {Number} fk_user
 */
TokenSystem.prototype['fk_user'] = undefined;

/**
 * Channel the token was issued for: 'ui' for the user interface (the default), 'api' for a programmatic REST client or 'mcp' for the MCP server. Not merely a label: auth_service.create_access_token gives a 'ui' token the fixed UI_ACCESS_TOKEN_TTL (18 hours) instead of the account's access_token_ttl, since the interface has no way to refresh one. It widens nothing on its own - what a token may do is decided by the account's token_scope_* columns alone.
 * @member {module:model/UsrTokenType} type
 */
TokenSystem.prototype['type'] = undefined;

/**
 * Whether the token is accepted for authentication. Set to false to revoke the token without deleting the row.
 * @member {Boolean} enable
 * @default false
 */
TokenSystem.prototype['enable'] = false;

/**
 * Scopes the token was issued with, as the space-separated list its `scope` claim carries, so what a token reaches can be read off the row instead of only out of the token. Reserved: nothing writes this column yet and no decision rests on it - auth_service.get_current_user takes the scopes from the token's own claims and intersects them with the account's token_scope_* columns, so a value written here grants nothing and withdraws nothing.
 * @member {String} token_scope
 */
TokenSystem.prototype['token_scope'] = undefined;

/**
 * Instant at which the token stops being valid, as timestamptz(6) - an absolute point in time to microsecond resolution, so it is unambiguous across time zones and daylight saving changes. NULL means the row sets no expiry of its own. This is the stored token's lifetime, which is not the same thing as the `exp` claim inside a JWT: `exp` is fixed when the token is minted and can never be changed, whereas this column can be shortened or extended on the row afterwards.
 * @member {Date} token_expired_on
 */
TokenSystem.prototype['token_expired_on'] = undefined;

/**
 * Marks the row as written through an API endpoint and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/DataStateType} data_state
 */
TokenSystem.prototype['data_state'] = undefined;

/**
 * Marks the row as written by an MCP agent and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
TokenSystem.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about this token. Not interpreted by the application.
 * @member {String} comments
 */
TokenSystem.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes about this token, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} notes
 */
TokenSystem.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata about this token, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} metadatas
 */
TokenSystem.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
TokenSystem.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update by the set_updated_at() trigger. Used for optimistic locking by the API.
 * @member {Date} updated_at
 */
TokenSystem.prototype['updated_at'] = undefined;






export { TokenSystem };

/**
 * The TokensSystem model module.
 * @module model/TokensSystem
 * @version 0.9.0
 */
class TokensSystem {
    /**
     * Constructs a new <code>TokensSystem</code>.
     * Token result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/TokensSystem
     */
    constructor() { 
        
        TokensSystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>TokensSystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TokensSystem} obj Optional instance to populate.
     * @return {module:model/TokensSystem} The populated <code>TokensSystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TokensSystem();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [TokenSystem]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TokensSystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TokensSystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                TokenSystem.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
TokensSystem.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
TokensSystem.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
TokensSystem.prototype['offset'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
TokensSystem.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
TokensSystem.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
TokensSystem.prototype['all'] = undefined;

/**
 * Token rows
 * @member {Array.<module:model/TokenSystem>} rows
 */
TokensSystem.prototype['rows'] = undefined;






export { TokensSystem };

/**
 * The Translation model module.
 * @module model/Translation
 * @version 0.9.0
 */
class Translation {
    /**
     * Constructs a new <code>Translation</code>.
     * Multilingual translation row for text attributes in skills tables.
     * @alias module:model/Translation
     */
    constructor() { 
        
        Translation.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Translation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Translation} obj Optional instance to populate.
     * @return {module:model/Translation} The populated <code>Translation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Translation();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_table')) {
                obj['fk_table'] = ApiClient.convertToType(data['fk_table'], 'String');
            }
            if (data.hasOwnProperty('fk_table_pk')) {
                obj['fk_table_pk'] = ApiClient.convertToType(data['fk_table_pk'], 'Number');
            }
            if (data.hasOwnProperty('fk_attribute')) {
                obj['fk_attribute'] = ApiClient.convertToType(data['fk_attribute'], 'String');
            }
            if (data.hasOwnProperty('master_language')) {
                obj['master_language'] = ApiClient.convertToType(data['master_language'], TslMasterLanguageType);
            }
            if (data.hasOwnProperty('de_tsl_state')) {
                obj['de_tsl_state'] = ApiClient.convertToType(data['de_tsl_state'], TslStateType);
            }
            if (data.hasOwnProperty('fr_tsl_state')) {
                obj['fr_tsl_state'] = ApiClient.convertToType(data['fr_tsl_state'], TslStateType);
            }
            if (data.hasOwnProperty('it_tsl_state')) {
                obj['it_tsl_state'] = ApiClient.convertToType(data['it_tsl_state'], TslStateType);
            }
            if (data.hasOwnProperty('en_tsl_state')) {
                obj['en_tsl_state'] = ApiClient.convertToType(data['en_tsl_state'], TslStateType);
            }
            if (data.hasOwnProperty('de')) {
                obj['de'] = ApiClient.convertToType(data['de'], 'String');
            }
            if (data.hasOwnProperty('fr')) {
                obj['fr'] = ApiClient.convertToType(data['fr'], 'String');
            }
            if (data.hasOwnProperty('it')) {
                obj['it'] = ApiClient.convertToType(data['it'], 'String');
            }
            if (data.hasOwnProperty('en')) {
                obj['en'] = ApiClient.convertToType(data['en'], 'String');
            }
            if (data.hasOwnProperty('tsl_table_updated_at')) {
                obj['tsl_table_updated_at'] = ApiClient.convertToType(data['tsl_table_updated_at'], 'Date');
            }
            if (data.hasOwnProperty('last_write_user')) {
                obj['last_write_user'] = ApiClient.convertToType(data['last_write_user'], 'String');
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Translation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Translation</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['fk_table'] && !(typeof data['fk_table'] === 'string' || data['fk_table'] instanceof String)) {
            throw new Error("Expected the field `fk_table` to be a primitive type in the JSON string but got " + data['fk_table']);
        }
        // ensure the json data is a string
        if (data['fk_attribute'] && !(typeof data['fk_attribute'] === 'string' || data['fk_attribute'] instanceof String)) {
            throw new Error("Expected the field `fk_attribute` to be a primitive type in the JSON string but got " + data['fk_attribute']);
        }
        // ensure the json data is a string
        if (data['de'] && !(typeof data['de'] === 'string' || data['de'] instanceof String)) {
            throw new Error("Expected the field `de` to be a primitive type in the JSON string but got " + data['de']);
        }
        // ensure the json data is a string
        if (data['fr'] && !(typeof data['fr'] === 'string' || data['fr'] instanceof String)) {
            throw new Error("Expected the field `fr` to be a primitive type in the JSON string but got " + data['fr']);
        }
        // ensure the json data is a string
        if (data['it'] && !(typeof data['it'] === 'string' || data['it'] instanceof String)) {
            throw new Error("Expected the field `it` to be a primitive type in the JSON string but got " + data['it']);
        }
        // ensure the json data is a string
        if (data['en'] && !(typeof data['en'] === 'string' || data['en'] instanceof String)) {
            throw new Error("Expected the field `en` to be a primitive type in the JSON string but got " + data['en']);
        }
        // ensure the json data is a string
        if (data['last_write_user'] && !(typeof data['last_write_user'] === 'string' || data['last_write_user'] instanceof String)) {
            throw new Error("Expected the field `last_write_user` to be a primitive type in the JSON string but got " + data['last_write_user']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying the translation row.
 * @member {Number} pk
 */
Translation.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created.
 * @member {String} uuid
 */
Translation.prototype['uuid'] = undefined;

/**
 * Name of the source table whose attribute is translated; logical reference, not a database foreign key.
 * @member {String} fk_table
 */
Translation.prototype['fk_table'] = undefined;

/**
 * Primary key (pk) of the row in the source table whose attribute is translated; logical reference, not a database foreign key.
 * @member {Number} fk_table_pk
 */
Translation.prototype['fk_table_pk'] = undefined;

/**
 * Name of the attribute (column) in the source table whose text is translated.
 * @member {String} fk_attribute
 */
Translation.prototype['fk_attribute'] = undefined;

/**
 * Reference language used during the development process.
 * @member {module:model/TslMasterLanguageType} master_language
 */
Translation.prototype['master_language'] = undefined;

/**
 * Translation status of the German text.
 * @member {module:model/TslStateType} de_tsl_state
 */
Translation.prototype['de_tsl_state'] = undefined;

/**
 * Translation status of the French text.
 * @member {module:model/TslStateType} fr_tsl_state
 */
Translation.prototype['fr_tsl_state'] = undefined;

/**
 * Translation status of the Italian text.
 * @member {module:model/TslStateType} it_tsl_state
 */
Translation.prototype['it_tsl_state'] = undefined;

/**
 * Translation status of the English text.
 * @member {module:model/TslStateType} en_tsl_state
 */
Translation.prototype['en_tsl_state'] = undefined;

/**
 * German text of the translated attribute.
 * @member {String} de
 */
Translation.prototype['de'] = undefined;

/**
 * French text of the translated attribute.
 * @member {String} fr
 */
Translation.prototype['fr'] = undefined;

/**
 * Italian text of the translated attribute.
 * @member {String} it
 */
Translation.prototype['it'] = undefined;

/**
 * English text of the translated attribute.
 * @member {String} en
 */
Translation.prototype['en'] = undefined;

/**
 * Copy of the `updated_at` timestamp of the source row (`fk_table` / `fk_table_pk`) taken when this translation was last reconciled with it, as timestamptz(6) - an absolute point in time to microsecond resolution, so it is unambiguous across time zones and daylight saving changes. Used together with `tsl_compare` to detect whether the source row has changed since the last translation. NULL as long as no reconciliation has happened yet.
 * @member {Date} tsl_table_updated_at
 */
Translation.prototype['tsl_table_updated_at'] = undefined;

/**
 * Identifier of the user who most recently created or updated the row.
 * @member {String} last_write_user
 */
Translation.prototype['last_write_user'] = undefined;

/**
 * Free-form comments about the row.
 * @member {String} comments
 */
Translation.prototype['comments'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
Translation.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update.
 * @member {Date} updated_at
 */
Translation.prototype['updated_at'] = undefined;






export { Translation };

/**
 * The TranslationQuery model module.
 * @module model/TranslationQuery
 * @version 0.9.0
 */
class TranslationQuery {
    /**
     * Constructs a new <code>TranslationQuery</code>.
     * Query payload for translation rows: SQL WHERE clause plus limit/offset pagination.
     * @alias module:model/TranslationQuery
     */
    constructor() { 
        
        TranslationQuery.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>TranslationQuery</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TranslationQuery} obj Optional instance to populate.
     * @return {module:model/TranslationQuery} The populated <code>TranslationQuery</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TranslationQuery();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TranslationQuery</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TranslationQuery</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
TranslationQuery.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
TranslationQuery.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
TranslationQuery.prototype['offset'] = undefined;






export { TranslationQuery };

/**
 * The Translations model module.
 * @module model/Translations
 * @version 0.9.0
 */
class Translations {
    /**
     * Constructs a new <code>Translations</code>.
     * Translation result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/Translations
     */
    constructor() { 
        
        Translations.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Translations</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Translations} obj Optional instance to populate.
     * @return {module:model/Translations} The populated <code>Translations</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Translations();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [Translation]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Translations</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Translations</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                Translation.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Translations.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Translations.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Translations.prototype['offset'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
Translations.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
Translations.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
Translations.prototype['all'] = undefined;

/**
 * Rows returned by the query
 * @member {Array.<module:model/Translation>} rows
 */
Translations.prototype['rows'] = undefined;






export { Translations };

/**
* Enum class TslLanguageType.
* @enum {}
* @readonly
*/
export class TslLanguageType {
    
        /**
         * value: "de"
         * @const
         */
        "DE" = "de";

    
        /**
         * value: "fr"
         * @const
         */
        "FR" = "fr";

    
        /**
         * value: "it"
         * @const
         */
        "IT" = "it";

    
        /**
         * value: "en"
         * @const
         */
        "EN" = "en";

    

    /**
    * Returns a <code>TslLanguageType</code> enum value from a Javascript object name.
    * @param {Object} data The plain JavaScript object containing the name of the enum value.
    * @return {module:model/TslLanguageType} The enum <code>TslLanguageType</code> value.
    */
    static constructFromObject(object) {
        return object;
    }
}

/**
* Enum class TslMasterLanguageType.
* @enum {}
* @readonly
*/
export class TslMasterLanguageType {
    
        /**
         * value: "de"
         * @const
         */
        "DE" = "de";

    
        /**
         * value: "fr"
         * @const
         */
        "FR" = "fr";

    
        /**
         * value: "it"
         * @const
         */
        "IT" = "it";

    
        /**
         * value: "en"
         * @const
         */
        "EN" = "en";

    

    /**
    * Returns a <code>TslMasterLanguageType</code> enum value from a Javascript object name.
    * @param {Object} data The plain JavaScript object containing the name of the enum value.
    * @return {module:model/TslMasterLanguageType} The enum <code>TslMasterLanguageType</code> value.
    */
    static constructFromObject(object) {
        return object;
    }
}

/**
* Enum class TslStateType.
* @enum {}
* @readonly
*/
export class TslStateType {
    
        /**
         * value: "draft"
         * @const
         */
        "_1_DRAFT" = "draft";

    
        /**
         * value: "preview_version"
         * @const
         */
        "_2_PREVIEW_VERSION" = "preview_version";

    
        /**
         * value: "post_editing"
         * @const
         */
        "_3A_POST_EDITING" = "post_editing";

    
        /**
         * value: "post_editing_done"
         * @const
         */
        "_3B_POST_EDITING_DONE" = "post_editing_done";

    
        /**
         * value: "specialiced_editing"
         * @const
         */
        "_4A_SPECIALICED_EDITING" = "specialiced_editing";

    
        /**
         * value: "specialiced_editing_done"
         * @const
         */
        "_4B_SPECIALICED_EDITING_DONE" = "specialiced_editing_done";

    
        /**
         * value: "final"
         * @const
         */
        "_5_FINAL" = "final";

    

    /**
    * Returns a <code>TslStateType</code> enum value from a Javascript object name.
    * @param {Object} data The plain JavaScript object containing the name of the enum value.
    * @return {module:model/TslStateType} The enum <code>TslStateType</code> value.
    */
    static constructFromObject(object) {
        return object;
    }
}

/**
 * The UploadResponse model module.
 * @module model/UploadResponse
 * @version 0.9.0
 */
class UploadResponse {
    /**
     * Constructs a new <code>UploadResponse</code>.
     * @alias module:model/UploadResponse
     * @param filename {String} 
     * @param path {String} 
     * @param size {Number} 
     */
    constructor(filename, path, size) { 
        
        UploadResponse.initialize(this, filename, path, size);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, filename, path, size) { 
        obj['filename'] = filename;
        obj['path'] = path;
        obj['size'] = size;
    }

    /**
     * Constructs a <code>UploadResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UploadResponse} obj Optional instance to populate.
     * @return {module:model/UploadResponse} The populated <code>UploadResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UploadResponse();

            if (data.hasOwnProperty('filename')) {
                obj['filename'] = ApiClient.convertToType(data['filename'], 'String');
            }
            if (data.hasOwnProperty('path')) {
                obj['path'] = ApiClient.convertToType(data['path'], 'String');
            }
            if (data.hasOwnProperty('size')) {
                obj['size'] = ApiClient.convertToType(data['size'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UploadResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UploadResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UploadResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['filename'] && !(typeof data['filename'] === 'string' || data['filename'] instanceof String)) {
            throw new Error("Expected the field `filename` to be a primitive type in the JSON string but got " + data['filename']);
        }
        // ensure the json data is a string
        if (data['path'] && !(typeof data['path'] === 'string' || data['path'] instanceof String)) {
            throw new Error("Expected the field `path` to be a primitive type in the JSON string but got " + data['path']);
        }

        return true;
    }


}

UploadResponse.RequiredProperties = ["filename", "path", "size"];

/**
 * @member {String} filename
 */
UploadResponse.prototype['filename'] = undefined;

/**
 * @member {String} path
 */
UploadResponse.prototype['path'] = undefined;

/**
 * @member {Number} size
 */
UploadResponse.prototype['size'] = undefined;






export { UploadResponse };

/**
 * The UserChange model module.
 * @module model/UserChange
 * @version 0.9.0
 */
class UserChange {
    /**
     * Constructs a new <code>UserChange</code>.
     * Request body of PATCH /auth/me: the profile fields to change, every one of them optional. What is written is decided by which keys the body actually carries, not by which values are non-null - a field left out of the body keeps its stored value, and a field sent as &#x60;null&#x60; clears it. The distinction is what makes changing one field possible: &#x60;img_avatar&#x60; is a picture held in the row itself, so a caller correcting the spelling of a name must neither have to send the picture along nor lose it by leaving it out.  An ApiBase rather than a plain BaseModel, for &#x60;img_avatar&#x60; alone: JSON has no byte type, so the field travels as base64, and without that configured on the request side the base64 text itself would be stored as the picture.  The username is deliberately absent, as it is on PasswordChange - the account is taken from the bearer token, so the endpoint can only ever change the profile of the account calling it. So is every other column of &#x60;usr_users&#x60;: &#x60;enable&#x60;, &#x60;ui_level&#x60; and the &#x60;token_scope_*&#x60; flags decide what an account may do and are nobody&#39;s own business to set, and &#x60;username&#x60; is the name every token is issued against. Those go through &#x60;/system-tables/users/update&#x60;, which requires the &#x60;admin&#x60; scope.
     * @alias module:model/UserChange
     */
    constructor() { 
        
        UserChange.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>UserChange</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserChange} obj Optional instance to populate.
     * @return {module:model/UserChange} The populated <code>UserChange</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserChange();

            if (data.hasOwnProperty('first_name')) {
                obj['first_name'] = ApiClient.convertToType(data['first_name'], 'String');
            }
            if (data.hasOwnProperty('surname')) {
                obj['surname'] = ApiClient.convertToType(data['surname'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('img_avatar')) {
                obj['img_avatar'] = ApiClient.convertToType(data['img_avatar'], File);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserChange</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserChange</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['first_name'] && !(typeof data['first_name'] === 'string' || data['first_name'] instanceof String)) {
            throw new Error("Expected the field `first_name` to be a primitive type in the JSON string but got " + data['first_name']);
        }
        // ensure the json data is a string
        if (data['surname'] && !(typeof data['surname'] === 'string' || data['surname'] instanceof String)) {
            throw new Error("Expected the field `surname` to be a primitive type in the JSON string but got " + data['surname']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }

        return true;
    }


}



/**
 * Given name of the account holder, written to `usr_users.first_name`. Send `null` to clear it; an empty string is refused, so a name is either a name or absent.
 * @member {String} first_name
 */
UserChange.prototype['first_name'] = undefined;

/**
 * Family name of the account holder, written to `usr_users.surname`. Send `null` to clear it; an empty string is refused.
 * @member {String} surname
 */
UserChange.prototype['surname'] = undefined;

/**
 * Address to reach the account holder at, written to `usr_users.email`. An address already registered to another account is refused with 409, the rule `/auth/sign-up` applies. Send `null` to clear it - note that an account without an address can no longer be reached, and that a password is only ever delivered by mail.
 * @member {String} email
 */
UserChange.prototype['email'] = undefined;

/**
 * Profile picture, base64 encoded in the request and stored as the raw bytes of `usr_users.img_avatar`. At most 1048576 bytes once decoded, which is a third less than that as base64. The content is not inspected: anything within the bound is accepted, so it is on the caller to send a picture. Send `null` to remove the picture.
 * @member {File} img_avatar
 */
UserChange.prototype['img_avatar'] = undefined;






export { UserChange };

/**
 * The UserChangeResult model module.
 * @module model/UserChangeResult
 * @version 0.9.0
 */
class UserChangeResult {
    /**
     * Constructs a new <code>UserChangeResult</code>.
     * Response body of PATCH /auth/me. Reached only when the row was written; every other outcome is an HTTP error, never &#x60;changed: false&#x60;. The profile is deliberately not echoed back - &#x60;img_avatar&#x60; would put the whole picture into the answer to a request that changed a name. A caller that wants the stored state reads GET /auth/me.
     * @alias module:model/UserChangeResult
     * @param changed {Boolean} Always true. Present so the response is a self-describing JSON object rather than an empty body.
     */
    constructor(changed) { 
        
        UserChangeResult.initialize(this, changed);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, changed) { 
        obj['changed'] = changed;
    }

    /**
     * Constructs a <code>UserChangeResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserChangeResult} obj Optional instance to populate.
     * @return {module:model/UserChangeResult} The populated <code>UserChangeResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserChangeResult();

            if (data.hasOwnProperty('changed')) {
                obj['changed'] = ApiClient.convertToType(data['changed'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserChangeResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserChangeResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserChangeResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

UserChangeResult.RequiredProperties = ["changed"];

/**
 * Always true. Present so the response is a self-describing JSON object rather than an empty body.
 * @member {Boolean} changed
 */
UserChangeResult.prototype['changed'] = undefined;






export { UserChangeResult };

/**
 * The UserInfo model module.
 * @module model/UserInfo
 * @version 0.9.0
 */
class UserInfo {
    /**
     * Constructs a new <code>UserInfo</code>.
     * Response body of GET /auth/me: what the calling account may know about itself. The four fields PATCH /auth/me writes, and beside them the read-only ones a client would otherwise have no way to reach - &#x60;usr_users&#x60; can only be read through &#x60;/system-tables/users/read&#x60;, which requires the &#x60;admin&#x60; scope, so without this an account could not so much as find out its own interface level.  Nothing here is secret, but nothing here is fetched twice either: &#x60;username&#x60; and &#x60;scopes&#x60; are what the request was already authorised against, and the rest is one lookup of the row.  &#x60;enable&#x60; is deliberately absent. A disabled account is refused by get_current_user before it reaches the endpoint, so the field could only ever read true and would say nothing. The credential columns - &#x60;password_hash&#x60;, &#x60;otp_hash&#x60;, &#x60;api_password_hash&#x60;, &#x60;mcp_password_hash&#x60; - are absent for the reason they are absent from the admin model too: they are not to be read back.
     * @alias module:model/UserInfo
     * @param username {String} Login name of the account, the `sub` claim of the token this was asked with. Cannot be changed through PATCH /auth/me - every token ever issued is recorded against it.
     * @param uuid {String} Stable identifier of the account, generated by the database. This rather than `pk` is the value to reference the account by outside the database, and it is immutable.
     * @param firstName {String} `usr_users.first_name`, or `null` while none is set. Changed through PATCH /auth/me.
     * @param surname {String} `usr_users.surname`, or `null` while none is set. Changed through PATCH /auth/me.
     * @param email {String} `usr_users.email`, or `null` while none is set. The only channel a password is ever delivered on. Changed through PATCH /auth/me.
     * @param imgAvatar {File} `usr_users.img_avatar` base64 encoded, or `null` while no picture is set. Note that this is the URL-safe base64 alphabet and carries no `=` padding, so a client decoding it needs a URL-safe decoder - a standard one silently discards the `-` and `_` characters and yields a corrupt picture. Changed through PATCH /auth/me.
     * @param uiLevel {module:model/UsrUiLevelType} The `usr_users.ui_level` of the account: the interface level it is meant to be presented at. `none` when the row has no level set, `pending` for an account that has registered and not been reviewed. Presentation only - it grants nothing, and what a token may do is decided by the `token_scope_*` columns alone. Not changeable here; an account does not promote itself.
     * @param requirePasswordChange {Boolean} True when the account has to replace its password, as after one was assigned to it by `/auth/sign-up` rather than chosen. The password is replaced through `/auth/password-change`.
     * @param scopes {Array.<module:model/Scope>} The scopes the *account* holds, read from its `token_scope_*` columns - the upper bound for every token it can be issued. Not the scopes of the token this was asked with, which may be fewer: a token carries what it requested, intersected with this set on every request. So a scope listed here is one the account may ask for, not necessarily one the token in hand already carries.
     */
    constructor(username, uuid, firstName, surname, email, imgAvatar, uiLevel, requirePasswordChange, scopes) { 
        
        UserInfo.initialize(this, username, uuid, firstName, surname, email, imgAvatar, uiLevel, requirePasswordChange, scopes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username, uuid, firstName, surname, email, imgAvatar, uiLevel, requirePasswordChange, scopes) { 
        obj['username'] = username;
        obj['uuid'] = uuid;
        obj['first_name'] = firstName;
        obj['surname'] = surname;
        obj['email'] = email;
        obj['img_avatar'] = imgAvatar;
        obj['ui_level'] = uiLevel;
        obj['require_password_change'] = requirePasswordChange;
        obj['scopes'] = scopes;
    }

    /**
     * Constructs a <code>UserInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserInfo} obj Optional instance to populate.
     * @return {module:model/UserInfo} The populated <code>UserInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserInfo();

            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('first_name')) {
                obj['first_name'] = ApiClient.convertToType(data['first_name'], 'String');
            }
            if (data.hasOwnProperty('surname')) {
                obj['surname'] = ApiClient.convertToType(data['surname'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('img_avatar')) {
                obj['img_avatar'] = ApiClient.convertToType(data['img_avatar'], File);
            }
            if (data.hasOwnProperty('ui_level')) {
                obj['ui_level'] = ApiClient.convertToType(data['ui_level'], UsrUiLevelType);
            }
            if (data.hasOwnProperty('require_password_change')) {
                obj['require_password_change'] = ApiClient.convertToType(data['require_password_change'], 'Boolean');
            }
            if (data.hasOwnProperty('scopes')) {
                obj['scopes'] = ApiClient.convertToType(data['scopes'], [Scope]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['first_name'] && !(typeof data['first_name'] === 'string' || data['first_name'] instanceof String)) {
            throw new Error("Expected the field `first_name` to be a primitive type in the JSON string but got " + data['first_name']);
        }
        // ensure the json data is a string
        if (data['surname'] && !(typeof data['surname'] === 'string' || data['surname'] instanceof String)) {
            throw new Error("Expected the field `surname` to be a primitive type in the JSON string but got " + data['surname']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['scopes'])) {
            throw new Error("Expected the field `scopes` to be an array in the JSON data but got " + data['scopes']);
        }

        return true;
    }


}

UserInfo.RequiredProperties = ["username", "uuid", "first_name", "surname", "email", "img_avatar", "ui_level", "require_password_change", "scopes"];

/**
 * Login name of the account, the `sub` claim of the token this was asked with. Cannot be changed through PATCH /auth/me - every token ever issued is recorded against it.
 * @member {String} username
 */
UserInfo.prototype['username'] = undefined;

/**
 * Stable identifier of the account, generated by the database. This rather than `pk` is the value to reference the account by outside the database, and it is immutable.
 * @member {String} uuid
 */
UserInfo.prototype['uuid'] = undefined;

/**
 * `usr_users.first_name`, or `null` while none is set. Changed through PATCH /auth/me.
 * @member {String} first_name
 */
UserInfo.prototype['first_name'] = undefined;

/**
 * `usr_users.surname`, or `null` while none is set. Changed through PATCH /auth/me.
 * @member {String} surname
 */
UserInfo.prototype['surname'] = undefined;

/**
 * `usr_users.email`, or `null` while none is set. The only channel a password is ever delivered on. Changed through PATCH /auth/me.
 * @member {String} email
 */
UserInfo.prototype['email'] = undefined;

/**
 * `usr_users.img_avatar` base64 encoded, or `null` while no picture is set. Note that this is the URL-safe base64 alphabet and carries no `=` padding, so a client decoding it needs a URL-safe decoder - a standard one silently discards the `-` and `_` characters and yields a corrupt picture. Changed through PATCH /auth/me.
 * @member {File} img_avatar
 */
UserInfo.prototype['img_avatar'] = undefined;

/**
 * The `usr_users.ui_level` of the account: the interface level it is meant to be presented at. `none` when the row has no level set, `pending` for an account that has registered and not been reviewed. Presentation only - it grants nothing, and what a token may do is decided by the `token_scope_*` columns alone. Not changeable here; an account does not promote itself.
 * @member {module:model/UsrUiLevelType} ui_level
 */
UserInfo.prototype['ui_level'] = undefined;

/**
 * True when the account has to replace its password, as after one was assigned to it by `/auth/sign-up` rather than chosen. The password is replaced through `/auth/password-change`.
 * @member {Boolean} require_password_change
 */
UserInfo.prototype['require_password_change'] = undefined;

/**
 * The scopes the *account* holds, read from its `token_scope_*` columns - the upper bound for every token it can be issued. Not the scopes of the token this was asked with, which may be fewer: a token carries what it requested, intersected with this set on every request. So a scope listed here is one the account may ask for, not necessarily one the token in hand already carries.
 * @member {Array.<module:model/Scope>} scopes
 */
UserInfo.prototype['scopes'] = undefined;






export { UserInfo };

/**
 * The UserQuerySystem model module.
 * @module model/UserQuerySystem
 * @version 0.9.0
 */
class UserQuerySystem {
    /**
     * Constructs a new <code>UserQuerySystem</code>.
     * Query payload for user rows: SQL WHERE clause plus limit/offset pagination.
     * @alias module:model/UserQuerySystem
     */
    constructor() { 
        
        UserQuerySystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>UserQuerySystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserQuerySystem} obj Optional instance to populate.
     * @return {module:model/UserQuerySystem} The populated <code>UserQuerySystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserQuerySystem();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserQuerySystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserQuerySystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
UserQuerySystem.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
UserQuerySystem.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
UserQuerySystem.prototype['offset'] = undefined;






export { UserQuerySystem };

/**
 * The UserSystem model module.
 * @module model/UserSystem
 * @version 0.9.0
 */
class UserSystem {
    /**
     * Constructs a new <code>UserSystem</code>.
     * A user account — and the row that decides what any token issued to it may carry.  A row can only be used to log in when &#x60;&#x60;enable&#x60;&#x60; is true and both &#x60;&#x60;username&#x60;&#x60; and &#x60;&#x60;password_hash&#x60;&#x60; are set — all three are checked by &#x60;&#x60;app/authentication/auth_service.py&#x60;&#x60;. The &#x60;&#x60;token_scope_*&#x60;&#x60; columns decide which scopes an issued access token may carry, one boolean per scope, so an account is granted exactly the set it needs rather than the nearest rung of a fixed ladder of roles. &#x60;&#x60;ui_level&#x60;&#x60; is the one place a rung is still named, and it is a presentation level only — it grants nothing.  The row carries one set of credentials per channel a token can be issued for, the three members of &#x60;&#x60;UsrTokenType&#x60;&#x60;. The unprefixed columns (&#x60;&#x60;username&#x60;&#x60; / &#x60;&#x60;password_hash&#x60;&#x60; / &#x60;&#x60;access_token_ttl&#x60;&#x60; / &#x60;&#x60;token_scope_*&#x60;&#x60;) are the set every login goes through today, for the &#x60;&#x60;ui&#x60;&#x60; and the &#x60;&#x60;api&#x60;&#x60; channel alike. The &#x60;&#x60;api_&#x60;&#x60; and &#x60;&#x60;mcp_&#x60;&#x60; prefixed columns repeat that same set for a programmatic REST client and for the MCP server, which is a separate application; they are columns of their own so a channel can be renamed, re-secreted, given its own token lifetime or locked out without touching the others. Both are reserved — no code path in this API reads them yet. The seed writes the &#x60;&#x60;mcp_&#x60;&#x60; set as a copy of the unprefixed one, so an account already reaches the MCP server with the same credentials, while the &#x60;&#x60;api_&#x60;&#x60; set is left unset.  The credential columns (&#x60;&#x60;password_hash&#x60;&#x60;, &#x60;&#x60;otp_hash&#x60;&#x60;, &#x60;&#x60;api_password_hash&#x60;&#x60;, &#x60;&#x60;mcp_password_hash&#x60;&#x60;) are deliberately excluded from the API model in &#x60;&#x60;app/api/system_tables/usr_users/models.py&#x60;&#x60;, so they can neither be read back nor written through the CRUD endpoints.  Every INSERT, UPDATE and DELETE on this table is captured in &#x60;&#x60;log_entries&#x60;&#x60; by the &#x60;&#x60;system.log_change()&#x60;&#x60; trigger.
     * @alias module:model/UserSystem
     */
    constructor() { 
        
        UserSystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['enable'] = false;
        obj['require_password_change'] = false;
        obj['token_scope_pending'] = false;
        obj['token_scope_public'] = false;
        obj['token_scope_read'] = false;
        obj['token_scope_write'] = false;
        obj['token_scope_project'] = false;
        obj['token_scope_admin'] = false;
        obj['token_scope_dev'] = false;
        obj['api_enable'] = false;
        obj['api_require_password_change'] = false;
        obj['api_token_scope_pending'] = false;
        obj['api_token_scope_public'] = false;
        obj['api_token_scope_read'] = false;
        obj['api_token_scope_write'] = false;
        obj['api_token_scope_project'] = false;
        obj['api_token_scope_admin'] = false;
        obj['api_token_scope_dev'] = false;
        obj['mcp_enable'] = false;
        obj['mcp_require_password_change'] = false;
        obj['mcp_token_scope_pending'] = false;
        obj['mcp_token_scope_public'] = false;
        obj['mcp_token_scope_read'] = false;
        obj['mcp_token_scope_write'] = false;
        obj['mcp_token_scope_project'] = false;
        obj['mcp_token_scope_admin'] = false;
        obj['mcp_token_scope_dev'] = false;
    }

    /**
     * Constructs a <code>UserSystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserSystem} obj Optional instance to populate.
     * @return {module:model/UserSystem} The populated <code>UserSystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserSystem();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('first_name')) {
                obj['first_name'] = ApiClient.convertToType(data['first_name'], 'String');
            }
            if (data.hasOwnProperty('surname')) {
                obj['surname'] = ApiClient.convertToType(data['surname'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('img_avatar')) {
                obj['img_avatar'] = ApiClient.convertToType(data['img_avatar'], File);
            }
            if (data.hasOwnProperty('enable')) {
                obj['enable'] = ApiClient.convertToType(data['enable'], 'Boolean');
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('ui_level')) {
                obj['ui_level'] = ApiClient.convertToType(data['ui_level'], UsrUiLevelType);
            }
            if (data.hasOwnProperty('require_password_change')) {
                obj['require_password_change'] = ApiClient.convertToType(data['require_password_change'], 'Boolean');
            }
            if (data.hasOwnProperty('access_token_ttl')) {
                obj['access_token_ttl'] = ApiClient.convertToType(data['access_token_ttl'], 'Number');
            }
            if (data.hasOwnProperty('token_scope_pending')) {
                obj['token_scope_pending'] = ApiClient.convertToType(data['token_scope_pending'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_public')) {
                obj['token_scope_public'] = ApiClient.convertToType(data['token_scope_public'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_read')) {
                obj['token_scope_read'] = ApiClient.convertToType(data['token_scope_read'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_write')) {
                obj['token_scope_write'] = ApiClient.convertToType(data['token_scope_write'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_project')) {
                obj['token_scope_project'] = ApiClient.convertToType(data['token_scope_project'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_admin')) {
                obj['token_scope_admin'] = ApiClient.convertToType(data['token_scope_admin'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_dev')) {
                obj['token_scope_dev'] = ApiClient.convertToType(data['token_scope_dev'], 'Boolean');
            }
            if (data.hasOwnProperty('api_enable')) {
                obj['api_enable'] = ApiClient.convertToType(data['api_enable'], 'Boolean');
            }
            if (data.hasOwnProperty('api_username')) {
                obj['api_username'] = ApiClient.convertToType(data['api_username'], 'String');
            }
            if (data.hasOwnProperty('api_require_password_change')) {
                obj['api_require_password_change'] = ApiClient.convertToType(data['api_require_password_change'], 'Boolean');
            }
            if (data.hasOwnProperty('api_access_token_ttl')) {
                obj['api_access_token_ttl'] = ApiClient.convertToType(data['api_access_token_ttl'], 'Number');
            }
            if (data.hasOwnProperty('api_token_scope_pending')) {
                obj['api_token_scope_pending'] = ApiClient.convertToType(data['api_token_scope_pending'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_public')) {
                obj['api_token_scope_public'] = ApiClient.convertToType(data['api_token_scope_public'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_read')) {
                obj['api_token_scope_read'] = ApiClient.convertToType(data['api_token_scope_read'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_write')) {
                obj['api_token_scope_write'] = ApiClient.convertToType(data['api_token_scope_write'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_project')) {
                obj['api_token_scope_project'] = ApiClient.convertToType(data['api_token_scope_project'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_admin')) {
                obj['api_token_scope_admin'] = ApiClient.convertToType(data['api_token_scope_admin'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_dev')) {
                obj['api_token_scope_dev'] = ApiClient.convertToType(data['api_token_scope_dev'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_enable')) {
                obj['mcp_enable'] = ApiClient.convertToType(data['mcp_enable'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_username')) {
                obj['mcp_username'] = ApiClient.convertToType(data['mcp_username'], 'String');
            }
            if (data.hasOwnProperty('mcp_require_password_change')) {
                obj['mcp_require_password_change'] = ApiClient.convertToType(data['mcp_require_password_change'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_access_token_ttl')) {
                obj['mcp_access_token_ttl'] = ApiClient.convertToType(data['mcp_access_token_ttl'], 'Number');
            }
            if (data.hasOwnProperty('mcp_token_scope_pending')) {
                obj['mcp_token_scope_pending'] = ApiClient.convertToType(data['mcp_token_scope_pending'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_public')) {
                obj['mcp_token_scope_public'] = ApiClient.convertToType(data['mcp_token_scope_public'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_read')) {
                obj['mcp_token_scope_read'] = ApiClient.convertToType(data['mcp_token_scope_read'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_write')) {
                obj['mcp_token_scope_write'] = ApiClient.convertToType(data['mcp_token_scope_write'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_project')) {
                obj['mcp_token_scope_project'] = ApiClient.convertToType(data['mcp_token_scope_project'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_admin')) {
                obj['mcp_token_scope_admin'] = ApiClient.convertToType(data['mcp_token_scope_admin'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_dev')) {
                obj['mcp_token_scope_dev'] = ApiClient.convertToType(data['mcp_token_scope_dev'], 'Boolean');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserSystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserSystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['first_name'] && !(typeof data['first_name'] === 'string' || data['first_name'] instanceof String)) {
            throw new Error("Expected the field `first_name` to be a primitive type in the JSON string but got " + data['first_name']);
        }
        // ensure the json data is a string
        if (data['surname'] && !(typeof data['surname'] === 'string' || data['surname'] instanceof String)) {
            throw new Error("Expected the field `surname` to be a primitive type in the JSON string but got " + data['surname']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['api_username'] && !(typeof data['api_username'] === 'string' || data['api_username'] instanceof String)) {
            throw new Error("Expected the field `api_username` to be a primitive type in the JSON string but got " + data['api_username']);
        }
        // ensure the json data is a string
        if (data['mcp_username'] && !(typeof data['mcp_username'] === 'string' || data['mcp_username'] instanceof String)) {
            throw new Error("Expected the field `mcp_username` to be a primitive type in the JSON string but got " + data['mcp_username']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying this user.
 * @member {Number} pk
 */
UserSystem.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created. Use this instead of pk when referencing the user outside the database.
 * @member {String} uuid
 */
UserSystem.prototype['uuid'] = undefined;

/**
 * Given name of the user, shown together with surname wherever the user is named in the user interface. Not used for authentication.
 * @member {String} first_name
 */
UserSystem.prototype['first_name'] = undefined;

/**
 * Family name of the user, shown together with first_name wherever the user is named in the user interface. Not used for authentication.
 * @member {String} surname
 */
UserSystem.prototype['surname'] = undefined;

/**
 * Email address of the user, used for contact and notifications. Not accepted as a login name — authentication goes through username. Deliberately not unique, so several accounts may share one address.
 * @member {String} email
 */
UserSystem.prototype['email'] = undefined;

/**
 * Profile picture of the user as raw bytes (bytea), kept in the row itself rather than as a path to a file, so an account carries its picture with it. NULL while no picture has been set. Not indexed and never searched — it is only ever read back whole.
 * @member {File} img_avatar
 */
UserSystem.prototype['img_avatar'] = undefined;

/**
 * Whether the account is active and allowed to authenticate against the REST API. Checked both when a token is issued and again on every request, so setting it to false locks the account out at once — including through tokens it already holds — without deleting the row.
 * @member {Boolean} enable
 * @default false
 */
UserSystem.prototype['enable'] = false;

/**
 * Login name of the user for the REST API, unique across all users and carried as the `sub` claim of every token issued to it. Required for authentication; a NULL username can never log in.
 * @member {String} username
 */
UserSystem.prototype['username'] = undefined;

/**
 * Level of the user interface this account is presented at, from 'none' (the default) up to 'dev'. A single rung named after the scope of the same name, so the interface an account is shown can be stated in one value. Not an authorisation control: what a token may do is decided by the token_scope_* columns alone, so raising this level widens no permission. Read by /auth/password-check, which reports it alongside the verdict on the credentials.
 * @member {module:model/UsrUiLevelType} ui_level
 */
UserSystem.prototype['ui_level'] = undefined;

/**
 * Whether the user has to replace the REST API password before using the account further, as after a password was assigned to it rather than chosen. Set by /auth/sign-up and /auth/password-reset, which assign a generated password, and cleared by /auth/password-change, which is the only endpoint that clears it. Reported by /auth/password-check and GET /auth/me so a client can send the user to change their password; it is not itself enforced, so an account that ignores it still authenticates and still holds its scopes.
 * @member {Boolean} require_password_change
 * @default false
 */
UserSystem.prototype['require_password_change'] = false;

/**
 * Lifetime in seconds of an access token issued to this user for the REST API. NULL falls back to auth_service.DEFAULT_ACCESS_TOKEN_TTL (24 hours). The lower bound of 1 keeps a stored 0 from issuing tokens that are already expired. Ignored for a token of type ui, which is always given auth_service.UI_ACCESS_TOKEN_TTL (18 hours) instead, since the interface has no way to refresh one.
 * @member {Number} access_token_ttl
 */
UserSystem.prototype['access_token_ttl'] = undefined;

/**
 * Whether the 'pending' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_pending
 * @default false
 */
UserSystem.prototype['token_scope_pending'] = false;

/**
 * Whether the 'public' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_public
 * @default false
 */
UserSystem.prototype['token_scope_public'] = false;

/**
 * Whether the 'read' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_read
 * @default false
 */
UserSystem.prototype['token_scope_read'] = false;

/**
 * Whether the 'write' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_write
 * @default false
 */
UserSystem.prototype['token_scope_write'] = false;

/**
 * Whether the 'project' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_project
 * @default false
 */
UserSystem.prototype['token_scope_project'] = false;

/**
 * Whether the 'admin' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_admin
 * @default false
 */
UserSystem.prototype['token_scope_admin'] = false;

/**
 * Whether the 'dev' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_dev
 * @default false
 */
UserSystem.prototype['token_scope_dev'] = false;

/**
 * Whether the account is active and allowed to authenticate as a programmatic REST client, the 'api' channel of UsrTokenType. The counterpart of `enable` for that channel, so a script or integration can be locked out without touching the login the user interface goes through. Reserved: every login is still checked against `enable` today, and no code path in this API reads this column.
 * @member {Boolean} api_enable
 * @default false
 */
UserSystem.prototype['api_enable'] = false;

/**
 * Login name of the user as a programmatic REST client, unique across all users; a NULL username can never log in there. A column of its own, so the name a script authenticates under can be changed without touching `username`. Reserved: no code path in this API reads this column, and the seed leaves it unset.
 * @member {String} api_username
 */
UserSystem.prototype['api_username'] = undefined;

/**
 * Whether the user has to replace the programmatic REST client password before using that channel further. The counterpart of `require_password_change` for the 'api' channel, so the two credentials can be expired independently. Reserved: no code path in this API reads this column.
 * @member {Boolean} api_require_password_change
 * @default false
 */
UserSystem.prototype['api_require_password_change'] = false;

/**
 * Lifetime in seconds of an access token issued to this user as a programmatic REST client. NULL falls back to auth_service.DEFAULT_ACCESS_TOKEN_TTL (24 hours). The lower bound of 1 keeps a stored 0 from issuing tokens that are already expired. Reserved: a token of type api is still given the lifetime from `access_token_ttl` today.
 * @member {Number} api_access_token_ttl
 */
UserSystem.prototype['api_access_token_ttl'] = undefined;

/**
 * Whether the 'pending' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_pending
 * @default false
 */
UserSystem.prototype['api_token_scope_pending'] = false;

/**
 * Whether the 'public' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_public
 * @default false
 */
UserSystem.prototype['api_token_scope_public'] = false;

/**
 * Whether the 'read' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_read
 * @default false
 */
UserSystem.prototype['api_token_scope_read'] = false;

/**
 * Whether the 'write' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_write
 * @default false
 */
UserSystem.prototype['api_token_scope_write'] = false;

/**
 * Whether the 'project' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_project
 * @default false
 */
UserSystem.prototype['api_token_scope_project'] = false;

/**
 * Whether the 'admin' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_admin
 * @default false
 */
UserSystem.prototype['api_token_scope_admin'] = false;

/**
 * Whether the 'dev' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_dev
 * @default false
 */
UserSystem.prototype['api_token_scope_dev'] = false;

/**
 * Whether the account is active and allowed to authenticate against the MCP server. The counterpart of `enable` for the MCP channel, so MCP access can be locked out without touching REST API access. Reserved as far as this API goes: no code path here reads this column, since the MCP server is a separate application.
 * @member {Boolean} mcp_enable
 * @default false
 */
UserSystem.prototype['mcp_enable'] = false;

/**
 * Login name of the user for the MCP server, unique across all users; a NULL username can never log in there. Seeded as a copy of `username`, but a column of its own, so MCP access can be renamed without touching the API login. Reserved as far as this API goes: no code path here reads this column.
 * @member {String} mcp_username
 */
UserSystem.prototype['mcp_username'] = undefined;

/**
 * Whether the user has to replace the MCP server password before using MCP access further. The counterpart of `require_password_change` for the MCP channel, so the two credentials can be expired independently. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_require_password_change
 * @default false
 */
UserSystem.prototype['mcp_require_password_change'] = false;

/**
 * Lifetime in seconds of an access token issued to this user for the MCP server. NULL falls back to auth_service.DEFAULT_ACCESS_TOKEN_TTL (24 hours). The lower bound of 1 keeps a stored 0 from issuing tokens that are already expired. Reserved: no code path in this API reads this column.
 * @member {Number} mcp_access_token_ttl
 */
UserSystem.prototype['mcp_access_token_ttl'] = undefined;

/**
 * Whether the 'pending' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_pending
 * @default false
 */
UserSystem.prototype['mcp_token_scope_pending'] = false;

/**
 * Whether the 'public' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_public
 * @default false
 */
UserSystem.prototype['mcp_token_scope_public'] = false;

/**
 * Whether the 'read' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_read
 * @default false
 */
UserSystem.prototype['mcp_token_scope_read'] = false;

/**
 * Whether the 'write' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_write
 * @default false
 */
UserSystem.prototype['mcp_token_scope_write'] = false;

/**
 * Whether the 'project' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_project
 * @default false
 */
UserSystem.prototype['mcp_token_scope_project'] = false;

/**
 * Whether the 'admin' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_admin
 * @default false
 */
UserSystem.prototype['mcp_token_scope_admin'] = false;

/**
 * Whether the 'dev' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_dev
 * @default false
 */
UserSystem.prototype['mcp_token_scope_dev'] = false;

/**
 * Marks the row as written through an API endpoint and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/DataStateType} data_state
 */
UserSystem.prototype['data_state'] = undefined;

/**
 * Marks the row as written by an MCP agent and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
UserSystem.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about this user. Not interpreted by the application.
 * @member {String} comments
 */
UserSystem.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes about this user, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} notes
 */
UserSystem.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata about this user, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} metadatas
 */
UserSystem.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
UserSystem.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update by the set_updated_at() trigger. Used for optimistic locking by the API.
 * @member {Date} updated_at
 */
UserSystem.prototype['updated_at'] = undefined;






export { UserSystem };

/**
 * The UserView model module.
 * @module model/UserView
 * @version 0.9.0
 */
class UserView {
    /**
     * Constructs a new <code>UserView</code>.
     * User row extended with its organization membership and its nested tokens.
     * @alias module:model/UserView
     */
    constructor() { 
        
        UserView.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['enable'] = false;
        obj['require_password_change'] = false;
        obj['token_scope_pending'] = false;
        obj['token_scope_public'] = false;
        obj['token_scope_read'] = false;
        obj['token_scope_write'] = false;
        obj['token_scope_project'] = false;
        obj['token_scope_admin'] = false;
        obj['token_scope_dev'] = false;
        obj['api_enable'] = false;
        obj['api_require_password_change'] = false;
        obj['api_token_scope_pending'] = false;
        obj['api_token_scope_public'] = false;
        obj['api_token_scope_read'] = false;
        obj['api_token_scope_write'] = false;
        obj['api_token_scope_project'] = false;
        obj['api_token_scope_admin'] = false;
        obj['api_token_scope_dev'] = false;
        obj['mcp_enable'] = false;
        obj['mcp_require_password_change'] = false;
        obj['mcp_token_scope_pending'] = false;
        obj['mcp_token_scope_public'] = false;
        obj['mcp_token_scope_read'] = false;
        obj['mcp_token_scope_write'] = false;
        obj['mcp_token_scope_project'] = false;
        obj['mcp_token_scope_admin'] = false;
        obj['mcp_token_scope_dev'] = false;
    }

    /**
     * Constructs a <code>UserView</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserView} obj Optional instance to populate.
     * @return {module:model/UserView} The populated <code>UserView</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserView();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('first_name')) {
                obj['first_name'] = ApiClient.convertToType(data['first_name'], 'String');
            }
            if (data.hasOwnProperty('surname')) {
                obj['surname'] = ApiClient.convertToType(data['surname'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('img_avatar')) {
                obj['img_avatar'] = ApiClient.convertToType(data['img_avatar'], File);
            }
            if (data.hasOwnProperty('enable')) {
                obj['enable'] = ApiClient.convertToType(data['enable'], 'Boolean');
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('ui_level')) {
                obj['ui_level'] = ApiClient.convertToType(data['ui_level'], UsrUiLevelType);
            }
            if (data.hasOwnProperty('require_password_change')) {
                obj['require_password_change'] = ApiClient.convertToType(data['require_password_change'], 'Boolean');
            }
            if (data.hasOwnProperty('access_token_ttl')) {
                obj['access_token_ttl'] = ApiClient.convertToType(data['access_token_ttl'], 'Number');
            }
            if (data.hasOwnProperty('token_scope_pending')) {
                obj['token_scope_pending'] = ApiClient.convertToType(data['token_scope_pending'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_public')) {
                obj['token_scope_public'] = ApiClient.convertToType(data['token_scope_public'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_read')) {
                obj['token_scope_read'] = ApiClient.convertToType(data['token_scope_read'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_write')) {
                obj['token_scope_write'] = ApiClient.convertToType(data['token_scope_write'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_project')) {
                obj['token_scope_project'] = ApiClient.convertToType(data['token_scope_project'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_admin')) {
                obj['token_scope_admin'] = ApiClient.convertToType(data['token_scope_admin'], 'Boolean');
            }
            if (data.hasOwnProperty('token_scope_dev')) {
                obj['token_scope_dev'] = ApiClient.convertToType(data['token_scope_dev'], 'Boolean');
            }
            if (data.hasOwnProperty('api_enable')) {
                obj['api_enable'] = ApiClient.convertToType(data['api_enable'], 'Boolean');
            }
            if (data.hasOwnProperty('api_username')) {
                obj['api_username'] = ApiClient.convertToType(data['api_username'], 'String');
            }
            if (data.hasOwnProperty('api_require_password_change')) {
                obj['api_require_password_change'] = ApiClient.convertToType(data['api_require_password_change'], 'Boolean');
            }
            if (data.hasOwnProperty('api_access_token_ttl')) {
                obj['api_access_token_ttl'] = ApiClient.convertToType(data['api_access_token_ttl'], 'Number');
            }
            if (data.hasOwnProperty('api_token_scope_pending')) {
                obj['api_token_scope_pending'] = ApiClient.convertToType(data['api_token_scope_pending'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_public')) {
                obj['api_token_scope_public'] = ApiClient.convertToType(data['api_token_scope_public'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_read')) {
                obj['api_token_scope_read'] = ApiClient.convertToType(data['api_token_scope_read'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_write')) {
                obj['api_token_scope_write'] = ApiClient.convertToType(data['api_token_scope_write'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_project')) {
                obj['api_token_scope_project'] = ApiClient.convertToType(data['api_token_scope_project'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_admin')) {
                obj['api_token_scope_admin'] = ApiClient.convertToType(data['api_token_scope_admin'], 'Boolean');
            }
            if (data.hasOwnProperty('api_token_scope_dev')) {
                obj['api_token_scope_dev'] = ApiClient.convertToType(data['api_token_scope_dev'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_enable')) {
                obj['mcp_enable'] = ApiClient.convertToType(data['mcp_enable'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_username')) {
                obj['mcp_username'] = ApiClient.convertToType(data['mcp_username'], 'String');
            }
            if (data.hasOwnProperty('mcp_require_password_change')) {
                obj['mcp_require_password_change'] = ApiClient.convertToType(data['mcp_require_password_change'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_access_token_ttl')) {
                obj['mcp_access_token_ttl'] = ApiClient.convertToType(data['mcp_access_token_ttl'], 'Number');
            }
            if (data.hasOwnProperty('mcp_token_scope_pending')) {
                obj['mcp_token_scope_pending'] = ApiClient.convertToType(data['mcp_token_scope_pending'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_public')) {
                obj['mcp_token_scope_public'] = ApiClient.convertToType(data['mcp_token_scope_public'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_read')) {
                obj['mcp_token_scope_read'] = ApiClient.convertToType(data['mcp_token_scope_read'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_write')) {
                obj['mcp_token_scope_write'] = ApiClient.convertToType(data['mcp_token_scope_write'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_project')) {
                obj['mcp_token_scope_project'] = ApiClient.convertToType(data['mcp_token_scope_project'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_admin')) {
                obj['mcp_token_scope_admin'] = ApiClient.convertToType(data['mcp_token_scope_admin'], 'Boolean');
            }
            if (data.hasOwnProperty('mcp_token_scope_dev')) {
                obj['mcp_token_scope_dev'] = ApiClient.convertToType(data['mcp_token_scope_dev'], 'Boolean');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('usr_2_org')) {
                obj['usr_2_org'] = ApiClient.convertToType(data['usr_2_org'], Usr2OrgSystem);
            }
            if (data.hasOwnProperty('tokens')) {
                obj['tokens'] = ApiClient.convertToType(data['tokens'], [TokenSystem]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserView</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserView</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['first_name'] && !(typeof data['first_name'] === 'string' || data['first_name'] instanceof String)) {
            throw new Error("Expected the field `first_name` to be a primitive type in the JSON string but got " + data['first_name']);
        }
        // ensure the json data is a string
        if (data['surname'] && !(typeof data['surname'] === 'string' || data['surname'] instanceof String)) {
            throw new Error("Expected the field `surname` to be a primitive type in the JSON string but got " + data['surname']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['api_username'] && !(typeof data['api_username'] === 'string' || data['api_username'] instanceof String)) {
            throw new Error("Expected the field `api_username` to be a primitive type in the JSON string but got " + data['api_username']);
        }
        // ensure the json data is a string
        if (data['mcp_username'] && !(typeof data['mcp_username'] === 'string' || data['mcp_username'] instanceof String)) {
            throw new Error("Expected the field `mcp_username` to be a primitive type in the JSON string but got " + data['mcp_username']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }
        // validate the optional field `usr_2_org`
        if (data['usr_2_org']) { // data not null
          Usr2OrgSystem.validateJSON(data['usr_2_org']);
        }
        if (data['tokens']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tokens'])) {
                throw new Error("Expected the field `tokens` to be an array in the JSON data but got " + data['tokens']);
            }
            // validate the optional field `tokens` (array)
            for (const item of data['tokens']) {
                TokenSystem.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying this user.
 * @member {Number} pk
 */
UserView.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created. Use this instead of pk when referencing the user outside the database.
 * @member {String} uuid
 */
UserView.prototype['uuid'] = undefined;

/**
 * Given name of the user, shown together with surname wherever the user is named in the user interface. Not used for authentication.
 * @member {String} first_name
 */
UserView.prototype['first_name'] = undefined;

/**
 * Family name of the user, shown together with first_name wherever the user is named in the user interface. Not used for authentication.
 * @member {String} surname
 */
UserView.prototype['surname'] = undefined;

/**
 * Email address of the user, used for contact and notifications. Not accepted as a login name — authentication goes through username. Deliberately not unique, so several accounts may share one address.
 * @member {String} email
 */
UserView.prototype['email'] = undefined;

/**
 * Profile picture of the user as raw bytes (bytea), kept in the row itself rather than as a path to a file, so an account carries its picture with it. NULL while no picture has been set. Not indexed and never searched — it is only ever read back whole.
 * @member {File} img_avatar
 */
UserView.prototype['img_avatar'] = undefined;

/**
 * Whether the account is active and allowed to authenticate against the REST API. Checked both when a token is issued and again on every request, so setting it to false locks the account out at once — including through tokens it already holds — without deleting the row.
 * @member {Boolean} enable
 * @default false
 */
UserView.prototype['enable'] = false;

/**
 * Login name of the user for the REST API, unique across all users and carried as the `sub` claim of every token issued to it. Required for authentication; a NULL username can never log in.
 * @member {String} username
 */
UserView.prototype['username'] = undefined;

/**
 * Level of the user interface this account is presented at, from 'none' (the default) up to 'dev'. A single rung named after the scope of the same name, so the interface an account is shown can be stated in one value. Not an authorisation control: what a token may do is decided by the token_scope_* columns alone, so raising this level widens no permission. Read by /auth/password-check, which reports it alongside the verdict on the credentials.
 * @member {module:model/UsrUiLevelType} ui_level
 */
UserView.prototype['ui_level'] = undefined;

/**
 * Whether the user has to replace the REST API password before using the account further, as after a password was assigned to it rather than chosen. Set by /auth/sign-up and /auth/password-reset, which assign a generated password, and cleared by /auth/password-change, which is the only endpoint that clears it. Reported by /auth/password-check and GET /auth/me so a client can send the user to change their password; it is not itself enforced, so an account that ignores it still authenticates and still holds its scopes.
 * @member {Boolean} require_password_change
 * @default false
 */
UserView.prototype['require_password_change'] = false;

/**
 * Lifetime in seconds of an access token issued to this user for the REST API. NULL falls back to auth_service.DEFAULT_ACCESS_TOKEN_TTL (24 hours). The lower bound of 1 keeps a stored 0 from issuing tokens that are already expired. Ignored for a token of type ui, which is always given auth_service.UI_ACCESS_TOKEN_TTL (18 hours) instead, since the interface has no way to refresh one.
 * @member {Number} access_token_ttl
 */
UserView.prototype['access_token_ttl'] = undefined;

/**
 * Whether the 'pending' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_pending
 * @default false
 */
UserView.prototype['token_scope_pending'] = false;

/**
 * Whether the 'public' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_public
 * @default false
 */
UserView.prototype['token_scope_public'] = false;

/**
 * Whether the 'read' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_read
 * @default false
 */
UserView.prototype['token_scope_read'] = false;

/**
 * Whether the 'write' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_write
 * @default false
 */
UserView.prototype['token_scope_write'] = false;

/**
 * Whether the 'project' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_project
 * @default false
 */
UserView.prototype['token_scope_project'] = false;

/**
 * Whether the 'admin' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_admin
 * @default false
 */
UserView.prototype['token_scope_admin'] = false;

/**
 * Whether the 'dev' scope may be granted to a REST API access token for this user. Read by auth_service.user_scopes() when a token is issued and again on every request, so clearing it withdraws the scope from the tokens the account already holds.
 * @member {Boolean} token_scope_dev
 * @default false
 */
UserView.prototype['token_scope_dev'] = false;

/**
 * Whether the account is active and allowed to authenticate as a programmatic REST client, the 'api' channel of UsrTokenType. The counterpart of `enable` for that channel, so a script or integration can be locked out without touching the login the user interface goes through. Reserved: every login is still checked against `enable` today, and no code path in this API reads this column.
 * @member {Boolean} api_enable
 * @default false
 */
UserView.prototype['api_enable'] = false;

/**
 * Login name of the user as a programmatic REST client, unique across all users; a NULL username can never log in there. A column of its own, so the name a script authenticates under can be changed without touching `username`. Reserved: no code path in this API reads this column, and the seed leaves it unset.
 * @member {String} api_username
 */
UserView.prototype['api_username'] = undefined;

/**
 * Whether the user has to replace the programmatic REST client password before using that channel further. The counterpart of `require_password_change` for the 'api' channel, so the two credentials can be expired independently. Reserved: no code path in this API reads this column.
 * @member {Boolean} api_require_password_change
 * @default false
 */
UserView.prototype['api_require_password_change'] = false;

/**
 * Lifetime in seconds of an access token issued to this user as a programmatic REST client. NULL falls back to auth_service.DEFAULT_ACCESS_TOKEN_TTL (24 hours). The lower bound of 1 keeps a stored 0 from issuing tokens that are already expired. Reserved: a token of type api is still given the lifetime from `access_token_ttl` today.
 * @member {Number} api_access_token_ttl
 */
UserView.prototype['api_access_token_ttl'] = undefined;

/**
 * Whether the 'pending' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_pending
 * @default false
 */
UserView.prototype['api_token_scope_pending'] = false;

/**
 * Whether the 'public' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_public
 * @default false
 */
UserView.prototype['api_token_scope_public'] = false;

/**
 * Whether the 'read' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_read
 * @default false
 */
UserView.prototype['api_token_scope_read'] = false;

/**
 * Whether the 'write' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_write
 * @default false
 */
UserView.prototype['api_token_scope_write'] = false;

/**
 * Whether the 'project' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_project
 * @default false
 */
UserView.prototype['api_token_scope_project'] = false;

/**
 * Whether the 'admin' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_admin
 * @default false
 */
UserView.prototype['api_token_scope_admin'] = false;

/**
 * Whether the 'dev' scope may be granted to a programmatic REST client access token for this user. Reserved: auth_service.user_scopes() reads the unprefixed token_scope_* columns whatever the channel, so this column grants nothing yet.
 * @member {Boolean} api_token_scope_dev
 * @default false
 */
UserView.prototype['api_token_scope_dev'] = false;

/**
 * Whether the account is active and allowed to authenticate against the MCP server. The counterpart of `enable` for the MCP channel, so MCP access can be locked out without touching REST API access. Reserved as far as this API goes: no code path here reads this column, since the MCP server is a separate application.
 * @member {Boolean} mcp_enable
 * @default false
 */
UserView.prototype['mcp_enable'] = false;

/**
 * Login name of the user for the MCP server, unique across all users; a NULL username can never log in there. Seeded as a copy of `username`, but a column of its own, so MCP access can be renamed without touching the API login. Reserved as far as this API goes: no code path here reads this column.
 * @member {String} mcp_username
 */
UserView.prototype['mcp_username'] = undefined;

/**
 * Whether the user has to replace the MCP server password before using MCP access further. The counterpart of `require_password_change` for the MCP channel, so the two credentials can be expired independently. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_require_password_change
 * @default false
 */
UserView.prototype['mcp_require_password_change'] = false;

/**
 * Lifetime in seconds of an access token issued to this user for the MCP server. NULL falls back to auth_service.DEFAULT_ACCESS_TOKEN_TTL (24 hours). The lower bound of 1 keeps a stored 0 from issuing tokens that are already expired. Reserved: no code path in this API reads this column.
 * @member {Number} mcp_access_token_ttl
 */
UserView.prototype['mcp_access_token_ttl'] = undefined;

/**
 * Whether the 'pending' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_pending
 * @default false
 */
UserView.prototype['mcp_token_scope_pending'] = false;

/**
 * Whether the 'public' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_public
 * @default false
 */
UserView.prototype['mcp_token_scope_public'] = false;

/**
 * Whether the 'read' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_read
 * @default false
 */
UserView.prototype['mcp_token_scope_read'] = false;

/**
 * Whether the 'write' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_write
 * @default false
 */
UserView.prototype['mcp_token_scope_write'] = false;

/**
 * Whether the 'project' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_project
 * @default false
 */
UserView.prototype['mcp_token_scope_project'] = false;

/**
 * Whether the 'admin' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_admin
 * @default false
 */
UserView.prototype['mcp_token_scope_admin'] = false;

/**
 * Whether the 'dev' scope may be granted to an MCP server access token for this user. Reserved: no code path in this API reads this column.
 * @member {Boolean} mcp_token_scope_dev
 * @default false
 */
UserView.prototype['mcp_token_scope_dev'] = false;

/**
 * Marks the row as written through an API endpoint and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/DataStateType} data_state
 */
UserView.prototype['data_state'] = undefined;

/**
 * Marks the row as written by an MCP agent and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
UserView.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about this user. Not interpreted by the application.
 * @member {String} comments
 */
UserView.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes about this user, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} notes
 */
UserView.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata about this user, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} metadatas
 */
UserView.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
UserView.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update by the set_updated_at() trigger. Used for optimistic locking by the API.
 * @member {Date} updated_at
 */
UserView.prototype['updated_at'] = undefined;

/**
 * Membership row linking the user to the organization
 * @member {module:model/Usr2OrgSystem} usr_2_org
 */
UserView.prototype['usr_2_org'] = undefined;

/**
 * Tokens issued to the user
 * @member {Array.<module:model/TokenSystem>} tokens
 */
UserView.prototype['tokens'] = undefined;






export { UserView };

/**
 * The UsersSystem model module.
 * @module model/UsersSystem
 * @version 0.9.0
 */
class UsersSystem {
    /**
     * Constructs a new <code>UsersSystem</code>.
     * User result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/UsersSystem
     */
    constructor() { 
        
        UsersSystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>UsersSystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UsersSystem} obj Optional instance to populate.
     * @return {module:model/UsersSystem} The populated <code>UsersSystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UsersSystem();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [UserSystem]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UsersSystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UsersSystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                UserSystem.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
UsersSystem.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
UsersSystem.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
UsersSystem.prototype['offset'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
UsersSystem.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
UsersSystem.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
UsersSystem.prototype['all'] = undefined;

/**
 * User rows
 * @member {Array.<module:model/UserSystem>} rows
 */
UsersSystem.prototype['rows'] = undefined;






export { UsersSystem };

/**
 * The Usr2OrgQuerySystem model module.
 * @module model/Usr2OrgQuerySystem
 * @version 0.9.0
 */
class Usr2OrgQuerySystem {
    /**
     * Constructs a new <code>Usr2OrgQuerySystem</code>.
     * Query payload for user to organization rows: SQL WHERE clause plus limit/offset pagination.
     * @alias module:model/Usr2OrgQuerySystem
     */
    constructor() { 
        
        Usr2OrgQuerySystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Usr2OrgQuerySystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Usr2OrgQuerySystem} obj Optional instance to populate.
     * @return {module:model/Usr2OrgQuerySystem} The populated <code>Usr2OrgQuerySystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Usr2OrgQuerySystem();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Usr2OrgQuerySystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Usr2OrgQuerySystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Usr2OrgQuerySystem.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Usr2OrgQuerySystem.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Usr2OrgQuerySystem.prototype['offset'] = undefined;






export { Usr2OrgQuerySystem };

/**
 * The Usr2OrgSystem model module.
 * @module model/Usr2OrgSystem
 * @version 0.9.0
 */
class Usr2OrgSystem {
    /**
     * Constructs a new <code>Usr2OrgSystem</code>.
     * Link table assigning users to organizations (many-to-many).  A user may belong to several organizations and an organization may have many users; each membership is one row here. Both foreign keys cascade on delete, so removing a user or an organization removes its memberships with it.  Every INSERT, UPDATE and DELETE on this table is captured in &#x60;&#x60;log_entries&#x60;&#x60; by the &#x60;&#x60;system.log_change()&#x60;&#x60; trigger.
     * @alias module:model/Usr2OrgSystem
     */
    constructor() { 
        
        Usr2OrgSystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Usr2OrgSystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Usr2OrgSystem} obj Optional instance to populate.
     * @return {module:model/Usr2OrgSystem} The populated <code>Usr2OrgSystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Usr2OrgSystem();

            if (data.hasOwnProperty('pk')) {
                obj['pk'] = ApiClient.convertToType(data['pk'], 'Number');
            }
            if (data.hasOwnProperty('uuid')) {
                obj['uuid'] = ApiClient.convertToType(data['uuid'], 'String');
            }
            if (data.hasOwnProperty('fk_organization')) {
                obj['fk_organization'] = ApiClient.convertToType(data['fk_organization'], 'Number');
            }
            if (data.hasOwnProperty('fk_user')) {
                obj['fk_user'] = ApiClient.convertToType(data['fk_user'], 'Number');
            }
            if (data.hasOwnProperty('data_state')) {
                obj['data_state'] = ApiClient.convertToType(data['data_state'], DataStateType);
            }
            if (data.hasOwnProperty('mcp_data_state')) {
                obj['mcp_data_state'] = ApiClient.convertToType(data['mcp_data_state'], McpDataStateType);
            }
            if (data.hasOwnProperty('comments')) {
                obj['comments'] = ApiClient.convertToType(data['comments'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], {'String': Object});
            }
            if (data.hasOwnProperty('metadatas')) {
                obj['metadatas'] = ApiClient.convertToType(data['metadatas'], {'String': Object});
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Usr2OrgSystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Usr2OrgSystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['uuid'] && !(typeof data['uuid'] === 'string' || data['uuid'] instanceof String)) {
            throw new Error("Expected the field `uuid` to be a primitive type in the JSON string but got " + data['uuid']);
        }
        // ensure the json data is a string
        if (data['comments'] && !(typeof data['comments'] === 'string' || data['comments'] instanceof String)) {
            throw new Error("Expected the field `comments` to be a primitive type in the JSON string but got " + data['comments']);
        }

        return true;
    }


}



/**
 * Auto-generated surrogate primary key uniquely identifying this user-to-organization link.
 * @member {Number} pk
 */
Usr2OrgSystem.prototype['pk'] = undefined;

/**
 * Globally unique identifier generated by the database when the row is created. Use this instead of pk when referencing the link outside the database.
 * @member {String} uuid
 */
Usr2OrgSystem.prototype['uuid'] = undefined;

/**
 * Foreign key referencing the linked organization; the link is deleted when that organization is deleted.
 * @member {Number} fk_organization
 */
Usr2OrgSystem.prototype['fk_organization'] = undefined;

/**
 * Foreign key referencing the linked user; the link is deleted when that user is deleted.
 * @member {Number} fk_user
 */
Usr2OrgSystem.prototype['fk_user'] = undefined;

/**
 * Marks the row as written through an API endpoint and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/DataStateType} data_state
 */
Usr2OrgSystem.prototype['data_state'] = undefined;

/**
 * Marks the row as written by an MCP agent and pending human review: 'created', 'updated', 'deletion_preparation' or 'undefined'. NULL once the row needs no review.
 * @member {module:model/McpDataStateType} mcp_data_state
 */
Usr2OrgSystem.prototype['mcp_data_state'] = undefined;

/**
 * Free-form comments about this user-to-organization link. Not interpreted by the application.
 * @member {String} comments
 */
Usr2OrgSystem.prototype['comments'] = undefined;

/**
 * Arbitrary structured notes about this link, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} notes
 */
Usr2OrgSystem.prototype['notes'] = undefined;

/**
 * Arbitrary structured metadata about this link, stored as JSONB. Not interpreted by the application.
 * @member {Object.<String, Object>} metadatas
 */
Usr2OrgSystem.prototype['metadatas'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created.
 * @member {Date} created_at
 */
Usr2OrgSystem.prototype['created_at'] = undefined;

/**
 * Timestamp (with timezone) set by the database when the row was created and refreshed on every update by the set_updated_at() trigger. Used for optimistic locking by the API.
 * @member {Date} updated_at
 */
Usr2OrgSystem.prototype['updated_at'] = undefined;






export { Usr2OrgSystem };

/**
 * The Usr2OrgsSystem model module.
 * @module model/Usr2OrgsSystem
 * @version 0.9.0
 */
class Usr2OrgsSystem {
    /**
     * Constructs a new <code>Usr2OrgsSystem</code>.
     * User to organization result set: echoes the query and carries total/count plus the matching rows.
     * @alias module:model/Usr2OrgsSystem
     */
    constructor() { 
        
        Usr2OrgsSystem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['where'] = 'pk > 0';
    }

    /**
     * Constructs a <code>Usr2OrgsSystem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Usr2OrgsSystem} obj Optional instance to populate.
     * @return {module:model/Usr2OrgsSystem} The populated <code>Usr2OrgsSystem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Usr2OrgsSystem();

            if (data.hasOwnProperty('where')) {
                obj['where'] = ApiClient.convertToType(data['where'], 'String');
            }
            if (data.hasOwnProperty('limit')) {
                obj['limit'] = ApiClient.convertToType(data['limit'], 'Number');
            }
            if (data.hasOwnProperty('offset')) {
                obj['offset'] = ApiClient.convertToType(data['offset'], 'Number');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('all')) {
                obj['all'] = ApiClient.convertToType(data['all'], 'Number');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [Usr2OrgSystem]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Usr2OrgsSystem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Usr2OrgsSystem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['where'] && !(typeof data['where'] === 'string' || data['where'] instanceof String)) {
            throw new Error("Expected the field `where` to be a primitive type in the JSON string but got " + data['where']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                Usr2OrgSystem.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * SQL WHERE clause
 * @member {String} where
 * @default 'pk > 0'
 */
Usr2OrgsSystem.prototype['where'] = 'pk > 0';

/**
 * Maximum number of rows to return
 * @member {Number} limit
 */
Usr2OrgsSystem.prototype['limit'] = undefined;

/**
 * Number of rows to skip
 * @member {Number} offset
 */
Usr2OrgsSystem.prototype['offset'] = undefined;

/**
 * Total number of matching rows
 * @member {Number} total
 */
Usr2OrgsSystem.prototype['total'] = undefined;

/**
 * Number of rows returned
 * @member {Number} count
 */
Usr2OrgsSystem.prototype['count'] = undefined;

/**
 * Number of rows in the table, ignoring `where`: the pool `total` was narrowed down from. Equals `total` when `where` matches every row.
 * @member {Number} all
 */
Usr2OrgsSystem.prototype['all'] = undefined;

/**
 * User to organization rows
 * @member {Array.<module:model/Usr2OrgSystem>} rows
 */
Usr2OrgsSystem.prototype['rows'] = undefined;






export { Usr2OrgsSystem };

/**
* Enum class UsrTokenType.
* @enum {}
* @readonly
*/
export class UsrTokenType {
    
        /**
         * value: "ui"
         * @const
         */
        "UI" = "ui";

    
        /**
         * value: "api"
         * @const
         */
        "API" = "api";

    
        /**
         * value: "mcp"
         * @const
         */
        "MCP" = "mcp";

    

    /**
    * Returns a <code>UsrTokenType</code> enum value from a Javascript object name.
    * @param {Object} data The plain JavaScript object containing the name of the enum value.
    * @return {module:model/UsrTokenType} The enum <code>UsrTokenType</code> value.
    */
    static constructFromObject(object) {
        return object;
    }
}

/**
* Enum class UsrUiLevelType.
* @enum {}
* @readonly
*/
export class UsrUiLevelType {
    
        /**
         * value: "none"
         * @const
         */
        "none" = "none";

    
        /**
         * value: "pending"
         * @const
         */
        "pending" = "pending";

    
        /**
         * value: "public"
         * @const
         */
        "public" = "public";

    
        /**
         * value: "read"
         * @const
         */
        "read" = "read";

    
        /**
         * value: "write"
         * @const
         */
        "write" = "write";

    
        /**
         * value: "project"
         * @const
         */
        "project" = "project";

    
        /**
         * value: "admin"
         * @const
         */
        "admin" = "admin";

    
        /**
         * value: "dev"
         * @const
         */
        "dev" = "dev";

    

    /**
    * Returns a <code>UsrUiLevelType</code> enum value from a Javascript object name.
    * @param {Object} data The plain JavaScript object containing the name of the enum value.
    * @return {module:model/UsrUiLevelType} The enum <code>UsrUiLevelType</code> value.
    */
    static constructFromObject(object) {
        return object;
    }
}

/**
 * The ValidationError model module.
 * @module model/ValidationError
 * @version 0.9.0
 */
class ValidationError {
    /**
     * Constructs a new <code>ValidationError</code>.
     * @alias module:model/ValidationError
     * @param loc {Array.<module:model/LocationInner>} 
     * @param msg {String} 
     * @param type {String} 
     */
    constructor(loc, msg, type) { 
        
        ValidationError.initialize(this, loc, msg, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, loc, msg, type) { 
        obj['loc'] = loc;
        obj['msg'] = msg;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>ValidationError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ValidationError} obj Optional instance to populate.
     * @return {module:model/ValidationError} The populated <code>ValidationError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ValidationError();

            if (data.hasOwnProperty('loc')) {
                obj['loc'] = ApiClient.convertToType(data['loc'], [LocationInner]);
            }
            if (data.hasOwnProperty('msg')) {
                obj['msg'] = ApiClient.convertToType(data['msg'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('input')) {
                obj['input'] = ApiClient.convertToType(data['input'], Object);
            }
            if (data.hasOwnProperty('ctx')) {
                obj['ctx'] = ApiClient.convertToType(data['ctx'], Object);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ValidationError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ValidationError</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ValidationError.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['loc']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['loc'])) {
                throw new Error("Expected the field `loc` to be an array in the JSON data but got " + data['loc']);
            }
            // validate the optional field `loc` (array)
            for (const item of data['loc']) {
                LocationInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['msg'] && !(typeof data['msg'] === 'string' || data['msg'] instanceof String)) {
            throw new Error("Expected the field `msg` to be a primitive type in the JSON string but got " + data['msg']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

ValidationError.RequiredProperties = ["loc", "msg", "type"];

/**
 * @member {Array.<module:model/LocationInner>} loc
 */
ValidationError.prototype['loc'] = undefined;

/**
 * @member {String} msg
 */
ValidationError.prototype['msg'] = undefined;

/**
 * @member {String} type
 */
ValidationError.prototype['type'] = undefined;

/**
 * @member {Object} input
 */
ValidationError.prototype['input'] = undefined;

/**
 * @member {Object} ctx
 */
ValidationError.prototype['ctx'] = undefined;






export { ValidationError };

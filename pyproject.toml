[project]
name = "xedu-org-ui-template"
version = "0.0.1"
description = ""
authors = [
    { name = "Pascal Helfenstein", email = "pascal.helfenstein@xemax.ch" },
    { name = "Thomas Ruoss", email = "thomas.ruoss@xemax.ch" },
]
requires-python = ">=3.13"
readme = "README.md"
dependencies = [
    "datamodel-code-generator[ruff]>=0.75.1",
    "httpx>=0.28.1",
    "jinja2>=3.1.6",
    "openapi-generator-cli[jdk4py]>=7.25.0",
    "openpyxl>=3.1.5",
    "pyclean>=3.7.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.15.0",
    "python-docx>=1.2.0",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "watchdog>=6.0.0",
    "markdown>=3.10.3",
    "deepl>=1.32.0",
    "textual>=8.2.8",
    "rich>=15.0.0",
]

[tool.pytest.ini_options]
pythonpath = "."

# Install the project as a package so `uv run` puts the project root on
# sys.path. Without this, `uv run app/i18n/sync_translations.py` runs with only
# the script's own directory importable and `import app` fails.
# Example: uv run app/i18n/sync_translations.py
[tool.uv]
package = true
required-environments = [
    "sys_platform == 'linux' and platform_machine == 'x86_64'",
]

[tool.setuptools.packages.find]
include = ["app*", "core*"]

# uv add --dev ....
[dependency-groups]
dev = [
    "headroom-ai[all]>=0.36.5",
]
